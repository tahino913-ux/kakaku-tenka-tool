# 価格転嫁見積ツール — 作業メモ（CLAUDE.md）

このファイルは Claude Code が起動時に自動で読み込みます。
**会社PC・自宅PCのどちらで開いても、この内容で状況を把握できます。**
（※Claudeの詳細メモリは各PCローカルで同期されないため、引き継ぎ情報はこのファイルに集約しています）

## このツールは何か

メーカーからの値上げに対して、**得意先ごとの「転嫁後 販売単価」を自動計算し、見積書(Excel)を出力**するツール。
**Node.js 標準機能のみで動作**（外部ライブラリ・`npm install` 不要）。動かすには Node.js を入れるだけ。
**将来はクラウドSaaS化して他社にも販売する構想**（自社サーバーあり）。だから「買い手が自分で設定できる」形を重視。

## 使い方

> 📖 操作する人向けの詳しい手順は **`使い方手順書.md` / `使い方手順書.html`**（プロジェクト直下）にまとめてあります。sim画面ヘッダーの **「📖 使い方」ボタン**（→ `/manual` ルートが手順書HTMLを新タブ配信）からも開けます（2026-05-29 追加）。

- `sim.bat` … ブラウザでシミュレーション。右上に「⚙ 設定」「メーカー見積を取り込む」。
- `取込.bat` … メーカー見積の **Excel(.xlsx) をドラッグ＆ドロップ** → `maker_quotes/` にメーカー別CSVを作成（複数シート=複数メーカーに対応。中東のような形式もOK）。
- `照合.bat` … `maker_quotes/` のメーカー見積 × 販売実績 → `input/` に照合結果CSVを出力（**maker_quotes/ に置いた .xlsx は自動で取り込み**。販売実績が旧.XLSなら自動でCSV変換）。流れ：**.xlsxをmaker_quotes/に置く（または取込.bat）→ 照合.bat → sim.batで見積書出力**。貼り付け取込は /import 画面でも可。
- `run.bat` … `input/` のCSV → `output/` に分析用Excelを一括出力（CLI）。
- `得意先別.bat` … sim で出した **仕入先別** 見積書を、**得意先ごとに1枚へ統合**（同じ得意先が複数メーカーにまたがっても1枚にまとまる）。最新の仕入先別セットを自動で読み込み、`output/得意先別_見積書_<日時>/` に出力＋フォルダを自動で開く。**価格は再計算しない**（仕入先別の承認済み単価をそのままコピー）。

## コードの構成（src/）

| ファイル | 役割 |
|---|---|
| `server.js` | シミュレーション画面のサーバ（中核・最大）。設定API・見積出力・取り込みAPIもここ |
| `importPage.js` | 「メーカー見積 取り込み（貼り付け）」画面 `/import`。仕入先ピッカー＋列マッピングの記憶/自動適用 |
| `settings.js` | `config.js`(初期値)＋`settings.json`(利用者設定) を合成。仕入先プロファイル `makers` の保存/読込も |
| `quoteXlsx.js` | 提出用 見積書Excel（No/商品名/現行単価/改定単価/実施日 の5列） |
| `merge_quotes.js` | **得意先ごとに1枚へ統合した見積書を出力**（仕入先別の出力済み見積書を読み、同じ得意先の明細を1ファイルに集約。価格は再計算せず既存値をコピー＝ズレない。`得意先別.bat` の中身） |
| `xlsx.js` / `xlsxutil.js` | Excel(.xlsx) 書き出し |
| `rules.js` | 転嫁ルール（add_increase[既定]/keep_margin_rate/markup/keep_sell） |
| `hanbai.js` | **販売実績ローダ**（販売大臣の階層CSVをフラット化＋コア商品名抽出） |
| `match.js` | **照合エンジン**（メーカー見積×販売実績→照合結果。CD一致→名前一致の2段階） |
| `shogo.js` | **照合の実行(CLI)**＝照合.batの中身。販売実績の自動選択→必要なら.XLS変換→照合→input/へ出力 |
| `xls2csv.js` | 旧Excel(.XLS)→CSV変換（Excel COMをspawn。Excel依存はここだけに隔離＝クラウドでは未使用） |
| `xlsxread.js` | **.xlsx 直読みリーダ**（Node標準 zlib のみでZIP解凍→セル値取得。Excel不要・クラウドでも動く） |
| `makerXlsx.js` | **メーカー見積 .xlsx → maker_quotes CSV 変換**（見出し列を自動検出。シート名=メーカー名。取込.bat / 照合.bat から呼ばれる） |
| `textnorm.js` | 全角/半角・大小の正規化（NFKC）= 照合キー `normName` |
| `pl.js` | 全社 損益インパクト計算 |
| `csv.js` / `columns.js` / `load.js` | CSV読込・列名のゆれ吸収 |
| `index.js` | CLI入口 |
| `config.js`（直下） | 初期値（会社情報・既定ルール・端数・上乗せ%・matchThreshold） |
| `settings.json`（直下） | 利用者が画面で保存した実際の設定（会社情報など） |

## ここまでの実装（2026-05-25 時点・すべて動作確認済み）

1. **初回設定画面（⚙設定）**：会社情報・見積文言・計算既定を画面で入力→`settings.json`に保存。初回は自動で開く。日野折箱店の実情報は登録済み（住所 広島県福山市明神町1-3-7 等）。
2. **重複の自動整理**：同じ自社商品に複数メーカー品が紐づいた誤マッチを、最も信頼できる1件に集約（CD一致＞名前一致%）。
3. **要確認リスト**：一致度がしきい値（既定80%・設定で変更可）未満は見積書から外し `要確認_低一致(N%未満).xlsx` に分離。
4. **見積書レイアウト刷新**：列= No/商品名/現行単価/改定単価/**実施日**。値上額・値上率は廃止。見積番号を自動採番。**実施日は商品ごと**（CSVの「切替日」を初期値、画面で1行ずつ編集可＋「全行へ」一括入力）。品目数を表示。
5. **未一致行にメーカー商品名表示**：シミュレーション画面で「-」だった休眠品に「［メーカー品］〇〇」を表示。
6. **全角/半角の正規化**：商品名・コードの照合で全角・半角（カナ/英数字）を区別しない（`textnorm.js`）。弊社=半角、メーカー=全角の差を吸収。
7. **メーカー見積 取り込み画面（/import）**：PDF/Excelの表を貼り付け→自動で列分解→列対応を指定・ズレ修正→`maker_quotes/`にCSV保存。
8. **メーカー見積 .xlsx 直読みインポータ（2026-05-26 追加・動作確認済み）**：`src/xlsxread.js`（Node標準zlibのみでxlsx解凍・解析＝**ゼロ依存/Excel不要**）＋`src/makerXlsx.js`（見出し列を自動検出、シート名＝メーカー名で**1ブック複数シート→メーカー別CSV**）。`maker_quotes/` に置いた .xlsx は **照合.bat 実行時に自動展開**。単発変換は `取込.bat` にドロップ。中東(問屋)の `日野折箱店様（容器メーカー）.xlsx`（3シート: エフピコ122/中央化学64/福助1）で検証済み。

### 📌 明日 会社PC（DBあり）で最初にやること（2026-06-05 夜 自宅PCで対応・引き継ぎ）

- **背景**：008080「ﾄﾚｰ279-1」が二重登録（dup-check 1件）。日野折箱店=自社製造(原価0)＋北原産業=仕入れ品(BA-2792・原価8.22→10.68)の両方に出て、得意先「鰻亭 きし野」の見積で**2行に二重表示**していた。**ユーザ判断＝008080は特例で自社折箱製造品の扱いが正**。
- **自宅PCで実施した修正（DB不要・外科的）**：
  1. `settings.json` の `productLinks["北原産業"]["008080"]→トレー279-1` を**削除**（007159 の同名紐付けは残置）。これは恒久修正＝**会社PCでDB再照合しても008080は北原に出ない**。バックアップ＝`settings.json.bak_20260605_008080fix`。
  2. 北原181812 CSV から 008080 の📌手動紐付け行（26行目）を**1行だけ削除**（リンクが作った行なので無効＝削除が正）。バックアップ＝`input/北原産業_照合結果_20260605_181812.csv.bak_008080`。
  - **検証OK**：`/api/dup-check`=`dups:[]`（二重ゼロ）／鰻亭の008080=**1件のみ＝日野折箱店/原価0**（自社製造として正しく1本化）。
- **⚠ 自宅PCで起きた事故と復旧（重要・教訓）**：上記の途中で `node src/shogo.js`(再照合)を実行したら、**自宅PCはDBが無い**（`MSSQL$OHKEN` サービス無し）ため**販売実績ファイル(限定的な年間1661件)へフォールバック**し、**日野の自社製造照合が 1085一致→0一致/719休眠 に劣化**した。自社製造は**全期間DBプールが必須**なので、**DBの無いPCで日野(9000)を再照合してはいけない**。→ 劣化版CSV 11本(`_2303`〜`_2304`)を `input/_old` へ退避し、**会社PCでDB生成された良好版(`_181811`〜`_181815`・日野1085一致)を最新に復帰**済み。dup-check/鰻亭の検証もこの良好版に対して実施。
- **✅ 2026-06-06 会社PC(DBあり)で完了＋真因判明＝コード恒久修正（重要）**：会社PCで `照合.bat` を流したら **dup-check が 1件のまま**（008080が北原に再発）。調査で前回の前提が誤りと判明＝**008080が北原に出る真因は productLink ではなく「名前一致(100%)＋発注先0014」**だった。
  - **真因**：008080は商品マスタで**自社製造分類(BUN1∈{1,5}・BUN2=1)**→日野の自社見積719品に含まれ matchSelf 一致（原価0）。**だが販売実績の発注先＝0014(北原)** なので北原の候補プールにも入り、北原が出した値上げ「トレー279-1」「トレー279」に**名前一致100%**で再マッチ＝二重。productLink を消しても名前一致は別経路なので DB再照合では止まらなかった（前回「恒久修正」の記述は誤り）。
  - **恒久修正（コード・外科的）**：`src/match.js` に opts `excludeSelfCodes`（自社製造分類の自社CD集合）を追加し、**非9000の照合では販売実績の自社CDがこの集合にある候補を除外**（自社で作る品＝原価0は他社から仕入れない＝自社製造品は9000側だけに出す）。`src/shogo.js` が `merged` から9000仕入先の品番を集めて集合化し、非9000の matchAll に渡す。`padSelfCode` をexport。
  - **検証（実DB再照合）**：全11仕入先で**変化は北原 106→104（008080の2行のみ）だけ**・他10仕入先と日野1085は完全同値＝副作用ゼロ。**dup-check=0件**。008080は**日野のみ**（鰻亭きし野/✓CD一致(自社品)/原価0）、北原の「トレー279-1」は正しい自社CD**007159（📌手動紐付け＝北原の正規品）**に残存。
  - input/ 整理：照合を複数回流したので旧349本を `input/_old` へ退避（復元可）。最新は全11仕入先1本ずつ。
  - ⚠ **要 sim.bat 再起動**（match.js/shogo.js変更＝今後の↻照合/自動照合に反映。現input CSVは既にクリーン）。バックアップ（`settings.json.bak_20260605_008080fix`・`input/北原産業…181812.csv.bak_008080`）は確認後に削除可（本修正で不要）。
- **未コミット**：2026-06-05 分の実装(11ファイル・約600行)＋008080修正＋下記AI取り込み実装＋本日(06-06)の自社製造CD除外(match.js/shogo.js)が未コミット（前回コミットは2026-06-03バッチ）。区切りが良ければ会社PCでまとめてコミット推奨。

### 🆕 sim.bat：文字化けエラー解消＋二重起動(再利用)時は自動で閉じる（2026-06-06・実行検証済み）

- **症状**：sim.bat 実行時に「'繧ｦ繧｣繝ｳ繝峨え縺ｧ' は…認識されていません」等の文字化けエラー＋最後に「続行するには何かキーを押してください」で止まる。
- **原因**：sim.bat が **UTF-8（BOMなし）** で日本語 echo を持つが、`chcp 65001` でも **cmd はバッチ本文を OEM(CP932) として解析**するため日本語行が化けてコマンド扱いになっていた（node の UTF-8 出力は chcp 65001 で正常表示）。BOMを付けても全角括弧等で一部の行が壊れた＝**cmdのバッチ解析は日本語が不安定**。
- **対処**：**sim.bat 本文を ASCII のみ**に（日本語の案内は node 側＝server.js の console.log が chcp 65001 で正しく表示する／「✓ すでに起動しています」「ブックマーク用URL」等は元々 node が出している）。`pause` → `if errorlevel 1 pause` にして**正常/二重起動(再利用＝exit 0)時は自動で閉じる**・エラー時(exit≥1)だけ停止。
- **検証**：`cmd /c sim.bat` 実行＝誤認識エラー0・node の日本語正常表示・**pauseせず自動で閉じる**を確認。
- **教訓**：**.bat 本文に日本語を書かない**（cmdの解析で化ける）。日本語の案内は node(UTF-8)＋chcp 65001 に任せる。bat はASCIIのみ。

### 🆕 基幹CSV（単価履歴/仕入原価）が見積書の単価と一致しない問題を修正＝発行時の確定単価を保存（2026-06-06・実API検証済み）

- **症状（ユーザ報告）**：①照合で取り込んだ単価 ≠ 仕入原価CSVの単価 ②得意先別見積で作成した単価 ≠ 単価履歴CSVの単価。
- **原因**：`collectHanbaiExportLines`/`collectCostUpdateLines` が **`buildCustomerCandidates({})`（settings既定で再計算）** で出力していた。得意先ページの**全体ルール変更・価格帯別・行ルール・手入力売価・掛率**は**画面セッション限り（未保存）**なので、既定再計算では見積書と単価がズレる（実証：掛率2.0で売価489.6→979.2でも輸出は既定489.6）。
- **修正（`src/server.js`）**：**発行時に確定した単価を保存し、提出済み行はそれを優先**。
  - `markItemsIssued` が各アイテムに **sell(新販売単価)・cost(新仕入単価)** を `品目ステータス.json` の issued エントリへ保存（発行リクエストは calcOpts 込み＝keep の単価＝見積書の単価）。
  - `buildCustomerCandidates` の issued 処理で、保存された sell/cost があれば **item.newSell/newCost を発行時単価で上書き**（既定再計算で潰さない）。→ 単価履歴CSV(新販売単価)・仕入原価CSV(新仕入単価) が**発行した見積書と完全一致**。
- **検証**：構文OK＋実API＝品目ステータスに疑似 issued(sell=99999,cost=88888) を注入→/api/customers の提出済み行が **newSell=99999・newCost=88888**（既定489.6/0で上書きされない）。品目ステータス.json はバイト一致で復元（実データ無傷）。⚠ 要 sim.bat 再起動→本セッションで再起動済み。
- **運用（重要）**：CSVを見積書と一致させるには **先に「発行」**してください（発行時の単価が保存され、CSVはそれを使う）。**未発行**の品は保存単価が無いため既定再計算（推計）になります。本修正**以前に発行済み**の品も保存単価が無い→**発行し直す**と一致します。「発行済みの得意先だけ」モードで出すと確実。
- **補足**：仕入原価(newCost)は本来メーカー新原価＝照合値と同じはずだが、自社製造(原価0で除外)や同一商品が複数仕入先（実施日が新しい方を採用）でズレて見えることがある。発行時単価保存でその得意先の発行値に固定される。

### 🆕 仕入原価CSVの単価ズレ＝真因「1自社商品が複数メーカー品に名前一致」＋ダウンロード時に警告（2026-06-06・実DB検証済み）
- **真因（実データで特定）**：仕入原価CSVの単価が照合値と違うのは、**1つの自社商品コードが複数の異なるメーカー品番に名前一致**し、それぞれ新仕入単価が違うため（例 002033→品番113448「T-新3人桶 嵌合蓋(V)AP」¥46.38 と 113447「T-新3人桶(P)嵌合蓋(V)」¥36.36 の両方に100%名前一致）。CSVは商品コードで1件に集約＝**どれか1つ**を出力するので、別の品番側を見ると「違う」。**表示とCSVは同じ値を選ぶ（整合）**＝CSVのバグではなく**照合の取り違え**（名前一致がゆるい）。実DB＝該当18件。
- **根本解決**：正しいメーカー品番を**商品マスタの商品名3(NM3)に登録→CD一致**にすれば、取り違え抑制が効いて1つに確定（これらは「CD一致化候補」CSVに含まれる）。発行済みは発行時の単価を使う（上項の修正）。
- **追加（`src/server.js`）**：`multiMatchCheck()`＝1自社商品×複数メーカー品で新仕入単価が食い違う品を検出（名前一致のみ・CD一致/手動は確定で対象外）。`/api/cost-export-check`・`/api/hanbai-export-check` の応答に `ambiguous` 件数を追加し、**カレンダーのCSVダウンロード確認ダイアログに「⚠ N件は複数メーカー品に一致＝単価不確定。商品名3に正しい品番を登録→↻照合 で確定」警告**を表示。
- **検証**：構文OK＋実API＝cost/hanbai check ともに ambiguous=18・配信sim JS構文OK。⚠ 要 sim.bat 再起動→本セッションで再起動済み。

### 🆕 仕入原価CSVの単価ズレ②＝計算値/浮動小数を「銭(2桁)」へ丸めて取込（17.112→17.11）（2026-06-06・実DB検証済み）
- **症状（ユーザ例）**：004828「お好み丸220R 本体PP内ﾗﾐ」が仕入原価CSVで **17.112**。照合・メーカー見積は **17.11**。
- **原因**：メーカー見積CSVの新単価が **14.26×1.2=17.112**（計算値）＝エフピコ取込は `17.111999999999998`(浮動小数)・朝日は `17.112`。メーカーはExcel表示で17.11(銭・2桁)に見せるがセル実値は17.112。照合がその実値を取り込み、CSVも17.112を出力。**取り違えではなく精度（丸め）問題**＝前項の修正では直らなかった。
- **修正（`src/shogo.js loadMakerQuote`）**：容器単価は**銭(小数2桁)が基準**。`sen(v)=Math.round(v*100)/100` で **現単価/新単価を取込時に2桁へ丸める**（17.112/17.111999…→17.11）。照合・表示・基幹CSV・売価計算がすべてメーカー見積どおり(17.11)に揃う。
- **検証**：実DB再照合＝004828 新仕入単価=**17.11**（17.112から修正）・件数不変(朝日619/大黒307)・dup-check=0・ambiguous=18(これは別商品同士の取り違えで丸めとは別件・NM3で解決)。⚠ 要 sim.bat 再起動→本セッションで再起動済み。
- **使い分け**：①計算値/浮動小数の3桁→銭丸めで解決(今回)／②1自社商品が複数メーカー品に名前一致(別商品)→NM3にメーカー品番登録(CD一致)で解決／③発行済みは発行時単価を使用。

### 🆕 得意先ページ：価格帯別ルール（現売価で転嫁ルールを変える・全体反映）（2026-06-06・実API検証済み）

- **依頼**：得意先ページの転嫁ルール（全体反映）に「現売価で転嫁が変わる」機能。例「〜○○円まではこの転嫁／次の〜○○円まではこの転嫁」と価格帯別にルールを変えたい。
- **実装**：
  - **`src/server.js buildCustomerCandidates`**：`opts.priceBands`＝`[{max:数値|null, rule, factor}]`（max昇順・null=「それ以上」）を解釈。`bandFor(現売価)` で帯を選び、**行ルール上書きが無い行に帯のルール/掛率を適用**。優先順位＝**手入力売価 > 行ルール上書き > 価格帯別 > 全体ルール**。掛率は 行掛率>帯掛率>全体factor。`applied.priceBands` を返す。
  - **`src/customersPage.js`**：計算バーに「💴 現売価で転嫁ルールを変える（価格帯別・全体反映）」チェック＋帯エディタ（`renderBands`）。各帯＝「〜[価格]円まで：[ルール][掛率]」＋最後「それ以上」＋「＋帯を追加」「×削除」。ONで単体ルール(cRule/cFactor)を無効化（帯が全体を支配）。`calcOpts` はON時のみ `priceBands` を送る。`showApplied` が帯内容を表示。初期化で「それ以上」を全体ルールにシード。
- **検証**：server/customersPage構文OK＋配信/customers JS構文OK（renderBands/cBandOn配信）＋**実API**：価格帯別[〜50=据置／それ以上=掛率1.5]で 現売価7.2→7.2・42→42（据置）／480→734.4・66→100.98・58→88.74（掛率1.5×自社2%）＝**現売価で帯が正しく切替**・ruleForRowも帯ごとに正しい。⚠ 要 sim.bat 再起動→本セッションで再起動済み。
- **使い方**：得意先ページ→「💴 現売価で…」ON→帯を追加（〜100円=粗利維持／〜500円=掛率1.2／それ以上=上乗せ 等）。行ごとの上書き・手入力売価は従来どおり最優先。

### 🆕 表示商品名を「マスタの商品名(NM1)」に＝伝票テキストの運賃/入数ゴミを排除（DB構造化フィールド活用）（2026-06-06・実DB検証済み）

- **依頼/症状**：002241「ｷｸﾛﾝ ｸﾘﾋﾟｶｽﾃﾝﾚｽ50」の表示名に「1/120」等が混ざる（うまく分けられない）。ユーザ指摘「DBから取るので分け方を修正すべき」。
- **原因（実DBで確定）**：DBには**最初からキレイに分かれて**いる（URIMEI.NAME=「ｷｸﾛﾝ ｸﾘﾋﾟｶｽﾃﾝﾚｽ50」/ NAME2=運賃 / NAME4=「1/120【20/120】」/ NAME5=発注先・SHOHIN.NM1=クリーン名・IRISUU=120）。なのに `db_hanbai` が **NAME+NAME2〜5 を全部連結**し、server の `parseSelfName` でそれを再分解しようとして「1/120」を取りこぼしていた＝連結→再分解が脆い。
- **修正（Path A＝表示用にマスタ名を別フィールドで通す・照合ロジックは不変＝低リスク）**：
  - `db_hanbai.buildSql/csvToRecords`：SHOHIN.NM1 を `masterName`（クリーンな商品名）として追加。
  - `match.js`：out行＋`RESULT_HEADER`に「商品名(マスタ)」列＋toCsv。`columns.js` に `masterName` エイリアス。
  - `server.js calcAll`：**productNameCore＝rec.masterName（あれば）優先**、無ければ従来の parseSelfName(core)。＝得意先ページ・見積書・メイン表の表示名がDB直結時はマスタ名（クリーン）に。
  - codeNorm（CD一致）は従来どおり伝票テキスト(NAME+NAME2-5)＋マスタNM2-5/TEK を見る＝照合は不変。名前一致(coreNorm)も従来どおり＝**照合結果に影響なし**（朝日608/大黒306 で件数不変を確認）。
- **検証**：構文OK＋実DB再照合＝CSVに「商品名(マスタ)」列・002241は伝票名が「…1/120【20/120】29（M-3)」でも**マスタ名＝「ｷｸﾛﾝ ｸﾘﾋﾟｶｽﾃﾝﾚｽ50」**・calcAll の productNameCore も全得意先クリーン。dup-check=0維持。⚠ ファイル方式（DBなし）は masterName 無し→従来の parseSelfName にフォールバック（壊れない）。⚠ 要 sim.bat 再起動→本セッションで再起動済み（8765）。
- **🆕 商品マスタのフィールド規約を確定（ユーザ提案・2026-06-06）**：DB直結なので商品マスタ(SHOHIN)のNM1〜5の意味を固定＝**商品名1(NM1)=商品名／商品名3(NM3)=メーカーコード／商品名2・4・5=使わない**。
  - `db_hanbai`：① 名前照合・表示は **NM1**（dispName=masterName||pname）＝`norm`/`coreNorm` を NM1ベースに（伝票テキストのゴミに左右されない）。② CD一致の探索(`codeNorm`)＝**伝票テキスト(pname・過去の埋込品番)＋NM3のみ**（旧 NM2+NM3+NM4+NM5+TEK から **NM3限定**に）。NM2/4/5/摘要は運賃・入数・発注先ゴミ＝誤CD一致の元なので除外。
  - **検証（実DB再照合）**：**CD一致は完全に不変**（朝日101・大黒208・北原139 が新旧同値＝NM3限定でCD一致の取りこぼし0＝NM2/4/5/摘要は実際コードでなかった）。名前一致は微増（朝日608→**619**・大黒306→307・北原224→225・日野1085不変）＝クリーンなNM1で照合が当たりやすい。dup-check=0維持。002241表示名クリーン。⚠ 要 sim.bat 再起動→本セッションで再起動済み。
  - **運用**：メーカー品番は**商品マスタの「商品名3」に入れる**のが正式ルール（CD一致で確実に拾う）。商品名2/4/5に入れても照合には使われない。
- **✅ /self ページ整理（2026-06-06・同セッションで実施）**：① マスタ入力ルール案内カード追加（NM1=商品名／NM3=メーカー品番／NM2・4・5=使わない＋「品番は商品名3に登録」案内）。② stale な「照合に含める期間(candidateMonths)」入力を撤去→「全期間で照合中・表示期間は得意先ページで」の案内に置換。③「別ファイルをプレビュー」見出しに「※DB直結時は予備」を付記（ファイル方式 legacy 明示）。配信/self JS構文OK。
- **残（任意）**：入数(IRISUU)を独立列で表示も可能（今は表示名から除外のみ）。

### 🆕 照合精度UP：「CD一致化 候補」CSV出力（メーカー品番をマスタ商品名3に登録する半自動支援）（2026-06-06・実DB/配信JS検証済み）

- **背景/方針**：ユーザ提案「自社マスタの商品名3にメーカー品番を入れている。メーカー見積にも品番があるので、両方そろえばCD一致で高精度」。**実データ確認＝そのとおり**：メーカー見積は品番100%あり（朝日331/大黒123/北原81 全部）なのに、自社マスタ側の品番未登録でCD一致は一部だけ（朝日CD50に対し名前173＋休眠93）。マスタ7987品中NM3入力2863（コードと【メーカー名】混在）。
- **照合は既にDBのマスタを見ている**：CD一致は売上明細名＋**SHOHIN NM2〜5＋摘要(TEK)** を探索範囲に含む（db_hanbai buildSql の codeNorm）。＝NM3にメーカー品番を入れて「↻照合」すれば**追加実装なしで自動的にCD一致に昇格**（2026-06-03検証済み）。
- **⚠ DBへの自動書き込みはしない（鉄則）**：販売大臣DBは読み取り専用。マスタ更新は**販売大臣自身の取込機能**で行う（単価履歴CSVと同じ思想）。ツールは「登録すべき候補CSV」を生成するだけ＝安全な半自動。
- **実装（`src/server.js` のみ）**：
  - `buildCdCandidates()`＝最新照合結果から「**名前一致**でメーカー品番あり・自社CDあり」を集める（自社CD×メーカー品番で重複排除・全ゼロ自社CD除外）。`{items:[{supplier,selfCode,selfName,makerCode,makerName,pct}], count, dormantWithCode(品番あり休眠=自社CD不明)}`。
  - `buildCdCandidatesCsv()`＝**UTF-8 BOM**（Excelで日本語化けしない）CSV。見出し「商品コード／メーカー品番（商品名3へ登録）／現商品名（参考）／仕入先／一致度%」。
  - `GET /api/cd-candidates`（件数＋上位300）／`GET /api/cd-candidates.csv`（DL）。
  - メイン画面に青バナー `#cdCand`＝「🏷 メーカー品番をマスタ(商品名3)に登録するとCD一致にできる品 N件 📥候補CSV」＋手順注記。初期化＋照合実行後(runShogo成功)に `loadCdCandidates()`。
- **使い方**：メイン画面の青バナーから候補CSVをDL → **一致度%で確認**（100%は確実・67%等は要確認＝同一自社CDに複数メーカー品番が当たる衝突もある）→ 販売大臣の商品マスタ取込で「商品名3」に登録 → 「↻照合」でCD一致に昇格。日々の手登録の狙い撃ちにも使える。
- **検証**：server構文OK＋実API＝**候補301件・品番あり休眠139件**＋CSV(UTF-8 BOM・302行・見出し正常)＋**メイン配信JS vm.Script構文OK**＋loadCdCandidates配信。⚠ **要 sim.bat 再起動**。本セッションで再起動済み（8765稼働確認）。
- **未確認（ユーザ）**：① 販売大臣の**商品マスタ取込フォーマット**（商品名3更新の列名/SJIS要否）＝この候補CSVをそのまま読めるか実機確認（読めなければ列順/エンコードを合わせる）。② ブラウザ実操作。
- **🐛 ユーザ報告で発覚＆修正（取り違え抑制・2026-06-06）**：候補に「001374←3770661(100%)」が出たがユーザ指摘「001374は実は3770660」。実DB確認＝**001374**「ｽｰﾌﾟｽﾌﾟｰﾝ#120白ﾊﾞﾗ100本袋」NM3=**3770660**（登録済み・正）／**005616**「同白」NM3=**3770661**。大黒見積は3770661＝005616にCD一致済み。名前が似て001374にも名前一致100%で**取り違え**ていた。
  - **修正① 候補リスト**：`buildCdCandidates` で「**既にCD一致しているメーカー品番**は候補に出さない」（その品番は正しい自社品に当たっている＝別品への名前一致は取り違え）。候補301→**264**。
  - **修正② 照合エンジン（`match.js matchOne`）**：**そのメーカー品がCD一致／📌手動紐付けで特定の自社品に当たっているなら、別の自社品への“名前だけ一致”を落とす**（同じ自社品の別得意先行は残す）。＝見積に別品が混入するのを防ぐ恒久対策（前述「B/矛盾チェック」の実装）。
  - **検証**：実DB再照合＝大黒の消えた一致はユニーク14件すべて取り違え（12=品番が別自社品にCD一致／2=別自社品に📌手動紐付け＝002939→702805は実は002406・002517→952840は実は006364）。**正しい一致の喪失0**。延べ121行減は「同一商品に正/誤の重複一致」の誤側だけ除去＝二重解消。dup-check=0維持。⚠ 要 sim.bat 再起動（match.js/server.js）→本セッションで再起動済み（候補264）。
- **次段の構想（未実装）**：マスタのメーカーコード→自社CD 厳密索引化（さらに確実に）。ユーザは当面「日常業務で都度マスタ登録」を進める方針。

### 🆕 照合は全期間で拾う＋得意先ページに「過去N年」表示フィルタ（最終売上日ベース）（2026-06-06・実DB/配信JS検証済み）

- **依頼/方針**：ユーザ提案「メインの照合は**システム内の過去履歴すべて**で行い、各得意先ページで**過去何年の実績を拾うか制御**できる方が良い」。＝候補は広く・表示は可変。既存「候補期間と損益期間の分離」の自然な延長。
- **実装**：
  - **照合＝全期間（全仕入先）**：`src/shogo.js` の run() が販売実績プールを `loadHanbaiRecords({fullHistory:true})` で**全期間**ロードし、全仕入先がそのプールで照合（従来は自社製造9000だけ全期間／他は12か月窓）。`getSelfHanbai` は廃止（1プールに統一）。**年間金額(損益)の窓は db_hanbai 側で直近約1年のまま据え置き＝損益は歪まない**。`candidateMonths`(/self)は実質無効化（全期間固定）。
  - **最終売上日を照合結果に出力**：`src/match.js` の出力行＋`RESULT_HEADER`＋`toCsv` に **「最終売上日」列**(ISO)を追加（DBレコードの lastDate を書き出す。従来は年間金額までで未出力だった）。`src/columns.js` に `lastDate` エイリアス追加（load.js が rec.lastDate にマップ）。`src/server.js` calcAll の行＋buildCustomerCandidates の item＋aggregateCustomers の得意先に **lastDate** を通す（得意先の lastDate＝全アイテム中の最新売上日）。
  - **得意先ページ「過去N年」フィルタ**：`src/customersPage.js` 前項の「取引なし先を隠す」チェックを **「表示期間」セレクト（過去1年[既定]／過去2年／過去3年／全期間）** に置換。`withinYears(c,years)`＝最終売上日が今日からN年前以降なら表示（最終売上日が無いファイル方式/未再照合は hasRecent で代用）。隠した件数を一覧上部に案内＋「全期間を表示」。一覧/詳細の「取引なし」タグは最終売上日があれば「最終 YYYY-MM」表示に。
  - **全ゼロ自社CD(000000)を除外**：全期間照合で商品コード000000(プレースホルダ/未設定品「ﾌﾙｰﾂｷｬｯﾌﾟWSR-90」等)が複数仕入先に出て**dup-check誤検知**になった→`db_hanbai.buildSql` に `REPLACE(sh.CODE,'0','')<>''`（全ゼロ除外）＋`crossSupplierDupCheck` でも全ゼロ(`/^0+$/`)スキップ。
- **検証（実DB・全期間再照合）**：全期間8511件（000000除外で9102→8511）。一致は過去客で大幅増（朝日253→610・大黒212→427・北原224・日野1085不変）＝全部拾えている。**照合結果CSVに最終売上日が全一致行で出力**(714/714・過去客は2021〜2023等)。**実API /api/customers**＝344得意先・全件 lastDate付き・**過去1年185／過去3年218／全期間344**（期間フィルタ動作）。**dup-check=0**（000000除外後）。**配信/customers JS vm.Script 構文OK**＋recentYears/withinYears配信。input整理（最新11本・旧33本 _old）。⚠ **要 sim.bat 再起動**（columns/match/db_hanbai/shogo/server/customersPage 変更）。本セッションで停止→新コードで再起動済み（8765配信確認）。
- **graceful degradation**：ファイル方式（DBなし）では最終売上日が出ない→過去N年フィルタは hasRecent(直近1年)で代用。コード/名前/カナ・取引判定は機能。
- **残検証（ユーザ）**：ブラウザ実操作（既定=過去1年で過去客が隠れる・期間切替で2/3年/全期間・最終売上日タグ・全期間で過去客の最終売上年が見える）は未実機。

### 🆕 得意先ページ：取引なし先を隠す＋得意先コード表示＋カナ検索（DB TOKUI連携）（2026-06-06・実DB/配信JS検証済み）

- **依頼**：得意先一覧に「すでに取引が無い先」が混じる→一覧から外す仕組み／得意先コードの表示／検索（名前・コード・カナ）。「DBからコードとカナ拾える？」→ **拾える**。
- **DB調査（読み取り専用）**：得意先マスタ `TOKUI` に **CODE=得意先コード(4桁)／NM1=名称／KCODE=検索カナ(半角カナ 例 中島商店→ﾅｶｼﾞﾏ)**。明示の「KANA」列は無いが KCODE が販売大臣の検索カナ。実DB＝661件・カナ647件。
- **実装**：
  - **`src/db_hanbai.js`**：`loadCustomerMaster(dbCfg)`＝TOKUI から `[{code,name,kana}]` を読む（諸口0000除外・**SELECTのみ**）。export。
  - **`src/server.js`**：`getCustomerMasterMap(force)`＝source=db/auto のとき TOKUI を読み `{code:{name,kana}}` を5分キャッシュ（DB不可/ファイル方式は空＝名前・コード検索のみ・画面は壊さない）。`buildCustomerCandidates` の item に **`annualQty`**（=r.qty・直近約1年の数量）を追加。`aggregateCustomers` が各得意先に **`code`（代表customerCode）・`kana`（マスタ由来）・`hasRecent`（annualQty>0 の品が1つでもある＝取引あり）** を付与。
  - **`src/customersPage.js`**：① 一覧に **得意先コード(ccodeチップ)** 表示＋取引なし先に「取引なし」タグ。② ツールバーに **「取引のない先を隠す」チェック（既定ON）**＝`hasRecent` で絞り、隠した件数を一覧上部に案内＋「すべて表示」リンク。③ 検索を **名前・コード・カナ** 対応に（`kanaKey`＝NFKC→カタカナをひらがな化→小文字/空白除去で「なかじま/ナカジマ/ﾅｶ」どれでも一致。正規表現は不使用）。詳細ヘッダーの社名にもコード/取引なしタグ。
- **検証**：3ファイル構文OK＋`loadCustomerMaster` 実DB（661件・カナ647）＋**実API /api/customers**＝273得意先・全件code/kana付き・**取引あり184/取引なし89**（「【使わない】〜」等の過去客が hasRecent:false＝既定で隠れる）＋**配信/customers インラインJS vm.Script 構文OK（画面全壊なし）**＋hideNoRecent/kanaKey/ccode 配信確認。⚠ **要 sim.bat 再起動**（db_hanbai/server/customersPage 変更）。本セッションで停止→新コードで再起動済み（PID更新・8765配信確認）。
- **graceful degradation**：ファイル方式（DBなしPC/他社）では kana は出ないが、コード（item.customerCode由来）と取引なし判定（qty）は機能。カナ検索だけ無効になる＝壊れない。
- **残検証（ユーザ）**：ブラウザ実操作（既定で取引なし先が隠れる・トグルで表示・コード表示・カナ/コード/名前で検索ヒット）は未実機。

### 🆕 メイン画面を「照合・紐付けの確認専用」に＝価格設定UIを撤去（2026-06-06・配信JS検証済み）

- **依頼/方針**：ユーザの気づき「メイン画面に単価の設定は要らない。メイン＝照合した商品を紐づけるページ、価格は得意先ページで個別に」。役割分担（メイン＝照合確認／得意先別＝見積作成）の総仕上げ。
- **変更（`src/server.js` のみ）**：
  - **上部の「全体方針」バーを撤去**（転嫁ルール/掛率/端数/自社上乗せ%/💾全体方針を保存）。価格計算ルールは **⚙設定モーダルに集約**（sRule/sFactor/sUnit/sMode/sUplift/sThreshold が既に完備＝機能の損失なし）。バー跡地は「ルールは⚙設定／価格・見積は得意先別」の案内文に置換。
  - **メイン表の「改定後仕入」を編集不可（読み取り表示）に**、**「行ルール」列を削除**（ヘッダ・ボディとも。列数16→15で一致）。`buildMainRow` の costCell を常に `num(newCost)`、ruleCell を撤去。`renderMainRows` の `.newcost`/`.rowrule` 配線も削除。**紐付け・一致度・休眠救済は残す**（メインの主役）。
  - **`settings()` を DOM ではなく `savedPolicy`（保存済み全体方針）から返す**ように変更。`applyToBar` は DOM でなく `savedPolicy` を更新。初期化(`initSettings`→`applyToBar`)で先にセット→`loadFile`/`loadAllImpact` が正しい方針を使う（boot順は await initSettings が先）。⚙設定の保存(`setSave`)後に `applyToBar`＋`recalc()`＋`loadAllImpact()` で表示・損益も新方針に追従。
  - **損益パネルはメインに残す（読み取り専用）**＝ユーザ選択。「全社の値上げ影響」は得意先ページに無い全体ビューで、全仕入先横断のメインが最適。前提＝⚙設定の全体方針（行ごと上書きは元々使わない＝従来どおり推計）。損益.csv入力欄(#plSales等)は全体方針と別物なので残置。
- **検証**：server.js構文OK＋**配信クライアントJS vm.Script 構文OK（画面全壊なし）**＋一時サーバ(:8799)で配信HTML確認＝`savedPolicy`配信あり・`savePolicyBtn`/`(編集可)`/`行ルール列`すべて撤去・列数ヘッダ15/ボディ15一致。⚠ **要 sim.bat 再起動**（server.js変更）。本セッションでは未再起動（ユーザが再起動して実画面で確認）。
- **残検証（ユーザ）**：ブラウザ実操作（メインに価格設定UIが無い・改定後仕入は読み取り・損益は⚙設定の方針で出る・紐付けは従来どおり動く）は未実機。

### 🆕 AI取り込みアシスト（第一弾・自社利用向け／2026-06-05 夜 実装・キー無しまで検証済み）

- **背景/方針**：自社（日野）はAIを入れる方針。第一弾＝**メーカー見積のAI取り込み**（崩れたExcel・**PDF手紙(カモ井等・従来未対応)**・貼り付けテキスト→商品明細を抽出）。設計の鉄則＝**「AIは抽出・提案だけ／確定は従来どおり ③確認グリッド＋人」**。価格計算・照合・見積書（rules.js/match.js）は**無改造**。
- **2大原則を維持**：① Claude APIを**Node標準 `https` で呼ぶ＝npm不要**。② **既定OFF**＝キー未設定なら外部送信ゼロ・完全ローカルのまま（従来動作）。外部APIへ出るのは新規 `src/ai.js` だけに隔離（xls2csv.js と同じ思想）。
- **実装ファイル**：
  - **`src/ai.js`（新規）**：依存ゼロのClaude client。`extractMakerQuote({supplier,text?,pdfB64?})`＝抽出用システムプロンプト＋`/v1/messages`(生HTTPS, `anthropic-version:2023-06-01`)→ JSON抽出→ **取り込みグリッド用2次元配列**(見出し: メーカー品番/メーカー商品名/規格/現単価/新単価/切替日)を返す。モデル＝テキストは `claude-haiku-4-5`(安価)、PDFは `claude-sonnet-4-6`(難物)。**安全装置**：抽出した単価が元テキストに在るか検証→無ければ警告／PDFは原文照合不可の旨を警告／`stop_reason==='max_tokens'`は途中切れ警告／refusalはok:false。**数字を創作させない**プロンプト。
  - **`config.js`**：`ai{enabled:false, apiKey:'', model:'claude-haiku-4-5', hardModel:'claude-sonnet-4-6'}` 既定（OFF）。
  - **`src/settings.js`**：getSettings/saveSettingsに `ai` を追加（**UI保存で消えないようマージ保持**＝この癖はCLAUDE.md頻出）。apiKeyは settings.json か **環境変数 ANTHROPIC_API_KEY**。
  - **`src/server.js`**：`GET /api/ai-status`(キー本体は返さず enabled/hasKey/model のみ)・`POST /api/ai-extract{supplier,text?,pdfB64?}`→ai.extractMakerQuote。`require('./ai')` 追加。
  - **`src/importPage.js`**：ステップ②に **「🤖 AIで読み取る」行**（`#aiRow`・**enabled時のみ表示**＝`/api/ai-status`で判定）。「貼り付け・表から」(#aiTextBtn=#srcのテキスト)／「PDFから」(#aiPdfBtn=PDFをbase64で送信)。結果は**既存の `loadRows(2次元配列)` に流し込む**＝以降は手動取り込みと同じ(列対応→確認→保存 `saveMakerQuote`)。警告は上部バナーに表示。仕入先未選択時はロック。
- **使い方（有効化＝会社PCで）**：`settings.json` の `ai` を `{"enabled":true, "apiKey":"sk-ant-..."}` にする（キーは console.anthropic.com で発行。settings.jsonはgitignore＝安全。環境変数でも可）→ **sim.bat 再起動** → /import の①で仕入先を選ぶと「🤖 AIで読み取る」が出る。コストはHaikuで1見積あたり概ね1円未満。
- **検証済み（キー無しまで）**：全5ファイル構文OK／**配信import JS構文OK(vm.Script 1/1)**＋aiRow等UI配信確認／`ai.status()`=enabled:false（OFF）／**キー未設定時は外部APIを叩かず ok:false**（外部送信ゼロ担保）／**settings往復＝UI風保存(ai無し)でも ai 設定が消えない**＋他キー(selfManufacture/makers)生存（settings.jsonはバイト単位で復元・実データ無傷）。
- **⚠ 未検証（キーが要る・会社PCで）**：実APIの**ライブ呼び出し**（実際の抽出精度・PDF読取・コスト）はAPIキー未設定のため未実施。リクエスト形式は claude-api スキルの仕様どおりに実装。会社PCでキーを入れて /import から実物のメーカー見積（カモ井PDF等）で要確認。⚠ **要 sim.bat 再起動**（server.js/importPage.js/settings.js/config.js/新規ai.js）。本セッションでは**サーバ未再起動**（AIはOFF・キー無しのため通常運用に影響なし）。
- **他社販売の方針（合意済み・未着手）**：基本プラン＝完全ローカル(AIなし)で「外部送信ゼロ」を売りに、**AIは既定OFFの上位（有料）機能**。日野で実証してから他社の上位プランへ。SaaS時はキーを各社持ちでなく**自社サーバ代理＋使用量メーター**が管理しやすい。次段は照合アシスト(休眠/要確認の救済提案)。

### 🆕 自社製造（メーカーコード9000＝日野折箱店）専用の「自社コード完全一致」照合（2026-06-05・実DB検証済み）

- **依頼/症状**：特例のメーカー見積として **自社製造の折箱（メーカーコード9000＝日野折箱店）** を取り込んだが、照合シミュレーションに出てこない（ほぼ全部 休眠）。
- **原因（実DBで特定）**：照合は「メーカー見積の仕入先コード(=9000)と販売実績の発注先コードが一致する自社品だけ」を候補にする（`matchOne` の `filterCode`）。だが自社折箱は**販売実績上 発注先9000で計上されていない**（実DB：000062=発注先0003／000708・741・869・873・876・8091・8092 等=0000諸口）。`SHIIRE` に 9000=日野折箱店 は在るのに過去伝票は別コードで切られている→**フィルタ9000で候補が全滅して休眠**。唯一062が出たのは桁数の合った📌手動紐付け(キー`000062`)が迂回したからで、他の紐付けは桁落ちキー(62,70…)で発火していなかった。
- **ユーザ案「商品名・完全一致」を実データ検証→不可**：販売実績名にゴミ（`（福通・西濃/元払い）【オザム】`等）が付き coreNorm が一致しない。さらに `折箱　1合` は `折箱　1合SﾏﾆRU` の前方一致で別品に誤マッチの危険。商品名・完全一致のヒットは 0/20。
- **採用＝自社コード完全一致（`src/match.js`）**：ユーザは取り込み時に**メーカー品番欄へ自社商品コード(62,70,708…)を入れている**。これを6桁ゼロ詰め(62→000062)し、販売実績の商品コードに**完全一致**で当てる方式。`matchOne` 冒頭で `opts.selfMatch===true || opts.purchaseCode==='9000'` のとき `matchSelf()`（仕入先フィルタ・名前一致・価格判定をすべて使わず自社コード完全一致のみ／得意先×商品へ展開）へ分岐。ステータスは **`✓ CD一致（自社品）`**＝既存の `matchScore`(/CD一致/→1000)・`/^✓/`・`matchPctCell` がそのまま効く（下流 無改造・他メーカー照合に影響なし）。
- **🆕 全期間さかのぼり（2026-06-05 追加・ユーザ依頼「過去実績 遡れるだけ遡って」）**：自社折箱は単発・季節品が多く直近1年の候補窓では取りこぼす（5品が休眠）。→ **メーカーコード9000の照合だけ、候補プールを全期間にする**。`db_hanbai.loadHanbaiFromDb` に `candidateStart`（明示ISO）を追加＝**候補起点だけ全期間へ／年間金額(損益)の窓は直近1年のまま据え置き**（CLAUDE.md既出の「候補期間と損益期間の分離」を踏襲＝損益は歪まない）。`shogo.loadHanbaiRecords(opts.fullHistory)` でDBモード時 `candidateStart='2000-01-01'`。`run()` は **9000メーカーのときだけ別プール `getSelfHanbai()`（全期間・必要時1回ロード）** で照合し、他メーカーは従来の窓のまま（影響なし）。
- **データ整理**：二重取り込み（`_1105`=コードあり／`_1111`=コード空）を `_1111` 退避で**コードあり版に一本化**（休眠の二重化を解消）。古い日野照合結果は `input/_old` へ退避。桁落ち productLinks(62…)は self モードでは未参照＝無害で残置。
- **検証**：match.js/db_hanbai.js/shogo.js/server.js 構文OK＋**実DB**：全期間プール9101件（通常窓2509件）に対し自社コード完全一致 **31/31品目・43行(得意先×商品)・休眠0**。以前休眠の868/874/877/878/005071 が復活（年間金額=0＝直近1年は販売なし＝損益据え置き正常）。826/829 は過去の得意先まで複数行展開（過去客は年間金額0）。照合CLI＝一致43/休眠0・全行 `✓ CD一致（自社品）`／**ライブAPI /api/ping OK・/api/calc=HTTP200**（前段で確認）。⚠ **読み取り専用(SELECTのみ)**不変。⚠ **要 sim.bat 再起動**（match.js/db_hanbai.js/shogo.js変更）。本セッションで新コード再起動済み（PID更新）。
- **🆕 原価=0・現単価=販売単価・値上げは手入力（2026-06-05 追加・ユーザ依頼／API検証済み）**：自社折箱は**仕入＝材料費なので商品原価は0**で扱い、取り込み時の**現単価は「販売単価」の意味**（仕入原価ではない）。ユーザ選択＝**値上げ後の販売単価は得意先別ページで1件ずつ手入力**（自動計算なし）。
  - 実装①`src/match.js matchAll`：自社製造(9000)モードは **現行仕入単価・新仕入単価をどちらも0**で出力（照合結果CSVの仕入2列が0／現販売単価は販売実績の現売単価のまま）。
  - 実装②`src/server.js calcAll`：`isSelfMade`(=makers[supplier].purchaseCode==='9000')のとき **原価0固定**。⚠ 当初は「既定ルールを keep_sell固定」にしたが、それだと**得意先別ページの全体ルール『現売価×掛率』等が keep_sellに上書きされて効かない**不具合になった（2026-06-05 ユーザ報告「掛率で再計算しても反応が無い」）→ **修正：ルールは全体設定・行上書きをそのまま適用**（掛率/手入力で値上げできる）。原価0だと add_increase 既定では「値上げなのに同額」になるが自社製造は誤りではないので、`anomaly.priceRowAnomaly` に **第4引数 selfMade を追加して ②(値上げなのに同額)判定だけ抑止**（①売単価0/欠落は引き続き検知）。要確認に落とさず、掛率や手入力には反応する。
  - 実装③`src/server.js buildCustomerCandidates`：先頭で `isSelfMadeSup(supplier)` を判定し、`priceRowAnomaly(... , isSelfMadeSup(cand.supplier))` で自社製造の②を抑止。行ルール/手入力売価(ruleForRow'manual')は従来どおり最優先。
  - **検証**：照合結果CSVの仕入2列=0・現販売単価=8.9等（売価のまま）／`calcAll`=43行・**priceWarning 0件**・原価0／**ライブAPI /api/customers**：既定（add_increase/原価0）で全品『対象』・要確認0、**現売価×掛率1.2で 折箱1合 65.7→78.84・1.5で→98.55**（ruleForRow=markup・原価0）＝掛率が効く。原価0＝粗利率100%で低マージン警告も出ない。⚠ **要 sim.bat 再起動**（match.js/server.js/anomaly.js変更）。本セッションで再起動済み（PID更新）。
  - **使い方**：得意先別ページで「現売価×掛率」で一括値上げ、または各折箱の改定後売価を手入力→発行。原価0なので粗利率・損益も自社製品として正しく出る。
- **🆕 自社コード無し再取込による「休眠の重複」防止（2026-06-05 追加・ユーザ報告→対応）**：症状＝日野を**自社コード無しで再取込**すると、コード有り版(matchSelf一致)とコード無し版(matchSelfは品番必須→休眠)が別キー(`CD:` vs `NM:`)で**統合されず重複し、休眠が増える**（実例 1105コード有り＋1232/1233コード無し→一致43＋休眠31）。`mergeMakerFiles` は品番あり/なしを畳めない既知の穴(懸念A)が自社製造で顕在化。
  - 対応：`src/server.js saveMakerQuote`に**防止策2段**。①**自動補完**＝自社製造(9000)で「自社コード欄(selfCode)に入れて メーカー品番が空」なら matchSelf が使う `makerCode` へコピー（どちらの列に入れても照合できる）。②**警告**＝補完してもコードが全く無い商品があれば `selfCodeWarning{missing,total}` を返し、`src/importPage.js` が保存後に黄色警告「自社コードが空の商品 N件＝照合されず休眠・重複の原因」を表示。非9000の仕入先には影響なし。
  - 復旧手順（重複が出たとき）：`maker_quotes/` のコード無し再取込CSVを `maker_quotes/_old/` へ退避→`照合.bat`(または↻照合)で照合し直す→古い重複ありの `input/..._照合結果_*.csv` も `input/_old/` へ。今回実施し**一致43・休眠0**に復旧（品目ステータスは保持）。
  - **検証**：server/importPage 構文OK＋`saveMakerQuote` 直接テスト（settings.jsonはバックアップ→バイト復元・テストCSVは固有名のみ削除）：①コード有り=警告なし／②selfCodeのみ=品番へ自動補完・警告なし／③コード無し=`{missing}`警告／④非9000(0029)=警告なし＝誤検知なし。/import 配信に selfCodeWarning 配線あり。⚠ **要 sim.bat 再起動**（server.js/importPage.js変更）。本セッションで再起動済み（PID更新）。
- **🆕 自社製造品をDBの商品分類から自動取り込み（手入力CSV廃止）（2026-06-05 追加・実DB検証済み）**：ユーザ提案「DB見れるのでそっちにしたほうが」。自社製造品は手入力ではなく**商品マスタ(SHOHIN)の商品分類で識別してDBから直接抽出**できる。
  - **DB調査の結論**：自社製造の目印は **BUN1（商品分類1）/BUN2（商品分類2）**。実DBで 取り込み済み31品＝BUN2=1・BUN1∈{1,5,7}。**条件「BUN1∈{1,5} かつ BUN2=1」＝全部『折箱…』で始まる自社折箱（794品・実績あり719）**。⚠ KCODE列は空・マスタ仕入先は0000・仕入先=9000は別物121品＝**分類(BUN1/2)以外に目印なし**。⚠ **外ｴｿﾞﾏﾆｶﾞﾘ(000062/000070)は分類7＝仕入れ品でユーザの取り込み間違い**→分類{1,5}抽出で自動除外。
  - **実装**：`db_hanbai.loadSelfProductsFromDb(dbCfg,{bun1,bun2,onlySold})`＝SHOHIN を BUN1∈bun1・BUN2=bun2・販売実績あり(EXISTS URIMEI) で抽出し `[{code,name}]` 返す（読み取り専用）。`server.refreshSelfFromDb()`＝抽出→**既存の同一仕入先 取込CSVを `maker_quotes/_old` へ退避→DB版の取込CSVを生成**（自社コード=メーカー品番・価格空＝原価0扱い）→ purchaseCode=9000 をプロファイルに確実化。`POST /api/self-from-db`＝実行後に自動照合。`config.selfManufacture{enabled,supplier,purchaseCode,bun1:[1,5],bun2:1,onlySold}`（既定OFF・**汎用版は不使用**／settings.jsonで日野は enabled:true）＋ settings.js 通し＋ `/api/hanbai-source` で返す。`/self` のDB案内バナーに **「🏭 自社製造品（折箱）をDBから取り込んで照合」ボタン**（enabled時のみ）。
  - **検証（実DB・ライブAPI）**：`loadSelfProductsFromDb([1,5],1)`＝**719品・62/70含まず・全部折箱・既知自社品を含む**。`/api/self-from-db`＝抽出719・旧1本退避・照合OK→新 日野照合結果 **一致1085/休眠4・ユニーク自社品715・62/70消滅**・maker_quotes日野はDB版1本に一本化。settings.json はバックアップ→キー追加のみ(14→15・source=auto/makers保持)。⚠ **読み取り専用(SELECTのみ)**不変。⚠ **要 sim.bat 再起動**（db_hanbai/server/settings/config/selfPage変更）。本セッションで再起動済み（PID更新）。
  - **使い方**：/self の「🏭 自社製造品をDBから取り込んで照合」を押すだけ。商品分類に自社品を登録しておけば、手入力なし・コード誤り/重複ゼロで自社折箱ライン全体が入る。分類を変えたいときは settings.json の selfManufacture.bun1/bun2 を編集。
  - **残検証（ユーザ）**：ブラウザ実操作（/self ボタン→取り込み→シミュレーションで715品確認）は未実機。715品は大きめなので得意先別の表示量は要observe。
- **補足/残**：取り込んだ見積の新単価欄は空でも問題なし（値上げは得意先別で手入力）。ブラウザ実操作確認は未実機（API/CLIまで検証）。

### 🆕 シミュレーション メイン表：見積書 作成済み（提出済み）の行を非表示（2026-06-05・ヘッドレス検証済み）

- **依頼**：「見積書を作成したものはシミュレーションに出ないようにした方が良いのでは？」＝作業中の品だけ表に残したい。
- **実装（`src/server.js`のみ）**：① `calcAll` が各行に **`itemStatus`**（''=未提出 / 'hold'=検討中 / 'issued'=提出済み）を付与（得意先別ページと同じ rowKey で `品目ステータス.json` を引く）。② クライアント `renderMainRows`：**通常表示(readOnly=false)では `itemStatus==='issued'` の行を描画しない**＝メイン表から消える。隠した件数を緑バー `#issuedHideNote`「✅ 見積書 作成済み N件は非表示」で案内。⚠ **実施日カレンダーの横断（読み取り）ビューでは従来どおり表示**（進捗確認のため・readOnly=true は隠さない）。③ 隠した行で壊れないよう手当て：`recalc` の入力欄参照を null ガード（非表示行は空送り＝サーバが rec 値で再計算）、`updateView` は元々 ID 取得で null ガード済み、`focusRow`（得意先別→メインのジャンプ）は提出済みで非表示の商品に飛んだとき案内を表示。
- **整合**：損益・品目数の集計（server summary）は従来どおり全件（提出済みも実改定なので損益に含める）＝表示だけ絞る。検討中(hold)は従来どおり表示（除外は得意先別で扱う）。
- **検証**：server構文OK＋**sim配信JS構文OK(vm.Script)**＋itemStatus 実API確認（注入で issued）＋**ヘッドレスChrome**：日野ファイルで baseRows **1089件・issued 36件→表示1053件**（36件非表示）・緑案内バー表示「作成済み 36件は非表示」を確認。品目ステータス.jsonはバックアップ→バイト復元（実データ無傷）。⚠ **要 sim.bat 再起動**（server.js）。本セッションで再起動済み（PID更新）。

### 🆕 提出（発行）した見積の実施日を「提出済み」に保存し、カレンダー・単価履歴に反映（2026-06-05・実API検証済み）

- **依頼**：実施日カレンダーに自社製造（日野折箱店 折箱）が日付付きで出ない（全部「実施日 未設定」）。→「**得意先提出の見積実施日を反映できないか**。原価は無いので単価履歴の方が必要」。
- **背景**：自社製造はDB取り込みで**切替日が空**（値上げ日は利用者が決める）。`buildCalendar`/`calcByDate` は CSV の切替日(effectiveDate)を見るので全部未設定だった。得意先別ページの実施日(rowEff)は ephemeral で保存されない＝発行後も日付が残らない。
- **実装（`src/server.js`のみ）**：**発行(issue)時に使った実施日を `品目ステータス.json` の issued エントリに `eff` として保存**。① `markItemsIssued(customer, items, quoteNo, atIso)` を rowKey配列→**keep行(items)受け取り**に変え `{s:'issued',at,quoteNo,eff:effectiveDate}` を保存（呼び出しを `markItemsIssued(customer, keep, …)` に）。② `buildCalendar`/`calcByDate`：各行の rowKey（得意先仕入先品番）で品目ステータスを引き、**issued なら保存した eff を実施日に優先採用**（空の切替日でもその日付に出る）。③ `buildCustomerCandidates`：提出済みアイテムは `item.effectiveDate=stEntry.eff`（＋noEff再判定）＝**単価履歴CSV(collectHanbaiExportLines は item.effectiveDate 使用・issued含む)** と得意先別表示にも反映。
- **動線**：得意先別で実施日を入力（発行は実施日必須ゲートあり）→発行→実施日が保存→カレンダー/単価履歴に出る。発行前は従来どおり「未設定」。
- **検証**：server構文OK＋**実API**：日野の対象品(赤飯木箱)に issued+eff=2026-08-15 を注入→`/api/calendar` がその商品を**2026-08-15に1件**表示／`/api/calc-by-date(2026-08-15)`＝日野1行・statusCounts{issued:1}。品目ステータス.jsonはバックアップ→バイト復元（実データ無傷）。⚠ **要 sim.bat 再起動**（server.js）。本セッションで再起動済み（PID更新）。
- 補足：原価0の自社製造は基幹取込で**単価履歴(売価)CSV**を使う（仕入原価CSVは不要）。提出済みの実施日が単価履歴の抽出(実施日≤カットオフ)に効くようになった。

### 🆕 得意先別：転嫁ルール（計算バー）を各得意先の社名の下へ移動（2026-06-05・ヘッドレス実描画検証済み）

- **依頼**：得意先別ページの「転嫁ルール」計算バーを、上部（メイン位置）から**各得意先の社名の下＝「該当商品〇品」と「提出済」の間**へ移動。
- **実装（`src/customersPage.js`のみ）**：計算バーは静的要素（配線・値をDOMに保持）なので、再描画(innerHTML)で消えないよう**ノードごと移動方式**。① 計算バーを `<div id="calcbarHome" style="display:none">` で包み `id="calcbar"` を付与（初期は非表示＝得意先未選択時は出ない）。② `selectCust` 冒頭で `calcbar` を home へ退避（innerHTML破棄から保護＝値・イベントを維持）。③ 詳細HTMLの detail-head（社名＋該当商品）直後に `<div id="calcbarSlot"></div>` を挿入。④ `$('#detailCol').innerHTML=html` の後に `calcbar` を slot へ差し込む。`load()`→`selectCust` 経由で再描画されるので、ルール変更しても値は保持される（ノード移動のため）。
- **検証**：customers 構文OK＋**配信JS構文OK(vm.Script)**＋**ヘッドレスChrome実描画**：得意先選択後 `#detailCol` の子順が **detail-head → calcbarSlot → notyet(提出状況) → effnote → bulkTargetBar**＝社名の下・提出済の上にバーが入る／`inSlot:true・visible:true・between:true`・cRule値あり を確認。⚠ **要 sim.bat 再起動**（customersPage.js）。本セッションで再起動済み（PID更新）。

### 🆕 得意先別：行ルール「掛率×」に行ごとの掛率入力欄を追加（2026-06-05・実API検証済み）

- **依頼/背景**：得意先ページで行ルール「掛率×(markup)」を選んでも**掛率の入力場所が分からない**。従来は上部の全体「掛率」ボックス（`#cFactor`・全体ルールがmarkupのときだけ表示）の値を全markup行で共用しており、全体ルールが掛率以外だと掛率欄が隠れて入力できなかった。
- **実装**：行ルールを「掛率×」にした行の**隣に小さな掛率入力欄（×__）を表示**（`rowFactorInput(p)`＝rowRules[rowKey]==='markup'のときだけ・既定値＝全体掛率）。`rowFactor{rowKey:数値}` を `calcOpts` で送り、`server.buildCustomerCandidates` が markup行の再計算で **行掛率があればそれを、無ければ全体factorを使用**（`hasRowFactor`/`effFactor`／`factorDiffers` でも再計算をトリガー＝全体もmarkupな場合に行だけ別掛率にできる）。空欄なら全体「掛率」を使う。
- **検証**：customers/server 構文OK＋**配信JS構文OK(vm.Script)**＋`rowFactor/rowFactorInput/rowfactor` 配信確認＋**実API**：ある行に 掛率×1.3 指定→現売価480×1.3=624×自社上乗せ2%=**636.48**／1.5→720×1.02=**734.4**＝行ごと掛率が正しく反映（自社コスト上乗せは従来どおり最終段で乗る）。⚠ **要 sim.bat 再起動**（server.js/customersPage.js）。本セッションで再起動済み（PID更新）。
- **🆕 UI調整（2026-06-05・ユーザ要望）**：① 行掛率の入力欄を **スピンボタン無しのテキスト入力**（type=number→text inputmode=decimal）に。② **行ルールをプルダウン(select)→ラジオボタンに変更**（`rowRuleRadios(p)`＝全選択肢をその場でクリック・行ごとに一意name連番でグループ化・先頭「（全体）」value=''＝全体ルール継承。`.rrradios` で折り返し表示）。配線も `select.rowrule:not(.rowround)` → `input.rowrule-radio` の change に置換（rowRules更新→load()）。掛率×を選ぶと隣に掛率欄が出るのは不変。検証：配信JS構文OK(vm.Script)＋ラジオ/text入力 配信確認＋/customers HTTP200。⚠ 要 sim.bat 再起動（本セッションで再起動済み）。ブラウザ実描画は未確認（ユーザ確認）。

### 🆕 得意先別：チェックボックスで「対象⇔検討中⇔提出済み」をまとめて移動（2026-06-05・実API検証済み）

- **依頼**：得意先ページで 検討中⇔対象 を、チェックボックスで複数まとめて移動できるように（自社折箱715品で1件ずつは大変）。**続けて 提出済み→対象 もチェックで一括**。
- **実装**：
  - **サーバ（`src/server.js`）**：`setItemStatusBulk(customer, rowKeys, status)`＝複数 rowKey を1回の書き込みで hold/対象 に。`status=''`（対象へ戻す）は **hold・issued どちらの行も削除**（チェックで明示選択した行のみ＝検討中→対象／提出済み→対象 の両方で使う）。`POST /api/item-status-bulk{customer,rowKeys[],status}`。
  - **画面（`src/customersPage.js`）**：対象表の操作列に**チェックボックス＋ヘッダー「全」選択**＋一括バー「☑選択N件　🤔選択をまとめて検討中へ」。`sectionHtml` を汎用化し **🤔検討中・✅提出済み の両セクションに チェックボックス＋全選択（`selAll-<kind>`）＋「↩選択をまとめて対象へ戻す」バー**（`selchk-hold`/`selchk-issued`）。配線は `BULK_GROUPS`（target→hold／hold→''／issued→''）で一括処理し、`bulkMove(cls,status)`→`/api/item-status-bulk`→`load()`。`bulkUpdate()` が各グループの選択数・ボタン活性・全選択状態を同期。1件ずつのボタンも併存。
  - 補足：提出済み→対象は item の status を消すだけ（発行履歴.json・見積書ファイルには触らない＝再編集/再発行できる状態に戻す）。
- **検証**：server/customersPage 構文OK＋**配信JS構文OK(vm.Script)**＋bulk要素/関数（target/hold/issued）配信確認＋**実API**：①対象→検討中→対象 changed=3で往復／②**提出済み→対象**：提出済み31→2件まとめて対象へ＝changed=2・提出済み29/対象+2 を確認（品目ステータス.jsonはバックアップ→バイト復元で実データ無傷）。⚠ **要 sim.bat 再起動**（server.js/customersPage.js）。本セッションで再起動済み（PID更新）。

### 🆕 実施日カレンダーの横断ビューに「進捗（提出済み/検討中/未提出）」を表示（2026-06-03・実データ検証済み）

- **依頼**：カレンダーで実施日を選んだとき、そこのアイテムが**どこまで進んでいるか**分かるように。→ 上の3区分（対象/検討中/提出済み）の状態をそのまま流用。
- **実装（`src/server.js` のみ）**：`calcByDate` が各行に `itemStatus`（''=未提出 / 'hold'=検討中 / 'issued'=提出済み）を付与（得意先別ページと同じ rowKey＝`得意先+仕入先+normName(品番|名)` で `品目ステータス.json` を引く）＋戻りに `statusCounts{issued,hold,todo}`。クライアント：横断（読み取り）ビューの各行に `progressBadge`（✅提出済み/🤔検討中/⬜未提出）を商品名の下に表示、`#dateFilterBar` のラベルに内訳「✅提出済 a・🤔検討中 b・⬜未提出 c」を併記。
- **効果**：「実施日が近いのに未提出の品」が一目で分かる＝取りこぼし防止。発行（提出）や🤔検討中の操作が、カレンダーの進捗に自動で反映される。
- **検証**：server構文OK＋**配信JS構文OK(vm.Script)**＋`progressBadge`/未提出 配信確認＋**実データ** calc-by-date(2026-06-01)=314件・全行に itemStatus・statusCounts={issued0,hold0,todo314}（新方式で未発行のため全件 未提出＝正常）。⚠ **要 sim.bat 再起動**。本セッションで再起動済み（PID更新）。
- 補足：旧来の per-customer 発行履歴（発行履歴.json）はアイテム単位の提出済みには使わない（カレンダーは per-item の `品目ステータス` 由来＝正確）。今後の発行・検討中操作で自然に埋まる。

### 🆕 得意先別：見積発行で「実施日」を必須化（未設定はブロック）（2026-06-04・ブラウザ実機検証済み）

- **依頼**：得意先ページの見積作成で実施日を必須にする。
- **背景（実データ）**：対象560件中**100件（約18%）が実施日未設定**（大黒工業・ハウスホールドジャパンの見積は切替日を持たない）。黙って要確認へ流すと一覧から消えて編集できず混乱する→**対象に残して赤く強調＋発行ゲートでブロック＋サーバ側でも発行拒否**の方式（低マージン強調/要確認ゲートと整合）。
- **実装（`src/server.js`）**：`isValidEff(v)`＝ISO `YYYY-MM-DD` 判定（normDateInput が揃えられない空・「未定」等を弾く）。`buildCustomerCandidates` が各アイテムに `item.noEff=!isValidEff(effectiveDate)` を付与（表示・出力 共通）。`exportCustomerQuotes`：対象(status'')の noEff を `missingEff[]`/`missingEffCount` に集約し、**プレビュー(action:check)で件数＋一覧を返す**／**発行(doIssue)時に1件でもあれば `{ok:false,reason:'missing_eff',message}` で発行しない**（クライアントを信用しないサーバ側強制）。検討中(hold)・提出済み(issued)は対象外。
- **実装（`src/customersPage.js`）**：`effMissingCount(c)`。対象表の実施日欄を `noEff` なら赤強調(`.effmissing`)＋ツールチップ、得意先ヘッダーに赤バナー `.effnote`「⚠ 実施日が未設定 N件…」。発行ゲート：`missingEffCount>0` で**発行ボタンを無効化**（文言「実施日を入力してください（発行不可）」）＋本文先頭に「実施日が未設定（発行に必須）N件」一覧＋戻って直す案内。`doIssue` は `reason:'missing_eff'` を受けたらゲートを閉じずメッセージ表示＋`load()`。
- **直し方（ユーザ）**：各行の実施日を直接入力、または上の「実施日 一括」で統一→再計算→赤が消えて発行可能に。
- **検証**：server/customersPage 構文OK＋**配信JS構文OK(vm.Script)**＋実API：ｂｉｂ check=未設定2件・**issue=ok:false/missing_eff/フォルダ未作成**・一括日付2026-07-01で missingEffCount0／**ヘッドレスChrome実機**：赤セル2・バナー・ゲートで発行ボタン無効「発行不可」・未設定セクション表示→一括日付入力で赤0・バナー消滅・発行ボタン有効化、コンソールエラー0。⚠ **要 sim.bat 再起動**。本セッションで再起動済み（PID更新）。

### 🆕 得意先別：アイテムを「対象／🤔検討中（除外）／✅提出済み」の3区分で管理（2026-06-03・実データ検証済み）

- **依頼**：①見積提出後のアイテムは別枠で管理したい ②一覧に出ているが見積から除外（検討中）できるように。→ **アイテム（商品＝rowKey）単位の状態**1つで両方を解決（ユーザ選択＝アイテム単位）。
- **状態（`output/品目ステータス.json`・gitignore・Drive同期）**：`{得意先名:{rowKey:{s:'hold'|'issued',at?,quoteNo?}}}`。`''`=対象（見積に載る）／`hold`=検討中（一覧に残すが見積から除外）／`issued`=提出済み（発行で自動付与・別枠へ）。価格・照合には無影響＝載せる/載せないと表示振り分けのみ。
- **サーバ（`src/server.js`）**：`readItemStatus`/`setItemStatus`/`markItemsIssued`/`clearIssuedStatuses` を追加。`buildCustomerCandidates` が keep 各行に `status`（issued は at/quoteNo も）を付与。`aggregateCustomers` が keep を **対象=products／holdProducts／issuedProducts** に3分割（productCount＝対象のみ）。`exportCustomerQuotes` は**対象(status'')だけ発行**し、発行した rowKey を `markItemsIssued`＝次回から提出済み別枠＆作業表から外れる。基幹CSV（単価履歴・仕入原価）は **hold だけ除外・issued は含める**（＝実際に改定した分は基幹へ反映）。`POST /api/item-status{customer,rowKey,status}`（hold/解除）。`/api/issue-log-reset` は `clearIssuedStatuses` も呼び、提出済みアイテムを対象へ戻す（バッジと別枠を同期。hold は残す）。
- **画面（`src/customersPage.js`）**：対象テーブル先頭に「操作」列＝**🤔 検討中**ボタン。表の下に **🤔検討中（除外）**（開）・**✅提出済み**（畳む）の `<details>` セクション（各行「↩ 対象へ戻す」、提出済みは見積No/提出日も）。左一覧に「検討中N」タグ。`setItemState(rowKey,status)`→`/api/item-status`→`load()` で3区分が即更新。発行成功後は `load()` で提出済みへ移動。
- **検証**：server/customersPage 構文OK＋**/customers 配信JS構文OK(vm.Script)**＋**実データ(ｂｉｂ)**：対象4→🤔検討中で1件が「検討中」へ移動・対象から消える(対象3)→「対象へ戻す」で対象4へ復帰、を実サーバで確認。`品目ステータス.json` はバックアップ/復元で実害なし。⚠ **要 sim.bat 再起動**（server.js/customersPage.js）。本セッションで新コード再起動済み（PID更新）。
- **未検証（ユーザ）**：ブラウザ実操作（🤔検討中→セクション移動／発行→✅提出済みへ自動移動／↩戻す）と、提出済みアイテムの基幹CSV反映は未実機。

### 🆕 マスタにメーカー品番を入れればCD一致で拾う＋照合の対象期間をUIで変更（2026-06-03・実DB検証済み）

- **依頼**：メーカー品番が分かるので**自社の商品マスタ（SHOHIN）に入れて確実にCD一致で拾いたい**。DB直結なので「照合を実行」で反映されるか？ → 続けて、照合の**販売期間もページ上で変えたい**。
- **(1) マスタの品番をCD一致で読む（`src/db_hanbai.js`）**：従来 CD一致は **売上明細(URIMEI)の商品名**だけを探索範囲(`codeNorm`)にしていた＝**マスタに品番を入れても照合は見に行かなかった**（URIMEIの過去伝票は凍結されるため）。→ 照合SQLに **SHOHIN の商品名2〜5（NM2-5）＋摘要（TEK）** を追加し、`codeNorm` に連結（**CD一致専用＝名前一致 `coreNorm` には入れない**ので名前照合は不変＝安全）。これでマスタにメーカー品番を入れておけばCD一致で確実に拾え、**DB直結なので「↻ 照合を実行」で即反映**（エクスポート不要）。
  - **実DB検証**：テスト品 自社006036「角切小鉢70A（赤金）」のマスタ NM3 に `CA11070` を登録 → `codeNorm` に `ca11070` 含む＝true、`codeHit('CA11070')`＝true、合成メーカー品(品番CA11070)で **[✓ CD一致] 006036** を確認。⚠ **読み取り専用(SELECTのみ)** 不変。
- **(2) 照合の対象期間をUIで変更（`db_hanbai.js`＋`server.js`＋`selfPage.js`）**：上の検証で 006036 は**最終売上2024-12-20＝過去約1年の窓外**で照合候補に出ないと判明。→ **「照合に含める期間（さかのぼり月数）」を /self で設定**できるように（既定12・12〜60か月に丸め）。⭐ 重要：**候補に含める期間（可変）と 年間金額(損益)の集計期間（常に直近約1年）を分離**＝期間を延ばしても**損益は歪まない**。
  - 実装：`db_hanbai.buildSql(candidateStart, end, annualStart, scale)`＝base は candidateStart..end（候補）、agg(年間金額)は `DENDATE>=annualStart`（直近1年）に限定。`monthsBackStart()`／`loadHanbaiFromDb` が `dbCfg.candidateMonths` を解釈。`shogo.loadHanbaiRecords` が `settings.hanbai.candidateMonths` を db 設定に載せて渡す。`GET /api/hanbai-source` に candidateMonths を追加、`POST /api/hanbai-period{months}` で保存（`saveSettings({hanbai:{candidateMonths}})`＝浅いマージで source/db 保持・自社品検索キャッシュ破棄）。/self のDB案内バナーに 月数入力＋保存＋注記を追加。
  - **実DB検証**：candidateMonths=12→件数2500・**006036なし**／=24→件数3191・**006036あり・年間金額=0（損益歪まない）・現売単価14.1・codeNormにca11070**。**期間を延ばすと候補に入りCD一致もするが損益は直近1年のまま**＝設計どおり。`POST /api/hanbai-period` 24→ok、999→60に丸め、保存後 `getSettings().hanbai.db.database` 生存（DB接続壊れない・settings.jsonバックアップ/復元で確認）。
- **検証総括**：db_hanbai/shogo/server/selfPage 構文OK＋**/self 配信JS構文OK(vm.Script)**＋期間UI要素あり。⚠ **要 sim.bat 再起動**（db_hanbai.js/shogo.js/server.js/selfPage.js 変更）。本セッションでは検証後に新コードで再起動済み（PID更新）。
- **使い方（ユーザ）**：① 商品マスタの「商品名2〜5」か「摘要」にメーカー品番を入れる ② 1年以上ご無沙汰の品も拾いたいときは /self で「照合に含める期間」を延ばす ③「↻ 照合を実行」。※ 期間外の品は損益に効かない（年間金額0）が見積には出せる。
- **残検証（ユーザ）**：ブラウザ実操作（/self で期間変更→保存→↻照合→角切小鉢70A赤金がCD一致で出る）は未実機。

### 🆕 休眠（メーカー品が未マッチ）をメイン画面から直接 手動紐付けで救済（2026-06-03・実DB検証済み）

- **依頼**：メーカー見積の自動照合で「実績はあるのに商品名の揺らぎで**休眠**になった商品」を、手動で直す方法。
- **これまでの穴（実コードで確認）**：休眠行＝メーカー品が自社品に1つも当たらなかった行で、**自社CDが無い**ため `linkCellHtml` がボタンを出さず「—」、`openLinkModal` も「自社商品コードが空」で拒否（`server.js`）。＝休眠はメイン画面から直せず、取り込みページ(/import)で自社コードを与えて取り込み直すしかなかった。
- **実装＝紐付けモーダルを2モード化（`src/server.js` のみ）**：
  - **maker（自社CDあり・従来）**：自社品に対して「確定するメーカー商品」を選ぶ（不変）。
  - **self（自社CD無し・休眠＝新）**：メーカー品(固定)に対して、**販売実績の自社品(自社CD)を検索して選ぶ**。保存で `productLinks[仕入先][選んだ自社CD]=メーカー商品名`（既存 `/api/product-link`）＝📌手動紐付け。休眠の解消は再照合が要るので、保存後に**「↻ 照合を実行」を即実行するか確認**するダイアログを出す。
  - 休眠行の紐付けセルに **「✏ 実績と紐付け」ボタン**（橙）を表示（`linkCellHtml`：自社CD無しでもメーカー商品名があれば出す）。横断（実施日フィルタ）ビューでも有効。
  - 候補は類似度順（同じ発注先＝仕入先コード一致を +0.15 で優先）。決定的トークン警告・検索（商品名/CD）も従来同様。
- **サーバ追加**：`GET /api/self-products?supplier=`＝販売実績の自社品を**自社CDで重複排除**して返す（`{code,name,purchaseCode,currentSell}`）。販売実績は短時間キャッシュ（5分）＝連打で毎回DB/ファイルを読まない。読み込みは `shogo.js` から切り出した共用 `loadHanbaiRecords()`（DB直結 'db'/'auto'/ファイル・**照合 run() と同一ロジックに集約**＝二重実装解消）。**⚠ 読み取り専用（SELECTのみ）**は不変。
- **検証**：① shogo/server 構文OK ② **配信クライアントJS 構文OK(vm.Script)**（休眠紐付けUI・/api/self-products・openLinkModal を配信HTMLに確認）③ **実DB**：`/api/self-products?supplier=大黒工業`＝自社品1260件・自社CD重複0・発注先0029一致197件・3.3秒（以後キャッシュ）④ run() に未定義参照なし＝照合は挙動不変。⚠ **要 sim.bat 再起動**（server.js/shogo.js 変更）。
- ⚠ テンプレートリテラル規約の罠：confirm の改行は `\\n`（`\n` だと配信時に実改行になり文字列が壊れる）。今回 vm.Script 検証で1回踏んで修正済み。
- **残検証（ユーザ）**：ブラウザ実操作（休眠行の「✏ 実績と紐付け」→自社品検索→保存→照合で休眠解消）は未実機。

### ✅ メーカー見積の二重取り込み防止＝検証完了＋makerXlsxをupsert化（2026-06-02・実データ検証済み）

ユーザ依頼「念入りに検証」。実データ（maker_quotes 全CSV＝生3507品／7仕入先）と人工境界ケースで二重取り込みを精査し、**二重計上（double-count）は全レイヤーで防止されている**ことを確認。唯一の実害＝.xlsx取込の上書きによる**データ消失**を修正（消失を根絶）。⚠ **要 sim.bat 再起動**（makerXlsx.js 変更）。

- **検証結果（4論点）**：
  1. **キー不一致で二重**：統合層（`mergeMakerFiles`）で「仕入先内の同名別キー二重＝0」。人工ケースで境界確認＝**A.品番あり/なしは統合層では畳めず2品残る**（唯一の統合層の穴）／**B.表記揺れ(品番なし)はNFKC＋空白除去で1品に統合**／**C.品番同じ・名前違いは品番キーで後勝ち統合**。ただしAも**最終dedup層**（`buildCustomerCandidates` の `supplier+productCode` キー `server.js:859-868`）が同じ自社商品に当たれば1件に集約するので、最終成果物（損益・得意先別見積書）は二重化しない。
  2. **別の仕入先名で二重**：統合後に「同一メーカー品番が複数仕入先＝0／同一商品名が複数仕入先＝0」。`makerChannel:{エフピコ→朝日食品容器}` が稼働しエフピコ122品が朝日に正しく畳まれている。**他に寄せるべき組合せは現データに無し**。
  3. **最終成果物の二重（最重要）**：全仕入先 最新CSVを `calcAll`→最終dedupと同一キーで集計＝**一致行530（生）→ユニーク464**。同一得意先×同一自社商品が複数候補（41組＝朝日ﾆｭｰｲｰｼﾞｰﾊﾞｯｸﾞ・大黒フルーツキャップ色違い等）でも**全て1件に集約**（scoreで最も信頼できる1件を選択）。**真の二重計上＝0**。
  4. **.xlsx部分取込の上書き＝唯一の実害（修正済み）**：旧 `makerXlsx.convert` は `<仕入先>.csv` を `fs.writeFileSync` で**上書き**＝同一仕入先の異なる部分xlsxを別々取込すると後勝ちで前の商品が**消失**（二重ではなくデータ消失）。システム全体は「蓄積＋商品単位後勝ち」モデル（paste-import＋mergeMakerFilesは商品を消さない）なのに、xlsx取込だけが消す唯一の例外だった。→ **convert を商品単位upsert化**（`makerXlsx.js:100`）：既存`<仕入先>.csv`を読み（`readExistingMakerRows`）、商品キー（品番優先・無ければ商品名NFKC正規化＝`prodKey`、shogo.makerProdKeyと同規則）で「今回を後勝ち」統合してから書き出す。
- **修正の検証**：① makerXlsx 構文OK ② 実xlsx（中東3シート）を一時フォルダで2回convert＋手動で部分取込1品(EXTRA-001)を注入→**再取込でEXTRAが消えず保持（旧:122品に戻り消失／新:123品で保持）・同キー重複0**＝消失根絶かつ二重化なし。価格更新も後勝ちで反映。実データ(maker_quotes)は変更せず一時フォルダで検証。
- **残課題（軽微・未対応）**：保存段階の重複ファイル蓄積（懸念#4＝`maker_quotes/`に日時付きCSVが溜まる）は**計算上は無害**（mergeMakerFilesが後勝ち集約）でフォルダが煩雑なだけ＝現状維持。気になればsaveMakerQuoteに重複検知/警告を足せるが優先度低。
- ⚠ コード変更後は **sim.bat 再起動必須**。PC移動するなら Drive同期完了を待つ／2台同時編集しない。

### 🆕 実施日カレンダー：月送り（◀月▶今月）をヘッダーから「チップの下・グリッドの直上」へ移動（2026-06-02・検証済み）

- **依頼**：カレンダー上部の「◀ 2026年5月 ▶ 今月」が上にありすぎて使いにくい。チップ（4/1 2件…）の下あたり＝日付グリッドの近くへ。
- **実装（`src/server.js`）**：`.cnav`（calPrev/calMonth/calNext/calToday）を `.chead`（タイトル＋畳む）から外し、**`#calChips` と `#calGrid` の間**に `<div class="cnav" id="calNavRow">` として移設。CSS `#calNavRow{ margin-left:0; padding:12px 20px 0; }`。ヘッダーは「📅 実施日カレンダー ＋ ▲畳む」だけに。ID・JS配線（$('#calPrev')等）は不変。
- **検証**：server構文OK＋配信クライアントJS構文OK＋配信HTML順序 chips→月送り→グリッド／重複IDなし。⚠ **要 sim.bat 再起動**。

### 🆕 実施日カレンダーの横断（読み取り）表でも「📌 紐付け」を可能に（2026-06-02・実機検証済み）

- **依頼**：実施日カレンダーで該当商品がシミュレーション（メイン表）に出たとき、その場で紐付けの設定をしたい。
- **背景**：横断ビュー(`loadByDate`→`renderMainRows(true)`)は読み取り専用で紐付けボタンを無効化していた。さらに `openLinkModal` は単一ファイル前提のグローバル（`currentSupplier`/`allMakerNames`/`currentLinks`）に依存＝横断（行ごとに仕入先が違う）では使えなかった。
- **実装（`src/server.js`）**：
  - **横断表でも紐付けボタンを表示**（`buildMainRow` の linkCell を常に `linkCellHtml`、`renderMainRows` で `.linkBtn` のクリックを readOnly でも配線）。`linkCellHtml` は確定判定に `matchStatus` の「📌 手動紐付け」も見る（横断は辞書が空なので）。
  - **`openLinkModal` を async・仕入先対応に**：`dateFilter` 中は**行の仕入先 `r.supplier`** を使い、`GET /api/link-context?supplier=…` で**その仕入先の候補（メーカー商品名）＋既存の手動紐付け**を取得してモーダルを出す。保存も `supplier=r.supplier` で `/api/product-link` へ。保存後は `loadByDate(dateFilter)` で横断表を再描画（通常時は従来どおり `currentSupplier`＋`loadFile`）。
  - **`linkContext(supplier)`＋`GET /api/link-context`**（新規）：指定仕入先の最新照合結果からメーカー商品名候補を集め、`settings.productLinks[仕入先]` を返す。
- **検証**：server構文OK＋**配信クライアントJS構文OK(vm.Script)**＋`openLinkModal` async・link-context fetch あり＋`/api/link-context(朝日)`＝候補331件/既存紐付け2件。**ヘッドレスChrome実機**：カレンダー日クリック→横断表に紐付けボタン2個→クリックで `#linkDlg` が開き**その行の仕入先の候補124件**をロード（supplier空ならモーダルを開かない作り＝仕入先は正しく入る）。⚠ **要 sim.bat 再起動**。
- 補足：横断ビューは一致行のみ（calcByDateが休眠/未一致を除外）＝紐付けボタンは全行にCD/商品名あり。紐付けの「別のメーカー品へ変更」を完全反映するには従来どおり ↻照合 が必要（モーダルにも明記）。確認用途（現マッチの確定）はその場で即反映。

### 🆕 二重登録（同じ商品が複数仕入先）の調査・修正＋検知機能（2026-06-02・実データ修正＆検証済み）

- **症状（ユーザ報告）**：仕入原価CSVで 商品7324 が 9.97 になるが、現行の最新は 9.2（最初9.97→上げ幅を抑えた修正版CSVを入れたので9.2が正）。
- **原因（実データで特定）**：修正版（63商品・約8%安い新価格）を**仕入先名「朝日食品容器」で取り込んだ**が、元は**「中央化学」で登録済み**（CF寿司容器・DXPD・SDビストロ・タレビン等＝中央化学ブランド／実体は問屋0013=朝日経由）。**同一メーカー品番が中央化学(旧9.97)と朝日(新9.2)の2仕入先に並存**＝mergeMakerFilesは仕入先ごとに分けるので両方残り、得意先別/損益/CSVが二重化。仕入原価CSVは商品コードで集約するが両方とも実施日2026-06-01で同点→旧側(9.97)を拾っていた。**63商品すべてが二重登録**だった。さらに `日野折箱店様（容器メーカー）.xlsx` が照合のたび 中央化学.csv(9.97) を再生成する＝旧価格の供給源。
- **修正（ユーザ選択＝朝日に統一）**：① `settings.json` の **makerChannel に `"中央化学":"朝日食品容器"` を追加**（中央化学を朝日へ寄せる＝今後の中央化学取込も朝日に畳まれ再分裂しない。settings.jsonは `.bak_20260602_dupfix` にバックアップ）② `maker_quotes/中央化学.csv` を `maker_quotes/_old/` へ退避 ③ **再照合**（`node src/shogo.js`・DB直結）④ 古い `input/中央化学_照合結果_*.csv` 12本を `input/_old/` へ退避（再照合で中央化学は朝日へ畳まれ新規作成されないため）。
- **検証（実データ）**：007324=**朝日のみ・新仕入9.2**（9.97消滅）／最新照合に**中央化学 supplier 消滅**／**得意先×商品の仕入先またぎ二重＝0組**（63商品すべて解消）／**仕入原価CSV `007324,9.2`** を実サーバで確認。
- **🆕 二重登録検知（再発防止・ユーザ要望）**：`crossSupplierDupCheck()`（`src/server.js`）＝最新照合結果で**同一自社商品コードが2仕入先以上に一致登録**されている商品を返す。surface 3か所：① `GET /api/dup-check` ② **メイン sim 上部に警告バナー `#dupAlert`**（起動時 `loadDupCheck`・該当商品と仕入先名を列挙）③ **取り込み保存後**（`/api/maker-quote` レスポンス `dupWarning`）に importPage が警告（「取り込んだ商品が別の仕入先にも登録されています」）。検証：構文OK＋両配信JS OK＋`/api/dup-check`=0件（修正後＝正しくゼロ）＋要素/関数存在。⚠ **要 sim.bat 再起動**。
- ⚠ **教訓再掲**：このbashに `pkill` 無し＋二重起動防止で、停止し損ねた古いサーバがreuseされ「新コードが配信されない」事象が再発。**停止は必ずPowerShellで `Get-NetTCPConnection -LocalPort 8765 -State Listen` の OwningProcess を Stop-Process**。

### 🆕 メイン表の「得意先」をリンク化＝クリックで得意先別ページへ（その得意先を自動選択）（2026-06-02・実機検証済み）

- **依頼**：メイン表の得意先は今リンクが無い。リンクにして得意先別ページへ移動できるように。
- **実装**：
  - **メイン表（`src/server.js`）**：`custCellHtml(r)` を新設＝実在の得意先名を `<a href="/customers?customer=<encoded>" target="_blank">`（緑・点線下線）でリンク化。`buildMainRow` の得意先セルを差し替え。未一致/休眠（得意先「-」/空）はリンクにしない。横断（実施日フィルタ）ビューでも同じ（得意先名があればリンク）。
  - **得意先別ページ（`src/customersPage.js`）**：初期化IIFEで `await load()` 後に **URL `?customer=` を読み、一致する得意先を `selectCust` で自動選択**＋一覧の該当行を `scrollIntoView`。見つからなければ `#msg` に「現在の改定対象に見つかりません」と表示（実施日/照合の結果に含まれない場合）。
  - 既存の逆方向リンク（得意先別の商品名→メイン sim の focusRow）と対になり、メイン↔得意先別を相互に1クリック移動できる。新タブで開く（sim/カレンダーの状態を保持）。
- **検証**：server/customersPage 構文OK＋両配信JS構文OK＋`?customer` 配線あり。**ヘッドレスChrome実機**：メイン表「株式会社　絆」がリンク→`/customers?customer=...`へ遷移→得意先別が `selName=株式会社　絆`・一覧ハイライト(.cust.sel)・右詳細見出し「株式会社　絆」で**自動選択**を確認。⚠ **要 sim.bat 再起動**。
- ⚠ **検証時の重要教訓（再発防止）**：このbashには **`pkill` が無い**（command not found で無音失敗）。さらに**二重起動防止(startOrReuse)**を入れたため、古いサーバが8765に残ったまま `node src/server.js` すると**reuseされ古いコードが配信され続ける**（「custCellHtmlが配信JSに無い」と誤認した原因）。**サーバ停止は必ず PowerShell で**：`Get-NetTCPConnection -LocalPort 8765 -State Listen` の `OwningProcess` を `Stop-Process` →（残れば）`Get-CimInstance Win32_Process | ? CommandLine -match server.js | Stop-Process`。TimeWait(PID=0)は実プロセス無し＝数分で自動解放。

### 🆕 起動の二重起動防止＋ポート固定（ブックマーク対応）（2026-06-02・実機検証済み）

- **依頼/背景**：sim.bat 起動はブックマークで対応できる？ → sim.bat は「サーバ起動＋ブラウザ自動オープン」のアプリ起動アイコン。ブックマークはサーバ稼働中の再オープン用。ただし旧実装は二重起動でポートが 8765→8766…とずれ、ブックマークが当たらない問題があった。→ **改良**：既に起動済みなら二重起動せず :8765 を開くだけ＝ポート固定。
- **実装（`src/server.js`）**：
  - **`/api/ping`**（ローカル限定・**パスワード不要**＝#1ガード直後/#2パスワードの前に配置・機微情報なし）＝`{ok:true, app:'kakaku-tenka-sim'}` を返す死活確認。
  - **`startOrReuse()`**（`require.main` 起動時に呼ぶ）：`probeOurServer(8765)`（/api/ping で app名一致を確認）→ **既に自分のサーバが居れば**：ブラウザで :8765 を開くだけ→`process.exit(0)`（二重起動しない）／**居なければ** `listen(8765)`。`listen` は従来どおり EADDRINUSE 時のみ次ポートへフォールバック（=8765が**別アプリ**に占有された稀なケースのみ 8766 へ）。`openBrowser()` をOS別ヘルパに整理（win=start/mac=open/linux=xdg-open）。起動ログに**ブックマーク用URL `http://localhost:8765`** を明記。
  - **`sim.bat`**：ブックマーク用URLと「もう一度押しても二重起動せず開き直す」案内を追記。
- **検証（実機・全分岐）**：① 構文OK ② 1台目=8765起動・`/api/ping`={app:...} ③ **2台目起動→「すでに起動しています」→ブラウザ開くだけ→exit0／8766は立たない（二重起動なし）／1台目は稼働継続(files 11件)** ④ `probeOurServer` 単体＝空きポート/非アプリ=false・本物=true（→非アプリ占有時は従来のEADDRINUSEフォールバックで次ポート）。
- **運用**：常に :8765 固定なので **ブックマーク `http://localhost:8765` が毎回確実に効く**（サーバ稼働中）。sim.bat はデスクトップ/タスクバーにピン留めすればアプリアイコン的に使える。⚠ **要 sim.bat 再起動**（既存サーバは一度 Ctrl+C で終了してから）。

### 🆕 実施日シミュレーションを「カレンダー内表示」→「メイン表に表示」へ作り替え（2026-06-02・実サーバ/実データ検証済み）

- **依頼**：前項（カレンダー内に損益＋明細を出す）を見て「**カレンダー内の表示は無くし、通常のシミュレーション画面（いつもの表・損益）にカレンダーで選んだ日の改定を出す**」に変更したい＝表示を一本化。
- **作り替え（`src/server.js` のみ）**：
  - **撤去**：前項でカレンダー詳細(`renderCalDetail`)に足した損益サマリ＋現→改定後の明細テーブル、`buildCalendar`/`buildEffIndex` の価格フィールド(cc/nc/cs/ns/dC/dS)、CSS `.calsim` を**すべて元に戻した**。
  - **新規 横断ビュー**：`calcByDate(body{date,rule,rounding,selfUplift})`＝全仕入先 最新CSVを全体方針で計算し、**提出対象（一致）かつ 実施日(正規化ISO=`normDateInput`)が一致**する行だけを集めて `{rows, summary, count, supplierCount}` を返す（`summarizeRows` は calcAll と同式）。行に `supplier` を付与。ルート `POST /api/calc-by-date`。
  - **メイン表に表示**：`loadFile` の行描画を `buildMainRow(r,i,readOnly)`＋`renderMainRows(readOnly)` に切り出し（**通常表示の見た目・16列は不変**）。`loadByDate(dateIso,label)`＝/api/calc-by-date を読み、`renderMainRows(true)`（**読み取り専用**：改定後仕入の編集欄／行ルール／紐付けを出さず、商品名の下に🏢仕入先名を小さく表示）→ `updateView`（既存・計算済みフィールドからセルを埋めるので横断でもそのまま動く）。表上に **`#dateFilterBar`**「📅 〇月〇日 の改定 N件（全X仕入先） ×解除」。損益パネル(`renderPL`)は dateFilter 中は**その日の集計**を表示（`#plScope` に「実施日 X の改定のみ」）。
  - **配線**：カレンダーの日クリック/チップ→`renderCalDetail`→`loadByDate`（同じ日が表示中なら再取得しない）。`renderCalDetail` は日選択時は**メイン表へ誘導する緑ノートのみ**（実施日未設定=__none だけ従来の一覧をカレンダー内に残す＝日付で絞れないため）。`recalc`/`recalcAll` は dateFilter 中は `loadByDate` で取り直す（方針変更が横断ビューに反映）。`#file` 変更・`loadFile` で `clearDateFilter`＝通常表示へ復帰。`#dateFilterClear`＝選択中ファイルを編集可能で再描画＋全社損益へ戻す＋カレンダー選択解除。
- **整合**：横断ビューの損益は `summarizeRows`＝calcAll/`impactAllSuppliers` と同式。実機で **calc-by-date(2026-07-01)=37件/3仕入先/仕入増¥112,961＝前項カレンダー集計と完全一致**＝二重計上等の差は無し。
- **検証**：server構文OK＋**配信クライアントJS構文OK(vm.Script)**＋#dateFilterBar/Clear/Label・新関数(buildMainRow/renderMainRows/loadByDate/clearDateFilter/dateFilter)存在＋calsim撤去確認＋calc-by-date実走（行/仕入先/損益・先頭行に cc6.4→nc6.8/cs7.6→ns8/eff2026-07-01・不正日付でok:false）。⚠ **要 sim.bat 再起動**。
- **仕様メモ**：横断ビューは**読み取り専用**（複数ファイルにまたがるため行編集の再計算経路`{file,rows}`が使えない）。個別の価格編集は従来どおり「対象」で1仕入先を選んで行う。期間（複数日まとめ）は未対応＝1日単位（カレンダーの日/チップ選択）。
- **残検証（ユーザ）**：ブラウザ実操作（日クリック→メイン表が横断・読み取りに切替＋バナー＋損益がその日に／解除で通常復帰）は未実機。

### 🆕 実施日カレンダー：日付クリックで「その日の価格シミュレーション（損益＋現→改定後明細）」（2026-06-02・実サーバ/実データ検証済み）※下記は上記で置き換え済み（カレンダー内表示は撤去）

- **依頼**：実施日で検索して価格シミュレーションする機能。**ユーザ選択**＝置き場所は実施日カレンダーに統合／検索はカレンダーの日クリックから／表示は損益合計＋明細リスト。
- **実装（`src/server.js` のみ）**：既存の「日クリック→その日の明細」を**価格シミュレーション表示に拡張**。
  - **サーバ `buildCalendar`**：各エントリに `cc/nc`(現→改定後 仕入)・`cs/ns`(現→改定後 売価)・`dC/dS`(年間 仕入増/増収インパクト＝`annualCostImpact/annualSellImpact`)を追加。値は calcAll の行から（＝既定の全体方針で計算・損益パネルと同じ素材）。
  - **クライアント `buildEffIndex`**：これらを日別index項目に持たせる。`renderCalDetail`：選択日の **📊損益サマリ**（件数・数量あり件数・仕入増¥/年・増収¥/年・差引¥/年）＋ 明細表に **「現仕入→改定後(改定%)」「現売価→改定後(改定%)」列**を追加（改定%は値上げ赤/値下げ青）。CSS `.calsim`/`td.r`/`th.r` 追加。
  - 検索UIは新設せず、**既存の日クリック・チップ・◀▶ がそのまま「実施日での絞り込み」**＝ユーザ要望（カレンダーから）どおり。__none（実施日未設定）も同様に試算表示。
- **整合**：損益は `impactAllSuppliers`（メイン損益パネル）と同じ「matched行のannualImpactを合算」方式＝**パネルと数字の基準が一致**。重複候補(同一得意先×商品が複数メーカー品にマッチ)の扱いも既存と同じ（別途の課題）。
- **検証**：server構文OK＋**配信クライアントJS構文OK(vm.Script)**＋**実サーバ/実データ**：/api/calendar=767エントリ全てに価格フィールドあり、実施日別の損益が集計（例 2026-06-01=284件・仕入増¥1,187,268/増収¥1,187,235、2026-07-01=37件/¥112,961）。差引≈0は既定方針 add_increase（仕入増を売価へ1:1転嫁）のため＝方針変更で差が出る。⚠ **要 sim.bat 再起動**。
- **残検証（ユーザ）**：ブラウザ実操作（日クリック→サマリ＋明細描画）は未実機。補足：シミュレーションは**保存済みの全体方針**で試算（ルールを変えたい時はsim上部で変更→カレンダー再読込で反映）。期間（複数日まとめ）集計が必要なら拡張可。

### 🆕 取り込みページ：仕入先の取り違え事故を防ぐ（クリア／自動クリア／警告／保存時ガード）（2026-06-02・検証済み）

- **依頼/症状**：A社を取り込んだ後そのままB社を取り込もうとすると、A社の表（grid）・貼り付けテキスト・仕入先名・列対応が残る。**実コードで確認＝重大リスク**：保存後も入力がクリアされず、仕入先だけB社に変えてそのまま保存すると **A社の商品がB社として保存される事故**が起きうる（onSupplierPick/onPurchaseCodePick は grid を消さない＝CLAUDE.md旧記述「切替で消える」は不正確だった）。
- **対処（`src/importPage.js`・4点セット）**：
  1. **保存成功後に自動で全クリア**（`resetImport()`）＝A→Bの連続取込が常にまっさらから始まる（最も効く根治）。成功メッセージは消えない**上部バナー `#topMsg`**（`showTop()`）に出す（mapAreaを隠すと中の#msgも消えるため、保存結果はバナーへ移した）。
  2. **手動「🆕 入力をクリア（別の仕入先へ）」ボタン**（ヘッダー）＝いつでも全初期化（データありor仕入先入力ありのとき確認ダイアログ）。
  3. **取り違え警告**：`loadedForSupplier`（gridを読み込んだ時点の仕入先名・`loadRows`で記録）と、いま選択中の仕入先が食い違うと上部バナーに黄色警告（`warnSupplierMismatch` を onSupplierPick/onPurchaseCodePick/#supplier入力 の3か所で呼ぶ）。
  4. **保存時ガード**：`loadedForSupplier !== supplier` のとき confirm「表示中のデータは『A』で読み込んだものです。『B』として保存しますか？」で最終防御。
  - 補助：`clearImportData()`（データのみ）/`resetImport()`（仕入先選択も）/`hideTop()`。loadRows は新データ読み込み時に `loadedForSupplier` を更新＋古い警告を消す。手入力フォーム・一括実施日もクリア対象。
- **検証**：importPage.js 構文OK＋**配信クライアントJS 構文OK(vm.Script)**＋追加要素(resetBtn/topMsg)・5関数/変数の存在＋配線（保存後自動クリア・保存時confirmガード・warnSupplierMismatch 3か所・loadRowsで記録）を確認。⚠ **要 sim.bat 再起動**。
- **残検証（ユーザテスト）**：ブラウザ実操作（A取込→保存→自動クリア／A読込→B選択で警告→保存時confirm）は未実機。

### 🆕 基幹システム取込CSV（単価履歴・仕入原価）を 実施日カレンダー へ移設（2026-06-02・実サーバ検証済み）

- **依頼/判断**：2つのDL（単価履歴CSV＝売価／仕入原価CSV）は得意先別ページ（＝見積書作成の場）に置くと文脈が合わない。**実施日が来た→更新する**作業なので、「実施日が来た」を実際に確認する **📅実施日カレンダー** が最適、とユーザ合意（配置=カレンダー内／得意先別からは撤去でスッキリ）。
- **実装**：
  - **`src/server.js`（sim画面のカレンダー `#calPanel` 内）**：`#calReminder` の直後に **`#calExport` バー**を追加＝「🏷 実施日が来た改定を 基幹システム（販売大臣）へ取込」＋実施日カットオフ`#calCutoff`(既定today)＋範囲セレクト`#calHanbaiScope`(改定すべて/発行済み・単価履歴のみ有効)＋**「📥 単価履歴CSV（売価）」`#calHanbaiBtn`**＋**「🏭 仕入原価CSV」`#calCostBtn`**＋`#calExpMsg`。CSS `.calexpbtn`/`#calExport`。クライアントJS `initCalExport()` で両ボタンに check→confirm→GET DL を配線（エンドポイントは既存の `/api/hanbai-export(-check)` `/api/cost-export(-check)` を再利用）。**正規表現は未使用＝二重エスケープ不要**、confirm内の改行は `\\n`（テンプレートリテラル規約どおり）。
  - **`src/customersPage.js`**：`#hanbaiBar`/`#costBar` の2バーと両ハンドラIIFEを撤去。代わりに **移動案内 `#hanbaiMoved`**（「基幹システムへの取込CSVは シミュレーション画面の 📅実施日カレンダー に移動しました」＋ `/` へのリンク）を残置＝迷子防止。
- **検証**：① server/customersPage 構文OK＋customers配信JS構文OK＋**customers孤児参照（hanbaiCutoff等7識別子）残存0** ② **実サーバ**：sim配信HTMLに calExport系6要素が存在＋**sim配信クライアントJS構文OK(vm.Script)**＋customersに移動案内あり ③ 両checkエンドポイント実走（単価履歴=315行/81得意先・税DB取得済 missingTax0／仕入原価=180商品）。⚠ **要 sim.bat 再起動**（server.js/customersPage.js）。
- 補足：カレンダーは常時表示パネル（モーダルではない）＝開く手間なくバーが見える。実施日が本日のときは既存の🔔赤バナーがすぐ上に出るので「今日が実施日→その場で2つDL」の動線が完成。

### 🆕 仕入先単価（新原価）更新CSV：実施日到来分を基幹システムへ取込（2026-06-02・実サーバ/実データ検証済み）

- **依頼**：指定の実施日がカレンダーで到来したとき、**仕入先単価（新原価）を基幹システムに更新**する作業を新規で。**CSV＝見出し＋「商品コード」「新原価」の2列**を、実施日にダウンロードできるよう、メーカー見積照合後に作成。
- **既存の「単価履歴CSV（売価更新）」とは別物**：あちらは得意先×商品の**販売単価**更新（11列・税付き）。今回は商品単位の**仕入原価**更新（2列）。両方とも販売大臣取込用の隔離機能。
- **新原価の出所＝メーカー見積の新仕入単価(`item.newCost`)**：`rules.js` で selfCostUplift(自社上乗せ%)は**売価側にしか効かない**ことを確認済み＝newCostは実際に払う仕入額そのもの。**小数を保持**（実コストは 6.8・9.97 等の小数。整数丸めしない＝基幹の仕入原価マスタを精度欠落させない）。商品コード＝自社商品コード(SHOHIN.CODE＝単価履歴CSVの商品コード列と同じ)。
- **実装**：
  - **`src/cost_export.js`（新規）**：`buildCostCsv(lines)`＝見出し「商品コード,新原価」(Shift_JIS固定バイト列・検証済み)＋データ行(ASCII数字/カンマ)。`numStr`で小数保持＆末尾0除去(最大4桁＝販売大臣内部精度)。コード無し/原価0は除外。Node標準にSJISエンコーダが無いため日本語は見出しのみSJISバイト埋め込み（単価履歴CSVと同方式）。
  - **`src/server.js`**：`collectCostUpdateLines(cutoff)`＝`buildCustomerCandidates` の keep行から、実施日(ISO)≤cutoff・商品コードあり・新原価>0 を**商品コードで1件に集約**（得意先に依らずコストは同じ／同一商品は実施日が新しい方）。発行の有無は問わない＝照合で確定した改定すべて。`buildCostExport(cutoff)`。ルート `POST /api/cost-export-check`（件数確認）／`GET /api/cost-export?cutoff=`（Shift_JIS添付DL）。DB不要。
  - **`src/customersPage.js`**：得意先別ページに既存「📥 販売大臣へ取込」バーの下に**「🏭 仕入先単価を基幹システムへ更新」バー**＝実施日カットオフ(既定today)＋「仕入原価CSVをダウンロード」。押すと check で件数確認→確認ダイアログ→GET でDL。
- **検証**：① cost_export/server/customersPage 構文OK＋**配信customers クライアントJS 構文OK(vm.Script)**＋UI要素4つ存在 ② buildCostCsv単体（見出しSJIS復号一致・42.3小数保持・コード無/原価0除外・CRLF） ③ **実サーバ+実データ**：check(today=2026-06-02)=180商品／check(2027-12-31=全実施日)=221商品・skipped0、DL=Shift_JIS見出し「商品コード,新原価」221行・**191行が小数(整数丸めなら欠落)**・商品コード重複0、スポットチェック 005861=新仕入単価6.8 がCSV「005861,6.8」と一致・実施日2026-07-01が将来カットオフでのみ出現＝フィルタ正常。
- ⚠ **日野/販売大臣 専用の隔離機能**（汎用版には載せない）。⚠ **要 sim.bat 再起動**（server.js/customersPage.js/新規cost_export.js）。
- **未確認（次回 or ユーザ）**：①基幹システム(販売大臣)の**仕入原価取込フォーマットの実仕様**＝見出し名/列順/小数許容/エンコード が本当にこれで読めるか（サンプルCSV未入手のため「商品コード,新原価・SJIS・小数保持」で仮置き）。実機取込で要確認、ズレたら numStr(整数化)や見出しを調整。②ブラウザ実操作（バー表示→DL）は未実機。

### 🆕 取り込みページ：実施日（切替日）の一括入力（実施日が無いメーカー見積向け）（2026-06-02・検証済み）

- **依頼**：メーカーのExcelに実施日の記載が無いものがある。取り込み時に**全商品へ実施日を一斉反映**したい。
- **実装（`src/importPage.js`）**：③（列の対応→保存）のグリッド上部に**「📅 実施日（切替日）を一括入力」バー**を追加＝日付入力＋スコープ選択（`空欄の行だけに入れる`/`全行に上書きする`）＋「全商品に反映」ボタン。
  - 日付は `jpDateToISO`（既存・どの形式でもISOに正規化＝`2026-07-01`/`7/1`/`2026年7月1日`/全角/Excelシリアル等）で揃える。読み取れない入力は赤字エラー。
  - **切替日の列が有る**メーカー見積：その列のセルを（スコープに従い）埋めて**見える化**。`render()` 末尾でも再反映するので、先頭行スキップ等で再描画しても消えない。
  - **切替日の列が無い**メーカー見積（今回のケース）：`forcedSwitchDate={date,scope}` を保持し、**`collect()` で各 item.switchDate へ付与**（authoritative＝列が無くても保存物に確実に入る）。緑メッセージ「実施日の列が無いので、保存時に全商品へ <日付> を付与します」。
  - スコープ `空欄の行だけ`（既定・安全）＝メーカーが一部だけ日付を書いている見積でも既存日付を壊さない／`全行に上書き`＝強制統一。
  - 新しいデータを読み込む（`loadRows`）と一括 実施日はリセット（別の見積へ持ち越さない）。
- **保存経路**：`it.switchDate`（ISO）→ `saveMakerQuote` が `切替日` 列へそのまま書き出し（`server.js:663`）→ 照合（loadMakerQuote）→ calc の effectiveDate に反映。**貼り付け/ファイル/手入力の全取込経路で共通に効く**（collect 経由）。
- **検証**：① importPage.js 構文OK ② **配信クライアントJS 構文OK（vm.Script・テンプレート解決後の実体）** ③ 追加UI要素4つ存在を確認。⚠ **要 sim.bat 再起動**。
- **残検証（ユーザテスト）**：ブラウザ実操作（実施日無しxlsxを読む→日付入力→全商品に反映→保存→照合結果/カレンダーに反映）は未実機確認。次回 headless で確認するか、ユーザ操作で確認。

### 🆕 メイン損益パネルを「全仕入先 合算」に変更（選択ファイルで数値が動かないように）（2026-06-01・動作確認済み）

- **指摘**：メインの損益数値が、選択中の照合結果ファイル（=1仕入先）の商品しか反映せず、ファイルを切り替えると数値が動く。**全部入っている商品（全仕入先）で集計しないと見る意味が無い**。
- **原因**：損益パネル `renderPL` は `lastSummary.totalCostImpact/totalSellImpact` を使うが、これは `calcAll(body.file)`＝**選択中1ファイルのみ**の集計だった。
- **実装**：
  - **サーバ（`src/server.js`）**：`impactAllSuppliers(body)` を新設＝`listLatestCsv()`（画面ドロップダウンと同じ全仕入先 最新CSV）を、現在の全体方針（ルール/端数/自社上乗せ%）で各々 `calcAll` し、`totalCostImpact/totalSellImpact` を合算（行ごと上書きは使わず全体方針で統一）。`supplierCount/withQty/perSupplier[]` も返す。`POST /api/impact-all`。`module.exports` に追加。
  - **クライアント**：`allImpact` を追加し `loadAllImpact()` で取得。`renderPL` は `allImpact`（無ければ暫定で `lastSummary`）の ΔC/ΔS を使用。パネル見出しを「**全仕入先の値上げ分**」に変更＋ `#plScope` に「＝全 N 仕入先・数量あり M 明細を合算」を表示。**ファイル切替(`#file` change)では損益は再計算しない**（表示の切替だけ＝全社は不変）。全体方針コントロール（ルール/掛率/端数/自社%）変更時は `recalcAll()`＝表の再計算＋`loadAllImpact()`。初期ロード・照合実行後にも取得。
- **検証**：server構文OK＋配信クライアントJS構文OK（vm.Script）。実データで `impactAllSuppliers`＝**7仕入先・数量あり524明細を合算**（合算 仕入増¥2,349,138/増収¥2,349,087）。単一ファイル（例 大黒工業¥432,764）から全社合算へ変わり、ファイル切替で動かないことを確認。⚠ **要 sim.bat 再起動**。

### 🆕 得意先別：見積書の「提出済み」が一目で分かる仕組み（2026-06-01・動作確認済み）

- **依頼**：得意先ページで見積を提出（発行）したとき、何か分かる仕組みが欲しい。
- **記録（`src/server.js`）**：`exportCustomerQuotes` の実発行時に **`output/発行履歴.json`**（gitignore・Drive同期で両PC共有）へ得意先ごとに `{lastIssuedAt, count, quoteNo, itemCount, folder}` を upsert（`recordIssuance`）。発行レスポンスに `issuedCustomers[]` を追加。API：`GET /api/issue-log`（一覧）／`POST /api/issue-log-reset`（body.customer=1件取消／空=全リセット）。価格・照合には一切無影響＝表示専用。
- **表示（`src/customersPage.js`）**：左一覧に **「✅ 提出済 M/D（×回数）」バッジ**＋行を薄緑（`.done`）。選択時ヘッダーに提出状況（最終提出日・見積No・品数・回数＋「提出済みを取消」ボタン）／未提出は灰バナー。ツールバーに「提出済 N/全」表示＋**「提出履歴をリセット」**（新サイクル開始用・確認あり・ファイルは消さない）。発行成功時に `issuedCustomers` で即バッジ反映＋`/api/issue-log` で正確化。`🔄最新の状態`でも履歴を取り直す（他PC発行も反映）。
- **検証**：構文OK＋配信JS OK。実API：bib を scope:one 発行→履歴に `{count:1,quoteNo:20260601-001,itemCount:4}` 記録→取消で消去を確認（テスト出力・履歴は後始末済み）。⚠ **要 sim.bat 再起動**。

## ★販売大臣DB 直結（2026-06-01 実装完了・実機検証済み）★

ユーザ要望「毎回のエクスポートをやめてDB直結したい（**見るだけ・書き込み無し**を厳守）」。会社PCを実機調査→**実装＆検証完了**。⚠ **読み取り専用(SELECTのみ)を厳守。会計DBへINSERT/UPDATE/DELETE/DDLは絶対禁止。**

### ✅ 実装したもの（2026-06-01）
- **`src/db_hanbai.js`（新規）**：販売大臣SQL Serverから販売実績を直接取得する**読み取り専用**ローダ。PowerShell(.NET SqlClient)を介し、`hanbai.js:parseHanbai` と同じレコード形を返す（下流 match.js/server は無改造）。SQLは得意先×商品(自社CD)で集計：現売単価=最新DENDATE行のTANK/10000、年間金額=期間内SUM(KG)、TOKSHI→TOKUI/SHO→SHOHIN/SHIIRE→SHIIRE をICODE結合。諸口(0000)・自社CD空は除外。
- **`src/shogo.js`**：`hanbai.source` で分岐＝`'file'`(既定)/`'db'`(厳格・失敗で中断)/`'auto'`(DB繋がれば直結・ダメればファイルへ自動フォールバック)。販売実績ファイルを引数で明示した時は常にファイル優先。
- **`config.js`**：`hanbai.source:'file'`（汎用既定）＋ `hanbai.db:{server:'localhost\\OHKEN', database:'HBDATA0001_001C', start:null, end:null, scale:10000}`。
- **`settings.json`（日野・gitignored）**：`hanbai.source:'auto'` ＝ **会社PC(DBあり)=直結／自宅PC(DBなし・Drive同期)=ファイル** を1設定で両立。
- **他社販売対応**：コードは汎用のまま。config既定が`'file'`なので**他社はDBを使わず従来どおり**（DB部分は未使用＝隔離）。会社固有差分は settings.json と config.hanbai.db のみ。汎用版は git タグ `v1.0-generic-base` で固定済み。

### 使い方・検証・OFF方法
- **使い方**：これまでどおり `照合.bat` を実行するだけ。`source:'auto'` なら自動でDB最新を読む（コンソールに「販売大臣DBから直接取得中」と出る）。**手動エクスポート不要**。
- **検証実績**：現行エクスポート(`2025-05_2026-05_年間販売実績.csv.XLS`)とDB集計を突合＝**ペア網羅100%(欠落0)・現売単価97.0%一致**。不一致3%は同日に複数伝票種(51売上/59別区分)があり現行レポートが別の行を「最新単価」に採る販売大臣内部のクセ／DB側が最新の実取引価格。照合の一致数も従来とほぼ同値(朝日189・中央化学100・大黒213)を実機確認。
- **OFFにしたい時**：`settings.json` の `"hanbai":{"source":"auto"}` を `"file"` に変える（または source 行を消す）→ 従来のファイル方式に戻る。
- **データモデル詳細**は下記「データモデルの実測」を参照（×10000スケーリング・DSHU種別・NAME連結 等）。

### 接続情報（このPCで確認済み）
- 販売大臣 **Super EX**＝**Microsoft SQL Server** バックエンド（Pervasiveではない）。インストール先 `C:\Program Files (x86)\OHKEN`。
- SQL Server インスタンス **`localhost\OHKEN`**（サービス `MSSQL$OHKEN` 稼働）。**Windows認証(Integrated Security)で接続可**。
- 既設のODBC DSN：`HBODDS *`（応研公式の販売大臣ODBCデータソース）、`HBWIN *`。ドライバ `ODBC Driver 17 for SQL Server`。※ツールからはODBCを使わず .NET SqlClient(PowerShell) 直結でも可。
- DB一覧：`HBCOMSTD`(共通マスタ) / `OHDIC`(辞書) / **`HBDATA0001_001C`(現用の売上台帳＝本体データ)** / `HBDATA0001_002C`(作成済み・空の次期ブック)。

### 本体データ＝`HBDATA0001_001C`（会社0001・現用ブック）
- **`URIMEI`（売上明細・170,696行）＝販売実績の本体**。得意先524・商品5,299・仕入先95。重要列：
  - `DENDATE`売上日 / `TOKSHI`得意先ICODE(→TOKUI.ICODE) / `SHO`商品ICODE(→SHOHIN.ICODE) / `NAME`(char50)商品名(明細直書き＝自社CD・メーカー品番が詰まる列) / `SUU`数量 / `TANI`単位 / **`TANK`売単価(現売単価)** / `KG`金額 / `HONTAI`本体価格 / **`BTANK`原単価(現仕入)** / `BKG`原価金額 / `SHIIRE`仕入先ICODE(→SHIIRE.ICODE) / `BIKOU`(char20)備考 / `ZEIRITU`税率。
- **`SHOHIN`（商品マスタ・7,984）**：`ICODE`(PK) / `CODE`(char15)自社商品コード / `NM1`商品名 / `KCODE`分類 / **`BARATANK`バラ売価 / `CASETANK`ケース売価**（現売単価）/ **`GENBARATANK`バラ原価 / `GENCASETANK`ケース原価**（現仕入）/ `BAIKA1..5BARATANK`得意先ランク別売価 / `LASTSHITANK`/`LASTSHIDATE`最終仕入 / `IRISUU`入数 / `TEK`摘要 / `SHIIRE`仕入先ICODE。
- **`TOKUI`（得意先・664）**：`ICODE`(PK)/`CODE`/`NM1`名/`ADD1`+`ADD2`住所/`TEL`/`FAX`/`TAN`担当。
- **`SHIIRE`（仕入先・116）**：`ICODE`(PK)/`CODE`/`NM1`名/住所/TEL＝ツールが取り込む仕入先マスタそのもの。
- ※ICODE=内部数値キー（結合用）、CODE=表示用コード。明細→マスタは ICODE で結合。

### 読み取りの実証コマンド（PowerShell・標準.NETのみ＝追加インストール不要）
```powershell
$cs="Server=localhost\OHKEN;Database=HBDATA0001_001C;Integrated Security=SSPI;TrustServerCertificate=True;Encrypt=False"
$cn=New-Object System.Data.SqlClient.SqlConnection $cs; $cn.Open()
$cmd=$cn.CreateCommand(); $cmd.CommandText="SELECT ... FROM dbo.URIMEI ..."  # SELECTのみ
# SqlDataAdapter.Fill で DataTable に取得 → CSV化
$cn.Close()
```
Node標準だけではSQL Server に繋げない（ドライバ非搭載）ため、**DB読取はPowerShell(.NET SqlClient)経由**にするのが無依存で確実（このPC実証済み）。クラウドSaaS化時はこの経路は使わず、各社がエクスポートしたCSVを渡す従来方式に戻す（DB直結はオンプレ運用専用）。

### データモデルの実測（2026-06-01・価格を1円もズラさないための要点）
- **⚠ 内部スケーリング ×10000**：`URIMEI.SUU`(数量)・`TANK`(売単価)・`BTANK`(原単価) は **10000倍の整数**で格納。実値＝`/10000`。例：SUU 10000000＝1000個、TANK 120000＝12.0円。`KG`(金額)は**素の円**（スケーリング無し）。検算「ROUND(SUU/1e4 * TANK/1e4)=KG」が売上(DSHU=51)の**92.5%(14022/15156)で一致**＝式は正。残り不一致は運賃・値引・セット親・手入力金額など通常ケース。
- ※ `TANKSHOKETA`/`SUUSHOKETA` は**表示小数桁**(2/0/3)であって内部スケーリングとは別物（混同しない）。
- **伝票種類 `DSHU`**：`51`=売上(15,156件・主) / `52`=値引等(1,111) / `59`=別区分売上(376・単価あり) / `81`=無償・本体のみ(59・単価0)。**「年間販売実績」に何を含めるかは現行エクスポートCSVと突合して確定**（おそらく51中心＋52で値引調整）。
- **商品名は連結**：`NAME`(主)＋`NAME2`(入数/運賃メモ例「100（大黒P1.5元未満800P別途300」)＋`NAME3`(埋込メーカー品番例「3485134」)。parseHanbai の body 相当はこの連結。
- **現売単価＝「バラ最新単価」**＝得意先×商品で**最新DENDATEのTANK**。年間金額＝期間内 `SUM(KG)`。原単価(BTANK)は0が多い（ツールは売単価を主使用＝問題なし）。
- **現行エクスポート元**：`config.hanbai.path` = `G:\マイドライブ\価格改定\価格改定AI\販売大臣CSV\2025-05_2026-05_年間販売実績.csv.XLS`（**2025-05〜2026-05 の1年**を得意先×商品で集計した.XLS）。これが再現の正解＝**変換して突合**する。
- **マスタ結合**：URIMEI.TOKSHI→TOKUI.ICODE(得意先)、URIMEI.SHO→SHOHIN.ICODE(自社CD=SHOHIN.CODE)、URIMEI.SHIIRE→SHIIRE.ICODE(仕入先CD=SHIIRE.CODE・4桁=purchaseCode)。

### 実装の振り返り（採用＝案B相当：構造化列から直接レコード化）
- **採用**：SQLの構造化列(TOKUI.NM1/SHOHIN.CODE/URIMEI.TANK/BTANK/SHIIRE.CODE)から parseHanbai形のレコードを直接組む `db_hanbai.js`。crammed-string解析を経由しないので堅牢。**照合中核(match.js/hanbai.js)は無改造**。
- **残課題/精緻化の余地**：①現売単価の3%差（同日複数伝票時の「最新単価」選択を販売大臣の挙動に完全一致させるなら、DSHU優先順位や51限定の調整が必要）。②年間金額(SUM KG)は現行レポートと76〜85%一致＝集計対象DSHU/期間の差。損益の規模感用途なので当面許容、必要なら現行レポート定義に合わせ込む。③期間は既定で約13か月ローリング（config.hanbai.db.start/end で固定可）。④原単価(BTANK)は0が多く未使用でOK。
- 旧プラン（参考・未採用）：案A＝SQL→現行CSV同形を生成して無改造で流す。CSVの階層フォーマット再現が脆いため案B採用。
- **2案**：
  - **案A（低リスク推奨の第一歩）**：SQL→**現行の販売実績CSVと同等のCSV**を生成し、`config.hanbai.path` に置くだけ。既存パイプライン無改造。※現行エクスポートCSVの実列構成を `hanbai.js` で確認してから列を合わせる必要あり。
  - **案B（堅牢・将来）**：SQLの構造化列(TOKUI.NM1/SHOHIN.CODE/URIMEI.TANK/BTANK/SHIIRE.CODE)から、match.jsが要するレコード形(normName/custName/currentSell/origCost/annualAmount/purchaseCode…)を**直接**組む新ローダ。crammed-string解析の脆さを回避できるが、hanbaiの出力形を正確に再現する要あり。
- **未確認(次回やる)**：①現行エクスポートCSV(`config.hanbai.path`)の実フォーマットと URIMEI 列の対応づけ ②「年間」の期間定義（DENDATEの範囲で絞るか、得意先×商品で最新単価をとるか）③ `BTANK`(原単価)が信頼できるか（旧メモ「原単価0が多い」は古い可能性。実データで要確認）。
- ⚠ 必ず**読み取り専用アカウント**で運用するのが理想（SQL権限でdb_datareaderのみ付与）。当面はWindows認証で読むが、書き込みクエリは一切発行しない。

## ★次にやる作業（ここから再開）★

### 🆕 /self（自社データ設定）にDB直結の案内を追加＝販売実績ファイル設定は会社PCでは不要（2026-06-01・検証済み）

- **指摘**：DBを見ているので「年間販売実績ファイル」の取込設定は不要では？期間は過去1年見れていればよい。
- **回答（実装）**：その通り。会社PCは `source:auto`＝販売大臣DBから直接取得＝ファイル設定は未使用。期間は**過去約1年をローリング自動取得**（`config.hanbai.db.start/end=null`で毎回自動）。手動エクスポートも期間設定も不要。
- **🆕 期間境界を月単位に変更（ユーザ要望「月が変わったら移動、1日単位では移動しない」）（`db_hanbai.defaultRange`）**：旧＝end=明日(日次でずれる)/start=13か月前月初。新＝**start=12か月前の月初／end=翌月の1日**（当月末まで含む排他上限）。これで**月内はどの日でも同じ窓・月替わりでだけ1か月前進**。例：2026年6月のどの日でも `2025-06-01〜2026-07-01`、7月になると `2025-07-01〜2026-08-01`。検証：6/1・6/15・6/30で同一、7/1で移動、年跨ぎOK。
- **画面（`src/selfPage.js`＋`/api/hanbai-source`）**：`/api/hanbai-source` に `source` と `dbRange{start,end}` を追加。/self の販売実績カード上部に、source が db/auto のとき**緑バナー**「✅ 販売大臣DBから直接取得（期間=過去約1年・自動）。下のファイル設定はDBが無いPC/他社運用の予備＝会社PycPCでは使わない」を表示。ファイルパス未設定でもDB時はエラー扱いしない。
- **検証**：server/selfPage 構文OK＋配信/self JS構文OK＋`/api/hanbai-source` が source:auto・dbRange 2025-05-01〜2026-06-02 を返すことを確認。⚠ **要 sim.bat 再起動**。ファイル設定UI自体は残置（フォールバック/他社版用）。

### 🆕 カレンダーの「販売実績ファイル要更新」リマインドをDB直結時は出さない（2026-06-01・検証済み）

- **指摘**：実施日カレンダーの「販売実績ファイル：最終更新 2026-05-28（要更新）」は、DB直結なら不要では？
- **その通り**：あの警告は**ファイル方式（手動エクスポートCSVを読む）前提**。`hanbai.source` が `db`/`auto`（会社PCはDBから直接＝常に最新）なら販売実績ファイルは使われず、警告は的外れ。
- **修正（`src/server.js`）**：`buildCalendar` の戻り値に `hanbaiSource` を追加。クライアント `renderReminder` で **source が `db`/`auto` のときは販売実績を緑「DB直結：常に最新（手動更新不要）」表示にし、ファイルの『要更新』を出さない**（`auto` は「DBが無いPCのみファイル使用」と補足）。`file` のときだけ従来のファイル新しさ警告。**損益.csv は別ファイル（PL用）なので従来どおり警告**（DB化していない）。
- **検証**：server構文OK＋配信sim画面JS構文OK。実サーバで `/api/calendar` が `hanbaiSource:"auto"` を返す（現settings＝auto）＝販売実績は要更新を出さない挙動に。⚠ **要 sim.bat 再起動**。
- 補足：`/self` ページの「照合で実際に使われる販売実績ファイル」は情報表示なので残置（ファイル方式/フォールバック時の確認用）。

### 🆕 販売大臣「単価履歴」取込CSVのダウンロード（実施日到来分）（2026-06-01・実機/DB検証済み）

- **依頼**：価格改定の見積を発行後、実施日が来たら、販売大臣に取り込み直すためのCSV（`売上履歴tesuto.csv` の形式）をダウンロードできるように。出力範囲＝**実施日が来た分だけ抽出**（ユーザ選択）。
- **対象ファイル形式**（`売上履歴tesuto.csv` を解析＝販売大臣の単価履歴取込・**11列・Shift_JIS・CRLF・BOMなし**）：`得意先コード, 商品コード, ＩＤ(=1固定), 履歴日付(年)(月)(日), バラ／ケース区分(=1固定), 単価(=新販売単価), 原単価(=新仕入単価), 消費税区分, 消費税率表№`。
- **税2列はDBから取得（確認済み）**：商品マスタ `SHOHIN.ZEIKBN`(=消費税区分／分布:2が7964・1が20)・`SHOHIN.ZEIRITU`(=消費税率表№／分布:1が7923・3が61＝軽減等の例外あり)。**固定でなく商品ごとにDB照会**（`db_hanbai.lookupShohinTax` 読み取り専用）。DB未接続時は標準値(2/1)へフォールバック＋警告。
- **Shift_JIS出力をライブラリ無しで実現**：日本語はヘッダー行のみ＝そのSJISバイト列(120B)を `src/hanbai_export.js` に固定埋め込み、データ行は数字/カンマ(ASCII)だけで連結。`buildHanbaiCsv(lines, taxMap)`。
- **抽出ロジック（`src/server.js collectHanbaiExportLines(cutoff, issuedOnly)`）**：`buildCustomerCandidates`(現設定で再計算)で、**実施日(ISO)≤指定日**かつ得意先/商品コードあり・新売価>0 の行を、**得意先×商品で1件に集約**（実施日が新しい方）。価格は現設定の再計算値＝発行後に全体方針を変えていなければ見積書と一致。
- **🆕 スコープ切替（ユーザ要望で既定変更）**：`issuedOnly=false`（既定＝**改定すべて**：発行の有無を問わず実施日到来分）／`true`（**発行済みの得意先だけ**）。`発行履歴.json` は issuedOnly時のみ参照。理由：ユーザは「実施日が来た改定を全部 販売大臣に入れたい」（カレンダー6/1=184件等）＝発行済みに限らず一括更新したい。実機(6/1まで)：改定すべて=258行/74得意先、発行済みのみ=2行/1得意先（bibのみ）。※258は6/1分184＋それ以前の日付分の合計（≤cutoffのため）。
- **UI（`src/customersPage.js`）**：得意先別ページに「📥 販売大臣へ取込」バー＝実施日カットオフ(既定:今日)＋**範囲セレクト（改定すべて／発行済みの得意先だけ）**＋「単価履歴CSVをダウンロード」。押すと `/api/hanbai-export-check`(body:cutoff,issuedOnly) で件数確認→確認ダイアログ(対象N行/M得意先/税DB未取得K件は標準値)→`GET /api/hanbai-export?cutoff=&issuedOnly=` で添付ダウンロード。`buildCustomerCandidates` の item に `customerCode` を追加。
- **検証**：全ファイル構文OK＋配信JS構文OK／buildHanbaiCsv単体（SJIS復号で11列・ID1・バラケース1・税率表3の軽減商品・不正実施日skip を確認）／**実サーバ＋DB**：check count=2・missingTax=0・dbError無し、DL=Shift_JIS/BOMなしで `0040,001387,1,2026,6,1,1,167,129,2,1` 等を確認（得意先0040・商品001387＝販売大臣実CODE・税2/1はDB由来）。
- ⚠ **日野/販売大臣 専用の隔離機能**（DB直結と同じ思想）。他社版（別基幹システム）は取込形式が異なるので**汎用版には載せない**。⚠ **要 sim.bat 再起動**。`売上履歴tesuto.csv` はサンプル（git管理外）。

### 🆕 セキュリティ強化3点（配布前の防御）（2026-06-01・実機検証済み）

モニター配布を見据えたセキュリティレビュー＋強化。まず現状を実コードで監査＝**外部送信ゼロ（全fetchが相対パス＝自分自身のみ／telemetry無し）・`127.0.0.1`バインド・外部実行は全て`execFile`の引数配列でシェル注入不可・CORS許可ヘッダ無し・`safeOpenPath`はROOT封じ込め・getRecsはbasename化で遡上不可**を確認。その上で3点追加（`src/server.js`＋`src/settings.js`）：
- **#1 ローカル専用ガード（既定ON・透明）**：リクエスト入口で **Host が localhost/127.0.0.1/::1 以外＝403**（DNSリバインディング対策）、**Origin があり localhost 以外＝403**（CSRF対策）。`isLocalHostHeader/isLocalOrigin`。正規の自分の画面操作は素通り。検証：低レベルhttpで偽装Host(attacker.com/1.2.3.4)→403・正規→200、外部Origin→403・ローカルOrigin→200。
- **#2 任意のアクセスパスワード（既定OFF＝認証なし）**：`settings.json` の `accessPassword` が設定された時だけ有効。未認証はHTML→ログイン画面(`LOGIN_PAGE`)／API→401。`POST /api/login` 一致で Cookie `apw=<sha256(apw|pw)>`（HttpOnly/SameSite=Strict/24h、**平文は保存せずハッシュのみ**）。`settings.js` の getSettings/saveSettings に `accessPassword` を追加（UI保存で消えない）。検証：バックアップ→passwordセット→未認証/401/誤・正・Cookie・通常表示を確認→**settings.json をバイト単位で原本復元**。
- **#3 手順書に「データの安全」節を追記**（`使い方手順書.md`＋`.html` の §13）：ローカル動作で外部送信なし／完全ローカルにしたいなら同期フォルダ以外に置く／共用PCは accessPassword でロック。
- ⚠ **要 sim.bat 再起動**（server.js/settings.js 変更）。配布用クリーン版でも #1 は自動で効く。共用でなければ #2 は設定不要。

### 🚧 最優先の次作業：汎用「販売実績CSV取り込み」（他社展開・モニター募集の前提）（2026-06-01・方針合意／未実装）

- **背景**：ユーザが同業の知人にモニター募集→将来SaaS化したい。その前に「**販売大臣以外の基幹システムのデータでも動くか**」を検証する方針。知人の実データ（実績の吐き出しCSV）を1件借りて形を確認するのが最善、で合意。
- **🔴 重要な発見（要対応）**：販売実績の読み込み `src/hanbai.js parseHanbai` は **販売大臣の階層出力（得意先見出し行＋字下げ商品行・先頭セルに〔自社CD＋商品名〕詰め込み・購買日列で商品行判定）に固定**。他社の基幹システムが出す**フラットなCSV（1行＝1明細：得意先/自社商品CD/商品名/原価/売価）は今のままでは読めない**（得意先がたどれず・商品行判定で読み飛ばす）。`config.hanbai.source:'file'` でも結局 parseHanbai を通る＝**“他社はファイルで従来どおり”は販売大臣形式前提だった**。メーカー見積側は柔軟（Excel/CSV/貼付/手入力＋列マッピング）なので、足りないのは**販売実績側だけ**。
- **やること**：**汎用 販売実績CSV取り込み**を新設（メーカー見積取込と同じ「列の対応を選ぶ」方式）。`得意先 / 自社商品コード / 商品名 / 現仕入(原価) / 現売価 /〔任意〕年間金額・数量・メーカー品番` を割り当て→ `parseHanbai` と同じレコード形（customerCode/customerName/productCode/productName/currentSell/annualAmount/origCost/norm/codeNorm/coreNorm/purchaseCode）に変換する新ローダ（`db_hanbai.js` が構造化列から直接組んだのと同じ作り）。照合中核(match.js)は無改造で流せる。1社の形に固定せず一般化。
- **進め方**：知人の実物CSVを1件入手→列を確認→それに合わせて取込を作り＋他システムにも効くよう汎用化→①取込②照合③得意先別自動見積を実機検証→OKでクリーン版を配布。データ取扱い：本人同意・ローカルのみ・検証後削除・一部でも可。
- **ボーナス**：データを貸してくれた知人が「自分のデータで動いた」を体験＝第一号モニターになりやすい。
- **配布の前提（再掲）**：現フォルダは日野の実データ・DB直結設定が丸ごと入る。配布は必ず**クリーン版**（settings.json/input/maker_quotes/output/発行履歴.json/config.hanbai.path/DB直結を除去。gitタグ `v1.0-generic-base` が土台）。

### 🆕 得意先ページから「提出済の見積書」を直接開く（2026-06-01・検証済み）

- **依頼**：得意先ページから、提出済みの見積書をそのページで開きたい。
- **実装**：
  - **サーバ（`src/server.js`）**：`POST /api/open-issued`（body.customer）を新設。発行履歴(`発行履歴.json`)から得意先の `folder` を引き、`output/<folder>/見積_<sanitizeName(得意先)>.xlsx` を `safeOpenPath` で開く（Windowsは explorer→Excel）。ファイルが無ければフォルダを開く、フォルダも無ければ「移動／削除された可能性」エラー。ROOT封じ込め済み。
  - **クライアント（`src/customersPage.js`）**：提出済みヘッダー（`.issuednote`）に **「📄 提出済の見積書を開く」ボタン（`.openq`）** を追加（取消ボタンの隣）。`openIssued(name)` が `/api/open-issued` を叩く。提出済みの得意先だけ表示。
- **検証**：server/customersPage 構文OK＋**配信クライアントJS 構文OK(vm.Script・解決後の実体で確認)**。実APIで ①履歴なし→「提出履歴がありません」②得意先空→エラー、を確認（happy pathはExcelが開くため実起動は避け、ファイル名規則＝exportQuotesと同一であることで担保）。⚠ **要 sim.bat 再起動**（server.js/customersPage.js 変更）。
- ⚠ **事故と教訓（2026-06-01）**：この作業中、テストスクリプト末尾の無条件 `unlinkSync('output/発行履歴.json')` で**実ファイルを誤削除**→ユーザが Google ドライブのゴミ箱から復元（ｂｉｂ 1件・count1/quoteNo20260601-001/itemCount4 で正確に復旧）。`発行履歴.json` は **gitignore＝Driveのみ**でバージョン管理外。**テストで実ファイル名を削除対象にしない**（後始末は固有名 `.__test_*` のみ・条件分岐後も無条件 unlink を書かない）。

### 🆕 取り込みページ①を「仕入先マスタ（仕入先コード）主役」に作り替え（2026-06-01・検証済み）

- **依頼**：①仕入先選択は自社マスタの仕入先コードをメインに。手打ち＋新規は意味が薄い。
- **業務の確認**：値上げ通知は「**問屋（朝日・中東など）からまとめて1つ**」届く＝仕入先＝発注先（マスタのコード）が単位、とユーザ確認。
- **重要な前提（実データ調査）**：仕入先コードと「メーカー名（＝照合キー）」は1対1ではない。**1コードに複数メーカーがぶら下がる**（例 `0013 朝日` の下に 朝日/福助/エフピコ/中央化学）。全データ（maker_quotes/照合結果/productLinks/makerChannel）は**短い仕入先名**がキー。マスタ会社名は `朝日食品容器　株式会社`/`ハウスホールドジャパン　株式会社 11：00`（ゴミ付き）等で名前がズレる。
- **対処（`src/importPage.js`）**：
  - **①の主役＝`#purchaseCodeSel`（マスタの仕入先コード）を先頭・目立つ位置**に。選ぶと `cleanName()` で会社名を掃除（㈱/株式会社/全半角空白/末尾の時刻ゴミ除去）して**仕入先名を自動補完**＋発注先コードを自動設定。掃除後の名前は**直接取引4社（朝日食品容器/大黒工業/ハウスホールドジャパン/旭創業）の既存キーと完全一致**＝照合結果/紐付けと**分裂しない**ことを検証済み。
  - **手打ち＋登録済み選択は「▸ その他」折りたたみに格下げ**（マスタに無い仕入先／問屋の下のメーカーを分けたいとき用）。`#supplierSel`（登録済み）と `#supplier`（直接入力）は残置＝中央化学/福助のような“問屋の下のメーカー”は従来どおり登録済み選択 or 名前手入力で扱える。
  - **ロック判定を簡素化**：`updateStep2Lock` は「仕入先名が入っていれば②を解放」に統一（マスタ選択／登録済み選択／手入力のどれでも名前が入れば進める）。
  - **「＋新規」は発注先コードを保持**（マスタで選んだ問屋コードを消さない＝問屋の下のメーカー名だけ手入力する用途に対応）。
- **検証**：importPage.js 構文OK＋**配信クライアントJS 構文OK(vm.Script)**＋`cleanName()` を配信実体で実行して5社の掃除結果が期待どおり（朝日食品容器/大黒工業/ハウスホールドジャパン/旭創業/エフピコ商事）。⚠ **要 sim.bat 再起動**。
- **残検証（ユーザテスト）**：ブラウザでの実操作（マスタ選択→仕入先名自動補完→②解放→保存）は未実機確認。次回 headless で確認するか、ユーザ操作で確認。

### 🆕 取り込みページに「方法C：1件ずつ手入力」＋自社コード直接入力を追加（2026-06-01・実機検証済み）

- **依頼**：メーカー見積取込ページにフリー入力（貼り付け）とは別に、**商品名・メーカーコード・自社コードなどを項目ごとに設定して入力**できるようにしたい。
- **方法C：1件ずつ手入力（`src/importPage.js`）**：ステップ②に「✏ 手入力をひらく」トグル＋**固定レイアウトの入力表**（メーカー品番／メーカー商品名*／**自社コード**／規格／現単価／新単価*／切替日）を追加。口頭・メモ・FAX・電話連絡など貼り付けが使えない時に項目ごとに直接入力できる。最終行に入力すると自動で1行追加。「この内容を下の表に反映 →」で**既存の③（列対応プレビュー→保存）パイプラインにそのまま流す**（貼り付け/ファイルと同じ保存・検証経路＝一貫UX）。手入力は固定レイアウトなので `suppressProfile` フラグで**保存済み書式(他レイアウト)による列上書きを抑止**。①で仕入先未選択の間はトグルもロック。
- **自社コードの意味づけ（`src/server.js saveMakerQuote`）**：FIELDSに `selfCode='自社コード'` を追加（手入力だけでなく**貼り付け/ファイル取込でも自社コード列をマッピング可能**）。`guess()` に自社コード判定を「コード→makerCode」より**前**に追加（`自社コード` が `コード` で品番に誤判定されないように）。保存時、**自社コードが入った行は `saveProductLink(仕入先, 自社CD, メーカー商品名)` で「📌 手動紐付け（100%確定）」として登録**＝照合で自社CD⇔メーカー商品名を確定（保存直後の自動照合から有効）。解除は「📊 一覧・進捗」から可能。**maker_quotes CSV は7列のまま（自社コード列は増やさない）＝照合/下流(match.js等)は無改造**＝紐付け辞書(settings.productLinks)経由で効かせる安全設計。
- **検証**：① server.js/importPage.js 構文OK＋**配信クライアントJS 構文OK(vm.Script)** ② `guess()` で手入力7ヘッダが全て正しい項目に自動マッピング（自社コード→selfCode を含む）③ `saveMakerQuote` 直接実行＝2件保存・linkedCount=1・CSVヘッダ7列(自社コード列なし)・`009999→"テスト容器 角 小 本体"` の紐付け生成（selfCodeありの1件だけ）・後始末で settings.json 不変 を確認（テスト出力は削除済み）。⚠ **要 sim.bat 再起動**（server.js/importPage.js 変更）。
- **追補：切替日の正規化を強化（全取込経路）（`src/importPage.js jpDateToISO`）**：取込画面の切替日が一部の形式（`7/1`・`8/15`・`2026.7.1`・全角・Excelシリアル）で揃わなかったのを、**サーバ側 `normDateInput` と同じ強さ**に書き換え＝NFKC（全角→半角）＋Excelシリアル(4-6桁)＋区切り何でも(`\\D{1,3}`：-/.年月日空白)＋年なしは当年。これは方法Cだけでなく**貼り付け・ファイル取込でも共通**（`applySkipAndRender` のB段で切替日列に適用）。検証：配信実体で 2026/7/1・2026.7.1・2026年7月1日・7月1日・7月1日～・7/1・8/15・全角・7-1・46133(→2026-04-21) が全てISOに揃い、「未定」/空はそのまま（ロックしない）こと、`jsDetectColumns` が手入力ヘッダ「切替日」を日付列(列6)として検出することを確認。

### 🆕 残課題のコード修正3件＋DB年間金額の精緻化＋input整理（2026-06-01・実機検証済み）

ユーザ依頼「（残課題を）全部やった方が良いのでしたらお願いします」を受け、CLAUDE.md の「報告のみ（未修正）」の安全な3件＋DB直結の精緻化＋フォルダ整理を実施。**照合エンジン中核(match.js/hanbai.js)は今回も未修正**（正解CSV未復元のためリスク回避）。⚠ **要 sim.bat 再起動**（anomaly.js/server.js 変更）。

- **(1) 価格異常を「行ごと上書き後の値」で再判定（`src/server.js buildCustomerCandidates`＋`src/anomaly.js`）**：従来は得意先別ページで行ごとに 行ルール/まるめ/手入力 で上書きしても、価格異常(priceReason)は**全体ルールでの計算値のまま**で再判定していなかった（既知の限界）。修正＝上書き後の `item.currentSell/item.newSell/ruleForRow` で `priceRowAnomaly` を**呼び直す**。`costConflict`（同一メーカー品番に新仕入が複数＝売価に依らないメーカー見積の重複）は元判定を保持。併せて `anomaly.js` の②「値上げなのに同額/値下げ」判定を **`keep_sell` に加え `manual`(手入力) も抑止**（利用者が意図的に決めた固定価格を要確認に落として発行から外さないため。①売単価0/欠落は引き続き常時検知）。**実機検証**：bib/S-01会計票で ①行をkeep_sell上書き→改定後70で一覧に残る（誤って要確認に落ちない）②手入力で値下げ(56)→一覧に残る ③手入力0→要確認へ正しく分離、を確認。
- **(2) `parseSelfName` の入数(packSize)誤認を抑制（`src/server.js`）**：運賃カッコありの行で商品名末尾の数字を入数と誤認し得た既知の限界。修正＝**運賃カッコが入数/運賃の指標語（ｹ/ｹｰｽ/元/未満/円/別途/個/枚/本/袋/P数 等）を含むときだけ**末尾数字を packSize として切り出す。`（朝日ﾋﾟｯｷﾝｸﾞ）`のような**問屋名だけのカッコ**の前の数字は商品名（サイズ等）として core に残す。検証：`白楊…3点 100（10ｹ元/未満800円）`→packSize=100、`寿司容器 20 （朝日ﾋﾟｯｷﾝｸﾞ）`→packSize空(core保持)、大黒未閉じ運賃→packSize=50。
- **(3) 統合見積書(`src/merge_quotes.js`)で据置(keep_sell)を誤って要確認に落とす問題を解消**：merge は**仕入先別の段階で承認済みの見積書**を読むが、`priceRowAnomaly` を ruleType 無しで呼んでいたため、据置(改定単価＝現単価)の正常行が②に該当して外れ得た。xlsxから ruleType は復元できないため、`priceRowAnomaly(cur,nw,'keep_sell')` を渡して②を抑止し、統合段では①「売単価が無い/0」だけを弾くように変更。検証：最新の仕入先別396明細で旧/新の要確認数とも0（現データに据置は無く回帰0＝据置が出たとき誤除外しない保険）。
- **(4) 販売大臣DB直結：年間金額の集計DSHUを精緻化（`src/db_hanbai.js`）**：会社PCで実DBに接続し、現行エクスポート(`2025-05_2026-05_年間販売実績.csv.XLS`・1661ペア)と突合して**集計対象DSHUを実測で確定**。
  - **現売単価＝変更なし**（全DSHUから最新DENDATE行のTANK）。検証で**全DSHU最新=97.1%／DSHU=51&TANK>0限定=79.8%**＝**現行の方が良い**ことが判明したため、当初の「51限定」案は採用せず現行ロジックを維持（コメントに実測値を明記）。
  - **年間金額＝`SUM(amt)`(全DSHU) → `SUM(CASE WHEN DSHU IN(51,52) THEN amt END)`(売上＋値引) に変更**。実測一致率：全DSHU=76.0%／51のみ=68.8%／**51+52=83.2%**／51+52+59=76.0%。51+52が最良＝現行レポートの年間金額は「売上−値引」。**実機検証**：改修後の `loadHanbaiFromDb` を実走（DB読取1.5s・2542ペア）→現売単価97.1%維持・年間金額83.2%(従来76.0%→+7.2pt)を本番コード経路で確認。⚠ **読み取り専用(SELECTのみ)厳守＝INSERT/UPDATE/DELETE/DDLは一切発行していない**。
- **(5) input/ の整理**：仕入先ごとに 0531(旧) と 0601_1407(新・本日の照合) の2本ずつ計12本があり、ツールは最新(0601)しか使わないため、**旧0531の6本を `input/_old` へ退避**（reversible）。0601が正当な再照合か行数で確認（朝日325→332・大黒207→259等＝同水準の正常な差）。退避後 input/ は6本(各仕入先1本)にクリーン化。**output/(見積書＝業務成果物)は私が作成したものではないため一切触っていない**（整理が要れば指示を）。

### 🆕 4件まとめ作業：手順書確認＋日付正規化の根治＋コードレビュー修正3件（2026-06-01・動作確認済み）

ユーザ依頼「①手順書にCSV削除手順を追加 ②取り込みの日付正規化バグ修正 ③コード全体の点検・改善 ④現状整理」を順に実施。**照合エンジン中核(match.js/hanbai.js)は今回も未修正**（正解CSV未復元のためリスク回避・報告のみ）。⚠ **要 sim.bat 再起動**（server.js/listPage.js を変更）。

- **(1) 手順書のCSV削除手順＝既に対応済みだった**：`使い方手順書.md`/`.html` の両方に「## 11. 対象（照合結果CSV）を一覧から消したいとき（手動）」が既に存在（input/ を開く→`<仕入先>_照合結果_<日時>.csv` を削除 or `input/_old` へ移動→sim再起動）。CLAUDE.md の「未着手」メモが古かっただけ＝**新規作業なし**。
- **(2) 取り込みの日本語日付→ISO年推定バグを根治（`src/makerXlsx.js`）**：従来 `serialToDate` は Excelシリアルしか変換せず、「7月1日～」等の日本語日付が**素通り**していた（カレンダーで過去月に出る等の根本原因）。新たに `normDate(v)` を追加＝Excelシリアル／`2026-07-01`／`2026/7/1`／`2026年7月1日`／`7月1日～`／`7/1`（**年なしは当年固定**＝過ぎた月日でも翌年に飛ばさない）／全角に対応、解釈不能（「未定」等）は素通り。`convert()` の切替日出力を `serialToDate`→`normDate` に。旧 `serialToDate` は server.js の貼り付けプレビュー(normalizeMakerGrid)でも使うため**挙動を変えず残置**（後方互換）。importPage.js の `jpDateToISO` は既に当年固定（5/31修正済）＝**両取り込み経路で当年固定に統一**。検証：normDate 11/11形式パス＋実xlsx(中東3シート)で 2026-06-01/07-01 に正規化を確認。
- **(3-a) 🔴重要バグ修正：`得意先別.bat` の統合見積書が空になる問題（`src/merge_quotes.js`）**：見積書レイアウトが 5/30 に「No列廃止→商品コード列」へ変わったのに、`readQuote` のヘッダー検出が旧 `r[0]==='No' && r[1]==='商品名'` のままで**永久に一致せず明細0件→全得意先スキップ→統合見積書が空**になっていた（手順書は今も④で `得意先別.bat` を案内＝実害大）。ヘッダーを**列名から動的に引く方式**（「商品名」「改定単価」を含む行を探し、商品コード/現行単価/改定単価/実施日/備考の各列を名前で対応づけ）に変更＝**新旧レイアウト両対応**＋統合見積書にも商品コード・備考が載るように。検証：実データで `node src/merge_quotes.js`→**86得意先・336明細**（正の成果物 `_1824` と同数）、新形式の見積書を生成→商品コード・備考とも正しく読めることを確認。
- **(3-b) listPage.js の操作ボタンのエスケープ漏れ修正**：`onclick="openPath('...')"` にファイル名を文字列連結していたが、`esc()` はシングルクオート `'` を escape しないため、仕入先名/ファイル名に `'` が混ざると壊れる潜在バグ。`unlink` と同じ **data属性＋委譲リスナ方式**（`.act-open[data-path]`/`.act-sim[data-file]`）に変更。配信スクリプト構文OK確認済。
- **(3-c) `safeOpenPath` の堅牢化（`src/server.js`）**：①ROOT封じ込めチェックを末尾セパレータ込み（`=== root || startsWith(root+sep)`）にし兄弟ディレクトリ取りこぼしを防止 ②`exec('explorer "..."'）`の文字列連結を **`execFile('explorer',[path])`**（引数配列＝シェル展開なし）に変更し、パスに `"` が混じってもコマンドが壊れない。/api/export のフォルダオープンも execFile 化。ローカル単一利用なので悪用リスクは低いが防御として実施。
- **報告のみ（今回は未修正）**：①`merge_quotes` の `priceRowAnomaly` に ruleType 未渡し＝据置(keep_sell)行が統合段で誤って要確認に落ちる（xlsxから ruleType を復元できないため。据置を統合経路に乗せない前提）。②`parseSelfName` の packSize 抜き取りが、運賃カッコありの行で商品名末尾の数字を入数と誤認し得る（要確認・実データ次第）。③行ごと上書き(rowRules/rowRound/manual)後に `priceReason`(価格異常)を再判定しない既知の限界。④match.js 集約キーの区切り無し連結・hanbai.js 末尾数字の仕入先コード誤認＝**照合精度に直結、正解CSV未復元のため触らない**。
- **(4) 現状整理（確認のみ・ファイルは消していない）**：input/ は計73本だが sim/得意先別は「仕入先ごと最新1本」を読む。最新は全6仕入先(ハウスホールドジャパン/中央化学/大黒工業/旭創業/朝日食品容器/福助)とも **2026-05-31 12:30 のクリーンな照合バッチ**で揃っている。旧67本は最新選択に無影響だがフォルダが混雑＝必要なら `input/_old` 退避を提案（ユーザ承認待ち）。最新の得意先別成果物＝`output/得意先別_見積書_20260531_1118`。settings＝`makerChannel:{エフピコ→朝日食品容器}`・新仕入先 旭創業 登録済み。

### 🆕 取り込み運用の再設計：仕入先ごと統合＋上書き更新＋メーカー寄せ替え＋商品コード（2026-05-31 夜・動作確認済み）

ユーザの「メーカー見積の取り込みと現運用に再考が必要」を受けた一連の改善。**照合エンジン中核(match.js/hanbai.js)は触らず**、取り込み〜集計の経路だけを直した。すべてユニットテスト＋実照合＋ヘッドレスChrome実機で検証済み。⚠ **要 sim.bat 再起動**。

- **(1) 得意先ページ＋見積書に「商品コード」列を追加**：得意先ページ明細表＆発行前プレビューで**仕入先と商品名の間**に自社商品コード列（`td.pcode` 等幅）。見積書(`quoteXlsx.js`)も5列→**6列**化（A商品コード/B商品名/C現行/D改定/E実施日/F備考）。先頭ゼロ保持のため文字列出力。`src/customersPage.js`/`src/quoteXlsx.js`/`src/server.js`(qrows 2か所に productCode 追加)。
- **(2) 案A：メーカー見積を「仕入先ごとに1本へ統合」＋同一商品は最新で上書き（`src/shogo.js`）**：
  - 旧：取り込みCSVごとに照合結果を1本出力→同じ仕入先を複数回取り込むと input/ にバラバラに増え、**メイン画面(対象ドロップダウン=最新1本)では古い分が消えて見え、得意先ページ(全CSV)では二重に出る**食い違いがあった。
  - 新：`mergeMakerFiles(makerFiles, channelMap)` を新設＝**仕入先ごとにまとめ、同じ商品（メーカー品番優先・無ければ商品名）は新しい取り込みファイルが後勝ち（上書き）**。古い→新しい順に upsert。1仕入先=1照合結果に。実例：朝日の取り込み4本→統合239品で1本／大黒2本→1本。
  - ユーザ要件「Aを100→120で取込→同じAを120→150で再取込したら上書き更新」を満たす（ユニットテストで A上書き・別商品B保持・重複なしを確認）。
- **(3) 得意先ページの読み込みを `listCsv()`→`listLatestCsv()` に統一（`src/server.js` buildCustomerCandidates）**：以前は input/ 全CSVを読み古い照合結果が混ざる恐れ。仕入先ごと最新1本だけ読む（案A統合で1仕入先1本になったので最新=完全）。メイン画面と挙動一致。
- **(4) メーカー→問屋(仕入先)の寄せ替え `makerChannel`（二重計上の解消）**：
  - 問題：**エフピコ商品は朝日食品容器(問屋,0013)経由で仕入れている**のに、中東xlsxのエフピコシートが「エフピコ」という別仕入先として取り込まれ、朝日の取り込み(エフピコ商品を含む)とぶつかり、**得意先24社60行で同じ商品が朝日とエフピコの2仕入先に二重計上**（価格は完全同一＝浮動小数差のみ）。エフピコ一致48品は全部朝日にもあった。中央化学/福助/大黒は重複なし＝**直すのはエフピコだけ**。
  - 対処：`settings.json` に **`makerChannel: {"エフピコ":"朝日食品容器"}`** を新設。`mergeMakerFiles` の channelMap で照合時に**エフピコを朝日の仕入れとして統合**（item.supplier も朝日に書換）。`src/settings.js` の `getSettings`/`saveSettings` に makerChannel を追加（UIで保存しても消えない）。結果：エフピコ単独仕入先が消え、得意先見積は518品→**458品・二重0**。今後 中央化学等も寄せたければ makerChannel に1行追加でOK。
  - 古いエフピコ照合結果は `input/_old` へ退避（最新1本選択でエフピコが復活しないように）。productLinks の「エフピコ」キー2件(007419/004858)は **「朝日食品容器」キーへ移行**（朝日側のメーカー名と完全一致を確認してから移行→📌手動紐付けで有効、一致商品の損失0）。
- **(5) 仕入先名のゴミ修正**：実データに `ハウスホールドジャパン」`（末尾に余分な`」`）が紛れていた→ maker_quotes CSV中身＋ファイル名＋settings.json makersキーから`」`除去（仕入先コード0047は維持）。古い`」`付き照合結果16本を `input/_old` へ。⚠ 名前で統合するので、今後`」`無しで取り込むと別仕入先化する穴を防ぐ予防措置。※仕入先マスタ(0047)の会社名にある余分な「11：00」は別件で未修正。
- **メモ**：ハウスホールドジャパンは規格品の全一覧見積（実績なし商品も含む）なので休眠952/987は**正常**（バグではない）。
- **未対応/limitation**：.xlsx取り込み(makerXlsx)は今も `<仕入先>.csv` 上書き方式。同じxlsx修正→入れ直しは更新反映されるが、別々の部分xlsxを2回入れると後勝ちで前が消える（Web貼り付け取込は全統合されるので影響なし）。必要なら makerXlsx も商品単位upsertに拡張可。

### 🆕 サーバが1回のエラーで全停止しないよう保護（「Failed to fetch」対策）（2026-05-31・動作確認済み）

- **症状**：得意先別ページで「個別作成」を押すと「通信失敗: Failed to fetch」。
- **切り分け**：Failed to fetch＝**サーバに届いていない**（クライアントのネットワークエラー）。個別作成の処理自体はサーバ側で正常（実APIで check OK・issue OK・ファイル生成・後始末まで確認）＝**コードのバグではなくサーバ停止**が原因。得意先選択は通信なし（画面内のみ）で動くため、ボタンを押して初めて停止に気づく流れ。
- **根本原因**：`src/server.js` に **グローバルなエラーハンドラが無かった**ため、未処理の例外/Promise拒否が1回でも起きると**プロセスごと落ち**、以降すべて Failed to fetch になっていた。
- **対処（`src/server.js`）**：`if (require.main === module)` 内に `process.on('uncaughtException')` / `process.on('unhandledRejection')` を追加＝エラーをコンソールに**ログするだけでプロセスは落とさない**（テスト時は require のみなので未装着＝テストのエラーは握りつぶさない）。⚠ **要 sim.bat 再起動**。
- **運用メモ**：再発したら sim.bat の黒い画面に `[uncaughtException] …` が出るので、その内容＋直前の操作（例：メーカー見積取込＝自動照合が走る）を確認。古いサーバ残留/別ポートも定番原因（再起動＋新しいタブで開く）。

### 🆕 得意先別：行ごとの「まるめ」上書きを追加（2026-05-31・動作確認済み）

- **依頼**：得意先別ページで改定後価格のまるめ処理を**行ごと**に設定可能に（全体の基準でよい商品もあれば、個別設定が必要な商品もあるため）。
- **仕組み**：既存の「行ルール上書き」と同じ方式。明細表の「行ルール」列の右に **「まるめ」列**（select。先頭＝（全体）＝上のまるめ設定を継承／optgroup＝切り捨て・四捨五入・切り上げ × 整数/0.1/0.01）。上書きした行は緑（`ov`）＋「適用中」に「🟢 行まるめ N件」。
- **クライアント（`src/customersPage.js`）**：状態 `rowRound{rowKey:"単位|処理"}`、`calcOpts()` に載せる、`rowRoundSelect(p)`、列ヘッダー（売価グループ右の行ルールの隣・全体14列に）、change で `load()`。⚠ rowround は `.rowrule` クラスも持つので、行ルールの配線は `select.rowrule:not(.rowround)`、まるめは `select.rowround` で**別々に bind**（二重発火防止）。
- **サーバ（`src/server.js` `buildCustomerCandidates`）**：`rowRound` を読み、各行で **effRuleType（行ルール上書き or 全体）と effRounding（行まるめ上書き "単位|処理" or 全体）** を決定。どちらかが全体と違えば `calcRow(r,{default:{type:effRuleType,factor},rounding:effRounding,selfCostUplift})` でその行だけ再計算（表示＝見積書＝同じ経路）。手入力売価(manual)は従来どおり最優先（まるめ非適用＝固定価格）。
- **検証**：構文OK＋配信JS OK。実APIで bib/S-01会計票：全体(銭・四捨五入)78.69 → その行だけ「整数・切捨て」上書きで **78**、**他行は不変** を確認。⚠ **要 sim.bat 再起動**。

### 🆕 得意先別：粗利率の列表示＋低マージン警告＋一覧の開閉（2026-05-31・動作確認済み）

ユーザ依頼3点（すべて `src/customersPage.js` のみ・サーバ変更なし＝粗利率はクライアントで算出。item に currentCost/newCost/currentSell/newSell が既にあるため）。

- **(1) 粗利率の列**：明細表の「現売価」の右に **現粗利率**、「改定後売価」の右に **改定後粗利率** を追加（売価グループ colspan 3→5）。`marginRate(sell,cost)=(売価-仕入)/売価×100`（売価0/無は—）、`fmtPct1`。列数はヘッダー/ボディとも13で一致。
- **(2) 低マージン警告（基準＝改定後粗利率／ユーザ選択）**：計算バーに **「低マージン警告 [15]% 未満」入力（`#cLowMargin`・既定15）**。`改定後粗利率 < しきい値` の行は表の改定後粗利率セルを**オレンジ強調**（CSS `td.lowmargin`）＋ツールチップ。詳細ヘッダーに黄色バナー「⚠ 低マージン N件（改定後粗利率 X%未満）」、左の得意先一覧にも **「薄利N」タグ**（`.lowm`）。しきい値変更は**再計算不要**＝`renderList()`＋`selectCust(selName)` で表示だけ即更新（`load()` は呼ばない）。※既存の「値上率が大きい(≥30%)」アラートはメイン画面の別物（上り幅の注意）。これは粗利率ベースの新規。
- **(3) 得意先一覧の開閉**：ツールバーに **「◀ 一覧を隠す / ▶ 一覧を表示」トグル（`#toggleListBtn`）**。`.wrap.list-collapsed .col-list{display:none}` で左を隠し、右の明細（`.col-detail{flex:1}`）が全幅に。トグルはツールバー常設なのでいつでも戻せる。
- **検証**：構文OK＋配信JS OK。実APIで各商品に currentCost/newCost あり＝粗利率算出可、しきい値15%で **改定後粗利率<15% が11件/8得意先**（例 bib DXPD丸38防曇蓋 現1.1%→改定後3.1%）を確認。⚠ **要 sim.bat 再起動**。

### 🆕 得意先別：商品名クリックでメイン画面の該当照合行へジャンプ（2026-05-31・動作確認済み／新しいウィンドウで開く）

- **ユーザ提案**：得意先別ページの商品名をリンクにし、クリックで別タブにメイン画面が開き、その商品の照合行（一致度・メーカー品・現/改定後 仕入 等）を確認できるように。役割分担「得意先別＝見積作成／メイン＝照合確認」の動線を1クリックに。
- **得意先別（`src/customersPage.js`）**：`prodLink(customer,supplier,code,name)` を新設。商品名を `<a target="_blank">` リンク（青・点線下線、CSS `a.prodlink`）にし、**明細表とプレビューの両方**で使用。リンク先＝`/?focusSupplier=<仕入先>&focusCustomer=<得意先>&focusCode=<自社CD>&focusName=<商品名>`（HTML属性内は `&amp;` 区切り＋値は encodeURIComponent）。プレビュー payload に `productCode` を追加（リンクに必要）。
- **メイン（`src/server.js`）**：起動時に URL パラメータを読み、`focusSupplier` があれば **その仕入先の最新照合結果ファイルを自動選択**（`<仕入先>_照合結果` 前方一致）して `loadFiles(target)`→`focusRow()`。`focusRow` は ①得意先＋自社CD ②得意先＋商品名 ③CDのみ ④名のみ の順で `baseRows` から該当行を探し、`scrollIntoView`＋黄色ハイライト（CSS `tr.focusrow`・6秒で消える）。見つからなければ黄色メッセージ。`focusKey`＝NFKC＋小文字＋空白除去で照合（テンプレートリテラル内なので正規表現は `\\s` 二重エスケープ）。通常起動（パラメータ無し）は従来どおり `loadFiles()`。
- **制約**：メインの対象ドロップダウンは「仕入先ごと最新1本」なので、同じ仕入先に複数メーカー見積がある場合は最新側に飛ぶ（実用上ほぼ問題なし）。
- **検証**：server/customersPage 構文OK＋両ページの配信JS OK。実サーバでエンドツーエンド＝得意先別プレビューの先頭サンプル（島田／エフピコ／CD007485／AP-F15-11本体ｴｺ）でリンク先ファイルを自動解決し、**そのファイル内に 得意先＋CD で該当行が存在**することを確認（＝飛び先で正しくハイライトされる）。⚠ **要 sim.bat 再起動**。

### 🆕 得意先別：見積書 発行前プレビューを追加（2026-05-31・動作確認済み）

- **依頼**：得意先別ページで見積書作成の確定ボタンの前にプレビューが欲しい。
- **変更前**：要確認が有る時だけモーダル（除外一覧）、無い時は `confirm()` ダイアログ→発行。発行される中身は見えなかった。
- **変更後＝常にプレビューモーダルを表示**：
  - **サーバ（`src/server.js` `exportCustomerQuotes` の action:'check'）**：戻り値に **`preview`** を追加＝発行時と同じ並び（仕入先→商品名）・同じ値で、得意先ごとの明細 `{supplier,productName,currentSell,newSell,effectiveDate,note}` を返す。上限 `PV_CUST_CAP=300`/`PV_ROW_CAP=4000`、超過時 `previewTruncated:true`（プレビューは確認用・発行は全件）。
  - **フロント（`src/customersPage.js`）**：`exportFlow` は要確認の有無に関わらず常に `openGate` を呼ぶ。`openGate` を改修＝①**発行される内容のプレビュー**（得意先ごとにセクション見出し＋明細表。`.pv-cust`/`.pv-head`/`.pv-cnt` のCSS追加、数値右寄せ `td.num`）→②要確認があれば下に「⚠ 要確認（発行から除外）」一覧（従来の赤/黄テーブル）。発行ボタンのラベルは要確認有=「要確認を除外して発行」/無=「この内容で発行」。**発行できる行が0なら発行ボタンを無効化**＋案内。「戻る（修正する）」は従来どおり。発行バーのヒント文も「発行前に内容をプレビュー表示」に更新。
- **検証**：server/customersPage 構文OK＋配信JS OK。実サーバ起動で `/api/customers-export` action:check（scope:all）＝issuable 96得意先/469品・review 8件・preview 96得意先（truncated false）、先頭 島田46品の明細（AP-F15-11 本体ｴｺ 14.3→17.5/実施日2026-06-01/備考）まで返ることを確認。⚠ **要 sim.bat 再起動**。

### 🆕 全体コードレビュー＆修正5件（2026-05-31・動作確認済み）

ユーザ依頼「全体をチェック・改善点/修正点」。4領域を並行レビュー→各指摘を再検証し、**安全で価値の高い5件のみ修正**（照合エンジン中核 match.js/hanbai.js は回帰検証用の正解CSVが失われている状態のため、リスク回避で今回は触らず報告のみ）。

- **(1) xlsx生成のXMLエスケープに制御文字除去（`src/xlsxutil.js` esc）**：XML1.0禁止のC0制御文字（タブ09/改行0A/復帰0D以外の0x00-0x1F）を除去。**備考(自由入力)や外部Excel由来の商品名**に制御文字が混じっても見積書が壊れない。※制御文字をソースに直書きしない（\u エスケープも転送で壊れた）ため**文字コード比較でフィルタ**する実装。xlsx.js/quoteXlsx.js 共用＝1か所で両対応。検証：VT/NUL除去・タブ/エンティティ保持・備考に制御文字を入れても生成OK。
- **(2) 据置(keep_sell)で一覧0件になる問題を修正（`src/anomaly.js`＋`src/server.js`）**：`priceRowAnomaly(currentSell,newSell,ruleType)` に ruleType を追加し、**keep_sell の行は②「値上げなのに同額」を判定しない**（据置は同額が正常）。①「売単価0」は引き続き検知。calcAll の呼び出しを `priceRowAnomaly(r.currentSell, r.newSell, r.ruleType)` に。merge_quotes は ruleType 無し呼び出し＝後方互換（②継続）。検証：keep_sell同額→空／add_increase同額→検知／keep_sell売単価0→検知／値上げ正常→空。
- **(3) 貼り付け取込の日本語日付が翌年(2027)になるバグを修正（`src/importPage.js` jpDateToISO）**：年なし「M月D日」を「次に来る同月日」で推定していた（当年の過去月日を翌年へ誤飛ばし）→**当年に統一**（下流 normDateInput と同方針）。※.xlsx自動取込(makerXlsx)は元々日本語日付を変換せず素通り＝この経路は無関係。makerXlsx 側の取込時ISO正規化は引き続き残課題。
- **(4) 不正/空JSONボディで500→フロントの赤エラー化を修正（`src/server.js` readBody）**：parse失敗時に reject せず **resolve({})**。各APIが 200+{ok:false} を返せ、フロントの分岐（ok:false→黄色）が崩れない。
- **(5) メイン画面の効いていない「実施日」UIを削除（`src/server.js`）**：役割分担で見積書作成を得意先別ページへ移した結果、メインの実施日 列・一括入力・「全行へ」は `recalc` が送らず**どこにも反映されないデッドUI**だった（編集しても無反応＝誤解の元）。削除＝表ヘッダーの実施日th／各行の `.effdate` 入力td／`#effectiveDate` 一括入力欄＋`#applyEffAll`ボタン＋そのclickハンドラ。**実施日カレンダー(📅)は別機能なので残置**。検証：列数ヘッダー/ボディとも16で一致、実サーバ起動→配信HTML取得で配信JS構文OK・effdate/effectiveDate完全除去を確認。
- **(6) 得意先別ページへのボタン重複を解消（`src/server.js`）**：メインに `/customers` へ飛ぶリンクが2つあった（①ヘッダー上部の小リンク「👥 得意先別 商品」＋②表上の緑バナー「👥 得意先別ページで見積書を作成 →」＝同機能）。ユーザ選択により**バナーだけ残しヘッダーの小リンクを削除**（CSSの `#customersLink` セレクタも除去）。`/customers` リンクは1つに統一。得意先別→メインは「← シミュレーション画面へ」で戻れる。実サーバ起動で確認。⚠ 要 sim.bat 再起動。
- **レビューで見つけたが未対応（リスク/低優先）**：①match.js 集約キーの区切り無し連結で稀に衝突しうる／hanbai.js 末尾数字を仕入先コードと誤認しうる＝**照合精度に直結、正解CSV未復元のため触らない**（実害が出てから慎重に）。②/list が得意先別の見積書発行を進捗判定しない。③listPage.js のボタンでファイル名に `'` 混入時に壊れる（実害低）。④取込後の自動照合がレスポンス完了まで待つ（重い販売実績で待ち時間・仕様上許容）。⑤確認ゲート「ステップB＝個別修正して含める」未実装。
- ⚠ **要 sim.bat 再起動**（全変更）。

### 🆕 取り込み後に自動で照合（「↻ 照合を実行」を押さなくてよくなった）（2026-05-30・動作確認済み）

- **依頼**：メーカー見積を取り込んだら、トップ(sim)の「照合を実行」を押さなくても対象に出るように。
- **背景**：従来は **取り込み→maker_quotes/ に保存しただけ**で、`input/` の照合結果は作られず、sim「対象」ドロップダウン（`listLatestCsv`＝input/ の `<仕入先>_照合結果_<日時>.csv` を読む）に出なかった。利用者は別途「↻ 照合を実行」or `照合.bat` が必要だった。
- **変更（`src/server.js`）**：Web取り込みの保存は全経路が `POST /api/maker-quote`（=`saveMakerQuote`）を通るので、この1か所で、保存成功後に **自動で `runShogo(['',''])` を実行**（maker_quotes 全件 × `config.hanbai` → input/）。結果をレスポンスに `shogo:{ok,files}` として返す。照合が失敗しても**見積の保存自体は成功扱い**のまま（`shogo.ok=false` を返すだけ）。
- **取り込み画面（`src/importPage.js`）**：保存完了メッセージに自動照合の結果を併記。成功＝「🔄 自動で照合しました（照合結果 N本を更新）。対象にすぐ出ます」、失敗＝黄色で「⚠ 自動照合に失敗（理由）。『↻ 照合を実行』を押してください」。
- **注意**：照合は販売実績（`config.hanbai.path`）を読む。旧.XLSなら Excel COM で自動CSV変換するため保存後に数秒かかる（＝「↻ 照合を実行」と同じ挙動）。CLI経路（`取込.bat`＝makerXlsx／maker_quotes に直置きした.xlsx）は従来どおり `照合.bat` で展開＝今回の自動照合はWeb取り込み(/import)が対象。
- **検証**：server.js / importPage.js 構文OK＋配信クライアントJS構文OK。⚠ **要 sim.bat 再起動**。

### 🆕 見積書：No列を廃止＋A4(横)1ページ幅に収まる印刷設定（2026-05-30・動作確認済み）

- **依頼**：見積書左端の No(1,2,3,4…) は不要。A4に収まるように。
- **変更（`src/quoteXlsx.js`）**：表をNo列なしの **5列（A商品名/B現行単価/C改定単価/D実施日/E備考）** に組み直し（列幅・全マージ・dim をA〜Eに整合）。印刷設定を追加＝**A4(`paperSize=9`)・横(landscape)・幅は1ページに収める(`fitToWidth=1`/`fitToPage`)**、高さは品目数で複数ページ可(`fitToHeight=0`)。余白を少し詰めた。縦にしたい時は `orientation` を `portrait` に変えるだけ（幅は fitToWidth で自動縮小）。
- **検証**：生成xlsxを解析し No(>No<)消滅・ヘッダー5列・dim A1:E…・pageSetup landscape/fitToWidth・F列セル無しを確認。⚠ **要 sim.bat 再起動**。

### 🆕 得意先別：見積書に「備考」列を追加（画面で入力→見積書に転記）（2026-05-30・動作確認済み）

- **依頼**：得意先別ページに備考欄を設け、入力すると見積書にも記載されるように。
- **流れ**：得意先別ページの明細表の右端に「備考」入力列を新設 → `rowNote{rowKey:値}` でサーバへ（`calcOpts()` に載る）→ `buildCustomerCandidates` が各 item に `note` を付与（表示＝見積書で同値）→ `exportCustomerQuotes` の qrows に `note` → `quoteXlsx.js` の備考列(E)に出力。空でも item に持たせて常に一貫表示。入力した行は緑(`man`)＋「適用中」に「🟢 備考 N件」。
- **変更ファイル**：`src/customersPage.js`（rowNote 状態・列・入力欄・listener）／`src/server.js`（buildCustomerCandidates で note 付与・export の qrows に note）／`src/quoteXlsx.js`（備考列）。
- **検証**：3ファイル構文OK＋配信JS OK。生成xlsxで備考ヘッダー・値の転記を確認。⚠ **要 sim.bat 再起動**。

### 🆕 得意先別：「改定後価格 まるめ」プルダウン（小数点カット等を名前で選択）（2026-05-30・動作確認済み）

- **依頼**：得意先別ページの自動計算 改定後価格に「小数点以下カット」「小数第1位以下カット」等の機能。
- **背景**：機能自体は既存の 端数単位(1/0.1/0.01円)＋端数処理(四捨五入/切上げ/切捨て) の組合せで実現可（カット＝切捨て floor）だが、2プルダウンの組合せが直感的でなかった。
- **変更（`src/customersPage.js`）**：2つの抽象プルダウン(`#cUnit`/`#cMode`)を **1つの「改定後価格 まるめ」(`#cRound`)** に置換。値は `単位|処理`（例 `1|floor`＝小数点以下カット）。optgroupで「切り捨て(カット)／四捨五入／切り上げ」×「整数/小数第1位/第2位」を名前で選べる。`calcOpts()` で単位・処理に分解して送信（サーバ側 `applyRounding` はそのまま）。`initControls` は settings の unit/mode を `単位|処理` に合成。**メインpage(sim)の端数コントロールは未変更**（依頼が得意先別のみ）。
- **検証**：構文OK・旧 cUnit/cMode 参照の残り無し・配信JS OK。applyRounding で小数カット系が期待値（77.9→77 等）を返すことを確認。⚠ **要 sim.bat 再起動**。

### 🆕 sim：得意先別への導線を独立バナー化（見えにくさ解消）（2026-05-30・動作確認済み）

- **症状**：メイン下部「👥 得意先別ページで見積書を作成 →」が見えにくい/見えない。原因＝`<a class="go">` だが CSS の白文字・余白は `button.go` 限定で `<a>` に効かず、既定リンク色が緑背景に埋もれていた＋多数コントロールの最後尾に押し込まれていた。
- **対応（`src/server.js`）**：フィールド列から出し、表の上に**独立した横長バナー**（薄緑＋左に緑アクセント帯／🧾＋見出し「見積書の作成は『得意先別ページ』で行います」＋説明＋右に白文字の緑ボタン、ホバーで濃色）として配置。⚠ **要 sim.bat 再起動**。

### 🆕 得意先別：改定後売価・実施日を直接入力可＋表記を自動で揃える（2026-05-30・動作確認済み）

- **依頼**：得意先別ページで「改定後売価」と「実施日」を**直接入力**でき、**どんな入力をしても表示が揃う**ように。
- **改定後売価**：列をテキスト入力に。手入力は **`¥`/`円`/カンマ/全角/空白を除去して数値に揃える**（`parsePriceInput`・NFKC）。手入力した行はその固定価格が最優先（`ruleForRow='manual'`）＝行ルールより上。空にするとルール計算へ戻る。手入力セルは緑(`man`)。
- **実施日**：列をテキスト入力に。**どの形式でも ISO `YYYY-MM-DD` に揃える**（`normDateInput`）。対応＝`2026-07-01`/`2026/7/1`/`2026.12.25`/`2026年8月3日`/`7月1日`/`8/15`（年なしは当年）/Excelシリアル`46133`→2026-04-21/全角。解釈不能（例「未定」）はそのまま保持。**自動の実施日も含め全行をこの正規化で表示**＝列全体が揃う。
- **流れ**：手入力は `rowSell{rowKey:値}`/`rowEff{rowKey:値}` でサーバに送り、`buildCustomerCandidates` が正規化して item に反映 → 画面は正規化後の値を再表示（＝入力直後に表記が揃う）。`calcOpts()` に載るので**表示も見積書(exportCustomerQuotes)も同じ値**。
- **サーバ（`src/server.js`）**：`normDateInput()`・`parsePriceInput()` を新設（normalizeEffDate の近く）。`buildCustomerCandidates` で manualSell/effManual を適用、effectiveDate は常に `normDateInput`。
- **検証**：構文OK＋配信JS OK。API：`¥1,234`→1234(sellManual)、`2026/7/1`→2026-07-01(effManual)、日付6形式すべて期待どおり（未定はそのまま）。**ヘッドレスEdge実機**：売価入力「¥1,234」→欄が「1234」に整形・緑・売価改定%再計算、実施日「2026/7/1」→「2026-07-01」、「売価手入力1件/実施日手入力1件」表示、コンソールエラー0。⚠ **要 sim.bat 再起動**。

### 🆕 メイン＝全体方針の決定＋損益確認／得意先別＝引き継いで実行（2026-05-30・動作確認済み）

- **背景（ユーザの気づき）**：見積作成を得意先別に移した結果、メインの転嫁ルールが「損益シミュレーション用」になっていた。役割を明確化＝**メインで全体方針を決めて保存→得意先別がそれを初期値として引き継ぐ**ように配線（前は両ページが settings 既定を別々に読むだけで、メインの選択が得意先別へ伝わらない穴があった）。
- **メイン(`src/server.js`)**：転嫁ルールのラベルを「**転嫁ルール（全体方針）**」に。**「💾 全体方針を保存」ボタン**を追加＝今のメイン上部の `転嫁ルール/掛率/端数(単位・処理)/自社上乗せ%` を `POST /api/settings` で `default{type,factor}/rounding{unit,mode}/selfCostUplift{rate}` として保存（`saveSettings` は部分マージ＝会社情報・見積文言は保持）。保存後 `#msg` に内容を表示。
- **得意先別(`src/customersPage.js`)**：ルール欄ラベルを「転嫁ルール（全体）＋『メインの全体方針が初期値』」に。**「↻ 全体方針を取込」ボタン**＝`initControls()` を再実行して settings 既定（=メインで保存した方針）を全体コントロールに読み直し→再計算（行ごとの上書きは保持）。`initControls` は従来から settings 既定を読むので、**メインで保存→得意先別を開く（or 取込ボタン）で引き継ぎ**。
- **役割の整理（確定）**：**メイン=照合確認＋改定後仕入(コスト)の確認/編集＋全体方針の決定＋全社損益インパクト**／**得意先別=全体方針を引き継ぎ、行ごとに営業微調整して見積書を発行**。
- **検証**：構文OK＋配信JS OK。API：方針 `{markup/1.4, 端数1円ceil, 自社+3%}` を保存→GETで反映＆**company.name 等は保持**、その後 原状復帰も確認（実 settings.json を汚さずテスト）。ヘッドレスEdge：sim=💾全体方針ボタン＋「全体方針」ラベル、customers=↻取込ボタン＋「メインの全体方針」ヒント、両ページ コンソールエラー0。⚠ **要 sim.bat 再起動**。

### 🆕 役割分担：メイン＝照合確認／得意先別＝見積作成（行ごとルール）（2026-05-30・動作確認済み）

- **ユーザ方針の確立**：①メインで改定額・転嫁ルールを**全体セット** → ②得意先別ページで**営業が行ごとに微調整**（「ここまで上げると逃げられる」等）→ ③**見積書作成は得意先別ページのみ**。メインは照合確認用。
- **(1) 得意先ページに「行ルール」列を追加（行ごと転嫁ルール）**：
  - 各商品行にルール選択（（全体）／上乗せ／粗利維持／掛率×／売価×仕入率／据置）。**既定＝（全体）＝上のメイン設定を継承**。上書きした行はセレクトが緑(`ov`)＋「🟢 行ルール上書き N件」表示。
  - **計算は client では持たず、行ルールを `rowRules{rowKey:ruleType}` でサーバに送り再集計**（表示も見積書も `buildCustomerCandidates`→`calcRow` の同じ経路＝1円もズレない）。`rowKey = 得意先仕入先(正規化品番|名:正規化品名)`。サーバ(`buildCustomerCandidates`)は各 item に `rowKey`/`ruleForRow` を付与し、上書きがあればその行だけ `calcRow(r,{default:{type:ov,factor},rounding,selfCostUplift})` で再計算。`calcOpts()` が rowRules を載せるので **表示(/api/customers)も発行(/api/customers-export)も同じ上書きが効く**。
  - 既定ルールの継承：得意先ページの全体ルール `cRule` は起動時に settings.json の既定(=メインの既定)を読む（従来どおり）＝「メインの転嫁ルールが入っている状態」。
- **(2) メイン(sim)から見積書作成を撤去**：`#exportBtn`「この内容で見積書を出力」を削除し、**`/customers` へのリンク「👥 得意先別ページで見積書を作成 →」**に置換。クライアントの export ハンドラも除去（ボタン消滅で null 参照エラーになるため必須の同時対応）。`/api/export`・`renderExportSummary` 等は無害なので残置（未使用）。
- **検証**：構文OK＋配信JS OK（fresh server）。API：先頭行を keep_sell 上書き→その行だけ newSell=現売価(70)・他行不変、markup×1.5上書き→70×1.5=105。**ヘッドレスEdge実機**：得意先=行ルール列4セレクト・据置変更で改定後売価70/改定%0.0%/セレクト緑/「上書き1件」、sim=exportBtn無し・/customersリンク有り、両ページ コンソールエラー0。⚠ **要 sim.bat 再起動**。
- **既知の限界（v1）**：価格異常/低一致(要確認)の判定は**全体ルールでの計算**を使う（行を keep_sell 上書きすると「値上げなのに同額」に該当し得るが現状は再判定しない）。実害は小（通常は別の値上げルールへ上書き）。気になれば行上書き後に anomaly 再判定する手当て可。

### 🆕 転嫁ルール「現売価 × 仕入改定%」を追加（2026-05-30・動作確認済み）

- **依頼**：転嫁ルールに「現売価 × 仕入改定%」（仕入の値上げ率をそのまま売価に乗せる）を追加。
- **式（`src/rules.js`）**：新ルール `sell_cost_rate` ＝ `現売価 × (新仕入 ÷ 現仕入)` ＝ 現売価×(1+仕入改定%)。現仕入が無い/0なら絶対額上乗せ(add_increase相当)にフォールバック。
- **⚠ 重要（実証済み）**：この式は **数学的に既存「現在の粗利率を維持(keep_margin_rate)」と同一**。理由＝`新仕入/(1−粗利率)=現売価×(新仕入/現仕入)`。実データ全396商品で **395件 完全一致**（残り1件も丸め境界 6.12/6.13 の差のみ）。**ユーザは「同じ結果と承知のうえ、直感的な言い回しの選択肢として残す」を選択**。
- **ラベル**：選択肢ラベルを **「現売価 × 仕入改定%（仕入と同率で値上げ＝粗利率維持と同結果）」** とし、同一であることを明記（sim上部・設定画面・/customers の3フルセレクト＋sim行ルールの短ラベル「売価×仕入率」＋RULE_LABEL）。
- **検証**：構文OK＋配信HTMLに sell_cost_rate が sim 3か所(全体/設定/行)＋customers に出現・新ラベル反映を確認。fresh server で S-01会計票＝現売価70×(54/49)=**77.14**（=粗利率維持77.14）。⚠ **要 sim.bat 再起動**。
- ⚠ **検証時の落とし穴（再発）**：bash の inline kill（複雑なPowerShellエスケープ）が失敗し**旧サーバが8765に残留**して旧結果を返すことが複数回あった。判定は **PowerShellツールで `Get-CimInstance Win32_Process … server.js … Stop-Process` → `Get-NetTCPConnection -LocalPort 8765` で空き確認 → 起動** の順で確実に。

### 🔧 転嫁ルール「掛率」を 新仕入×掛率 → 現売価×掛率 に変更（2026-05-30・動作確認済み）

- **依頼**：転嫁ルール `markup`（掛率）の計算式を **新仕入×掛率 → 現売価×掛率** に変更。
- **変更**：`src/rules.js:computeNewSell` の markup を `newCost*factor` → `currentSell*factor` に。UIラベル「新仕入 × 掛率」を **「現売価 × 掛率」** に（sim上部・設定画面・/customers の3か所）。行ごとルールの短ラベル「掛率」と手順書は式に触れていないので変更不要。
- **検証**：API実走で S-01会計票 現売価70×1.3=**91**（旧は新仕入54×1.3=70.2）。add_increase 等 他ルールは不変（70+(54-49)=75）。⚠ **要 sim.bat 再起動**。
- ※ rules.js はルール計算の中核で sim/見積書出力/得意先集計の全経路が通る＝**1か所の修正で全画面・全出力に反映**される。

### 🆕 改定%（現→改定後）をメイン画面と得意先ページに表示（2026-05-30・動作確認済み）

- **要望**：現仕入→改定後仕入・現売価→新売価の **改定%** を、メイン(sim)画面と得意先(/customers)ページで分かるように。
- **sim（`src/server.js`）**：表の「仕入値上額」「値上額」セルを **値上額＋その下に改定%** の2段表示に（`setAmtRate`/`rateOf` を新設。`updateView` の ci/si を差し替え）。値上げ=赤・値下げ=青の `.ratetag`。ヘッダーに「(改定%)」を併記。改定%は **現単価>0 のときだけ** 算出（0/未設定は—、編集して再計算にも追従）。
- **得意先（`src/customersPage.js`）**：明細表をグループ見出し「仕入（現→改定後）／売価（現→改定後）」に拡張。列＝**現仕入・改定後仕入・改定% ／ 現売価・改定後売価・改定% ／ 実施日**。`pctChg()` を新設（値上げ=赤/値下げ=青）。`/api/customers` の各商品は既に `currentCost/newCost`（buildCustomerCandidates で付与済）を返すのでそのまま使える。
- **検証**：構文OK＋配信JS OK。API実走で商品に currentCost/newCost あり（49→54）。**ヘッドレスEdge実機**：sim 仕入値上額セル＝「7.05」＋赤「+20.0%」、得意先＝S-01会計票 仕入49→54=**+10.2%**・売価70→76.5=**+9.3%**、両ページともコンソールエラー0。⚠ **要 sim.bat 再起動**。

### 🆕 得意先別 商品一覧（/customers）に計算カスタマイズを追加（2026-05-30・動作確認済み）

- **狙い（ユーザ方針）**：今後は **見積書作成は /customers（得意先別）を起点**にし、**メインpage(sim)は「商品の照合確認」用**に役割分担する。その第一歩として、/customers にメインpageと同じ計算カスタマイズを載せた。
- **追加した操作（`src/customersPage.js`）**：ページ上部に「計算カスタマイズ バー」を新設。**転嫁ルール／掛率（markup時のみ表示）／端数 単位／端数 処理／自社コスト上乗せ%／実施日 一括** ＋「この設定で再計算」ボタン。起動時に `settings.json` の既定値を読み込んで初期表示（メインpageと同じ既定）。ルール/端数の変更は即再計算、掛率・自社%・実施日はEnter/フォーカスアウトで再計算。再計算後も選択中の得意先を保持。上部に「適用中: …」サマリ表示。
- **サーバ（`src/server.js`）**：`aggregateCustomers(opts)` を**引数で計算設定を受け取れるよう拡張**（`ruleType/factor/roundingUnit/roundingMode/selfUplift/forceEffectiveDate`。未指定は settings.json 既定にフォールバック＝従来GETと後方互換）。`forceEffectiveDate` 指定時は全行の実施日をその日付に統一。戻り値に `applied`（実際に使った設定）を追加。ルート `/api/customers` を **GET（既定）/POST（設定つき）両対応**に。
- **検証**：① server.js / customersPage.js 構文OK＋配信クライアントJS構文OK ② API実走：add_increase 70→75・粗利維持 70→77.14・掛率1.3 70→70.2 と**ルールで価格が変わる**こと、実施日一括が**全行反映**を確認（得意先86件/商品396）③ **ヘッドレスEdge実機**：6コントロール表示・既定値反映・得意先選択・**掛率1.5/端数1円切上げ/実施日2026-09-01で即再計算（70→83）**・選択保持・コンソールエラー0 をスクショ確認。⚠ **要 sim.bat 再起動**。
- **既知の注意**：**keep_sell（据え置き）を選ぶと全行が「要確認」になり0件表示**になる。理由＝据え置き＝改定単価が現単価と同じ→既存の価格異常検知（「値上げなのに同額/値下げ」を要確認へ）に全行が該当し、aggregateCustomers が要確認行を商品一覧から除外するため。仕様上は正しい（値上げしない見積は通常作らない）が、紛らわしいので将来 keep_sell 時は異常判定を緩める等の手当て余地あり。
- **✅ 本ページからの見積書「出力」＋発行前 確認ゲート＝実装済み（2026-05-30・ステップA）**：ユーザ方針＝**見積書作成は /customers を起点**にし、**要確認（価格異常・低一致）があれば発行を止めて一覧 → 人が確認 → 発行**。
  - **出力ボタン2つ（③両方）**：「📄 全得意先の見積書を作成」＝月次の本番／「📄 選択中の得意先だけ作成」＝訂正の再発行（得意先選択で有効化・ボタンに得意先名表示）。
  - **発行前 確認ゲート**：出力を押すとまず `action:'check'`（書かない）。**要確認が1件でもあれば発行を止めてモーダル**で一覧（得意先・仕入先・商品名・現/改定単価・理由。価格異常=赤系/低一致=黄系で色分け）。「戻る（修正する）」＝閉じてメイン照合画面で修正、「要確認を除外して発行」＝それらを除いて発行（=従来の自動分離を**明示的な確認操作**に変えた）。要確認0件なら確認ダイアログ後そのまま発行。
  - **出力形式**：得意先ごと1枚（複数メーカー横断）の見積書を `output/得意先別_見積書_<日時>/` に。要確認は同フォルダに `要確認(発行から除外).xlsx` として控え。**価格はこの画面の設定で直接計算**（sim→得意先別.bat の2段を1段に統合。merge_quotes.js/得意先別.bat は従来どおり別経路として残置）。
  - **サーバ（`src/server.js`）**：`aggregateCustomers` を共通コア `buildCustomerCandidates(opts)`（keep/review を理由つきで分離して返す）に分解し、表示用 `aggregateCustomers` と出力用 `exportCustomerQuotes(opts,doIssue)` の両方が同じコアを使う＝**画面と提出物が必ず一致**。ルート `POST /api/customers-export`（action: 'check' | 'issue'、scope: 'all' | 'one'）。
  - **検証**：① 構文OK（server/customersPage/配信JS）② API実走：check all＝issuable 86得意先/396品・要確認7件（全て価格異常＝未晒バーガー袋5.3/5.5重複・ホワイティクラフトテープ値下げ等、CLAUDE.md既知例と一致）、issue one＝`見積_ｂｉｂ.xlsx` 生成を確認（テスト出力は後始末で削除）③ **ヘッドレスEdge実機**：全得意先ボタン→ゲート表示（要確認7件・色分け・サマリ「発行86得意先/396品・除外7件」）・コンソールエラー0 をスクショ確認。⚠ **要 sim.bat 再起動**。
- **ステップB（次の一手・未実装）**：確認ゲートの中で**個別に修正して含める**（価格の手入力で訂正して発行に戻す／その場で手動紐付け確定して低一致を解消）。今は「戻ってメイン画面で直す」運用。頻度を見てBに進む。

### 🆕 実施日カレンダー機能を追加（2026-05-30・動作確認済み）／全仕入先横断・当日強調・更新喚起まで実装

- **何を**：sim画面ヘッダーに **「📅 実施日カレンダー」ボタン**を追加。クリックで月カレンダーのモーダルが開き、**全仕入先を横断して「実施日（価格改定の発効日）」を日付ごとに集計**して表示する。価格改定がいつ・どの得意先/商品で発効するか（=見積提出の段取り）が1枚で分かる。
- **どこ（`src/server.js` のみ）**：
  - サーバ：`buildCalendar()`（input/ の**全仕入先 最新CSVを `calcAll` で計算し一致行だけ集約**＝`{date, customer, product, supplier}`。併せて販売実績ファイルと損益.csvの最終更新月も返す `fileFreshness()`）＋ ルート `GET /api/calendar`。**既存の計算ロジックは流用のみで変更なし**。
  - 画面：ヘッダー `#calBtn`、`</header>` 直後に `#calOverlay`/`#calModal`（更新バナー＋当日バナー＋チップ＋月グリッド＋明細）、CSS、クライアントJS（`openCalendar`〔/api/calendar を fetch〕/`renderCalendar`/`renderReminder`/`buildEffIndex`/`parseEffDate`/`excelSerialToYMD`/`renderCalDetail`）。
- **3つの要望に対応**：
  1. **全仕入先 横断**：1枚のカレンダーに5仕入先を集約（明細に「仕入先」列）。実データで266件（6/1=166・4/21=42〔朝日のExcelシリアル46133〕・未設定58）を確認。
  2. **当日が実施日なら赤強調**：本日に実施日がある日のセルを**赤**（`.calcell.has.today`）にし、上部に赤バナー「🔔 本日 M/D が実施日の商品が N件」＋チップに🔔。既定表示月も「本日に実施日があればその月」を優先。
  3. **月替わりの更新喚起**：カレンダー上部に**販売実績ファイル**と**損益.csv**の最終更新を表示。**今月のものでなければ黄色「要更新」バナー**＋「自社データ設定を開く」リンク、今月なら緑「今月更新済」。（実データは両方2026-05＝今月なので緑。来月になると黄色に変わる挙動）。
- **仕様メモ**：休眠/未一致は改定なしで除外。実施日が空/読めない行は「実施日 未設定 N件」チップにまとめ要入力を促す。チップでその月へワンタップ移動、色つきの日クリックでその日の明細（仕入先・得意先・商品名）。◀▶で月送り・「今月」・Escで閉じる。日=赤/土=青。
- **実施日パースは混在対応**：`2026-06-01` / `2026年7月1日` / `7月1日～` / `6/1`（年なしは当年補完）/ Excelシリアル `46133`→2026-04-21。単体テスト10/10。
- **⚠ 実装上の罠（CLAUDE.md既出）を順守**：クライアントJSは `const PAGE = \`...\`` テンプレートリテラル内文字列として配信されるため**正規表現は `\\d` `\\D` `\\s` と二重エスケープ**（単一だと黙って壊れる）。`${`/バッククォートは追加コードに不使用。
- **検証**：① server.js構文OK＋**配信される（エスケープ処理後）クライアントJSの構文チェックOK**（画面全壊バグ型を排除）② `/api/calendar` 実レスポンス確認（5仕入先・266件・ファイル鮮度）③ parseEffDate 単体10/10 ④ **ヘッドレスEdge+CDPで実機描画**：横断表示・更新バナー（緑）・明細の仕入先列・**当日エントリ注入で赤セル/赤バナー/🔔チップ**・コンソールエラー0 をスクショ付きで確認。⚠ **反映には sim.bat 再起動が必須**。
- **既知の残課題/補足**：朝日の実施日が **Excelシリアル46133＝2026-04-21（過去日）** のまま生で載るため、カレンダーでは4月に42件出る（提出時は forceEffectiveDate 等で 6-01 に寄せている別処理がある）。根治は「取り込み時の日本語/シリアル日付→ISO正規化」（既存の未修正課題）。さらに絞り込み（得意先フィルタ等）が要れば拡張可。

### 📌 次回いちばん最初にやること（2026-05-29 夜 自宅PCへ移動・引き継ぎ）

- **使い方手順書に「対象CSVの消し方（手動）」を1項目追加する**（ユーザ依頼・今回は未着手）。対象＝`使い方手順書.md` と `使い方手順書.html` の両方（HTMLはsim画面の「📖 使い方」ボタンが配信）。内容：input/ フォルダを開き、`<仕入先>_照合結果_<日時>.csv` を削除 or `input/_old` へ移動（戻せる）→ sim再起動で一覧から消える。出力済み見積書(output/)は別物で消えない。※ユーザは「画面ボタンは不要・手動でよい」と選択済み（削除ボタンは作らない方針）。
- **PC移動の注意**：会社PC→自宅PC。**Driveの同期完了を待ってから**自宅PCで開く（右下アイコン「最新」）。2台同時編集はしない。現在の状態はすべて保存済み（CLAUDE.md・src・使い方手順書）。
- ⚠ コード変更を反映するには **sim.bat の再起動が必須**。テスト中に起動した残留サーバは停止済み。

### 🟢 最新状態サマリ（明日はここから読む・2026-05-29 昼 更新）

- **✅ 得意先ごと統合見積書＝完成（2026-05-29 昼）**：得意先からは「メーカーが誰か」は関係なく、自分が買う全商品の改定が1枚で欲しい。そこで `src/merge_quotes.js`（新規）＋`得意先別.bat`（新規）を追加。**sim で出した仕入先別見積書を読み、得意先ごとに1枚へ統合**（複数メーカーにまたがる得意先も1枚に集約）。**価格は再計算せず既存xlsxの単価をそのままコピー**＝仕入先別の承認済み価格と1円もズレない（エフピコ全25社で完全一致を実証済み）。**最新成果物＝`output/得意先別_見積書_20260529_1322/`**（**87得意先・1枚ずつ・明細342行**、複数仕入先=46社、島田は4仕入先30品が漏れなく1枚に）。動かし方＝`得意先別.bat` ダブルクリック（最新の仕入先別セットを自動選択→統合→フォルダを開く）。
- **✅ 統合版の実施日を正常化（2026-05-29 午後）**：`_1158` の実施日に**問題63件**（空欄47・2027年14・過去日2026-04が2）があった。原因＝メーカー見積取り込み時の**日本語日付→ISO年推定バグ**（「4月21日」のように今年過ぎた月日を「次に来る4/21」＝2027年と誤推定）。`merge_quotes.js` に `normalizeEffectiveDate`（不正な日付＝2026-06-01〜12-31の範囲外/空欄を既定日 `2026-06-01` に置換、正常な06-01/06-21/07-01は保持）を追加。結果：実施日は **06-01・06-21・07-01 の3種のみ**、空欄/2027/過去日=0、**価格ドリフト0件**。⚠ **根本原因（取り込みの年推定）は未修正**＝`makerXlsx.js`/日本語日付変換で「過ぎた月日でも当年を優先」等に直すのが本筋（次回の課題）。仕入先別 `_1158` フォルダ自体には元の日付が残る（統合の元データ）。
- **✅ 価格の異常検知を sim・見積書出力・統合の全段で共通化（2026-05-29 夕方・最新成果物）**：ユーザ方針「メーカー側の見積が間違うこともある＝**エラー（要確認）で知らせて人が確認する流れ**にしたい」を全段に展開。**共通モジュール `src/anomaly.js`**（`priceRowAnomaly`＝①売単価が無い/0、②値上げなのに改定単価が下がる/同じ、を判定）を新設し、`merge_quotes.js`・`server.js`(calcAll/exportQuotes) が共用。
  - **calcAll（sim画面）**：各行に `priceWarning`＋`costConflict` を付与。**同一メーカー品（品番）で新仕入が複数あるメーカー重複**も検知（例: 大黒テープ品番2061740が1280/1450）。⚠ ただし「1自社商品に複数の別メーカー品が名前一致した取り合い（白↔ピンク等）」は別品番なので**拾わない**（dedup/色ガードが解決する別現象＝過検知しない）。画面では**改定単価セルが赤くなり**ツールチップで理由、表の上に赤バナーで件数表示。`recalc` でnewCost編集にも追従。
  - **exportQuotes（見積書出力）**：価格異常・メーカー重複の行を**見積書から外して要確認へ**（CD一致でも外す）。要確認ファイル名を `要確認(低一致・価格異常).xlsx` に。`src/xlsx.js` の出力列に**「要確認の理由」列を追加**（なぜ要確認かが分かる）。
  - **merge_quotes（統合）**：各仕入先の要確認xlsxを統合フォルダにも `_要確認_<仕入先_照合結果_日時>.xlsx` として**集約コピー**（同一仕入先の複数照合結果でも上書きしないよう一意名）。
  - **最新・正の成果物＝`output/得意先別_見積書_20260529_1824/`（86得意先・336明細・価格異常の残り0件・要確認xlsx5ファイル同梱）**。仕入先別は `_1822` セット。検証：自社消費分(売単価0)・大黒テープ(値下げ&1280/1450重複)・未晒バーガー袋(5.3/5.5重複)を全段で分離、見積書本体への漏れ0、価格ドリフト0、実施日は06-01/06-21/07-01の3種のみ。**提出はこの `_1824` を使う**。
- **✅ 警告表示のブラウザ実機確認＝合格（2026-05-29 夕方）**：headless Chrome+CDPで sim を実走検証。🔴赤セル(価格異常・背景#fdecea)・🟡黄セル(値上率注意・背景#fff4d6・▲48%等)・赤/黄バナーが正しく描画、**スクリプトエラー0**（前回のような画面全壊なし）。インラインJSの正規表現は今回追加分では未使用＝二重エスケープ問題も無し。
- **✅ input/ の重複照合と「結果が変わった」件＝原因究明し解決（2026-05-29 夕方）**：実機確認中、本日17:13〜14に照合が複数回実行され input/ に `_20260529_171x` の照合結果が大量に出来ていたと判明。simは「各仕入先最新1本」を拾うため `_1714` を表示し提出物 `_1824`(=_2347ベース)とズレる恐れがあった。**内容突合**：`_2347`(一致374)と`_1714`(一致373)の差は1行だけ（壱乃藏 FMフレックスストロー乳白）。**根本原因＝2回の照合の間に手動紐付け(productLink)が1件追加されたため**（`大黒工業 / 自社CD 007750 → ＦＭフレックスストロー乳白`）。この紐付けが「乳白」を確定し、別柄「ストライプ」への87%ノイズ名前一致を取り合い防止で除外した＝**正しい動作**。つまり **`_1714`の方が正しい**（私の当初「_1714が劣化」判断は誤り）。**バグでも非決定性でもなく、照合は決定的**（同じ入力＋同じ設定→同じ結果）。**重要：提出物 `_1824` は紐付けを出力時に適用済み**（exportQuotes→calcAll が現在のproductLinksを反映）＝ `_2347`(紐付け前)入力から作っても全5件の紐付けが正しく反映され壱乃藏も乳白670→780.3で正しい。検証で5件全てOK。整理として `_1714`系21ファイルを `input/_old/` へ退避し input/ を `_2347` の7ファイルに一本化（sim=提出物に統一。どちらの入力でも出力時に紐付け適用で同結果）。**結論：照合・提出物ともに信頼できる。再現性の問題なし。**
- **✅ 決定性を実データで実証＋再調査で二重確認（2026-05-29 夜）**：照合（shogo＝販売実績.XLSをExcel変換→全maker_quotes照合）を回し直し検証。
  - **① 照合の決定性**：本日の全照合バッチ（17:13・17:14・19:50＝計3回以上）が**全7ファイル・全行バイト一致**（差異0）。「同じ入力＋同じ設定→同じ出力」を実証。※17:13-14に複数回走ったのは利用者が照合.batか sim「↻照合」を数回実行したため（無害）。
  - **② 入力→提出物まで通しの再現性**：クリーンな post-link run(_1950) から見積書を作り直すと、提出物 `_1824` を**完全再現**（336/336明細・過不足0・価格差異0）。通しで決定的かつ `_1824` は正しいと確認。
  - 検証用の新run・テスト出力は全て退避/削除し、input/ は `_2347` 7本、output/ は `_1822`(仕入先別)＋`_1824`(提出物) のみに整理済み。
- **✅ sim に「値上率が大きい行」の黄色注意表示を追加（2026-05-29 夕方）**：ユーザ要望「上げ幅は正しいが注意喚起してほしい」。`calcAll` が値上率 **≥30%**(`RATE_CAUTION_PCT`、server.js冒頭・調整可)の行に `rateWarning` を付与。sim では改定単価セルを**黄色(▲)**＝注意のみ／赤(⚠＝価格異常で除外)とは別、表上に黄色バナーで件数。**除外はしない＝見積書には通常どおり載る**（赤＝除外とは扱いが違う）。実データでは六ツ折ナプキン等18行(31〜52%、仕入0.38→0.58の実値上げ＝正当)が黄色に。赤と黄が同時に付く行は無し（赤優先）。**全社の照合監査も実施**：色違い誤マッチ＝0（CD一致のみ）、極端値上率はナプキンの正当な転嫁のみ、得意先間価格差は現売単価差由来＝正常。提出物 `_1824` に実害なしを確認。
- **✅ この昼に直した既存バグ2件（`src/server.js`）**：
  1. **`parseSelfName` の未閉じ運賃カッコ取りこぼし**：`（白）`等の短い注記カッコが大黒の未閉じ運賃カッコより前にあると整形が止まり、商品名に運賃・コード・※が残っていた（大黒10行）。→ **全カッコを走査して未閉じ運賃カッコを検出**＋`（700円）`型も運賃扱い、に修正。商品名内の `ｹｰｽ`/`P8` 等に誤反応しない安全方式。回帰15ケース合格・色名 `（白）`等は品名として保持。
  2. **`exportQuotes` の実施日が生のExcelシリアル**：朝日42行が `46133` のまま表示されていた。→ **Excelシリアル→ISO日付変換**を追加。併せて `forceEffectiveDate` オプションを追加し、朝日は全103行を `2026-06-01` に統一（混在していた `46133`＝過去日 2026-04-21 を一掃）。**価格には無影響**（表示のみ）。
- **✅ `_0731`(仕入先別バグ版) を退避済み（2026-05-29 午後）**：朝 `_0731` 6フォルダ（実施日シリアル/大黒名バグあり）は `output/_old/` へ移動（削除でなく退避＝戻せる）。`output/` には修正版 `_1158`(仕入先別) と 最新統合 `_1322` だけが残る。
- **✅ 実機確認済み（2026-05-29 午後）**：統合 `_1322` を Explorer で開き、4仕入先にまたがる島田を検証＝30品・汚い名前0・生シリアル0・全品クリーン。
- **次の一手（候補）**：①`output/得意先別_見積書_20260529_1824/` をExcelで最終目視して提出。同梱の `_要確認_*.xlsx`（自社消費分・大黒テープ・バーガー袋の計6行）は理由列を見て、正しい単価が分かれば手動で見積書に追加（平川テープは大黒見積1280/1450の食い違い＝切替日が後の1450＝→売単価2040が妥当だが要メーカー確認）。②output直下の残骸 `大黒_…_見積書_20260529_1711`（中途半端な大黒735のみ。Excelで開いていてロック中だったので未削除）をExcelを閉じてから削除。③`_old/`（退避旧データ）を本当に削除。④**取り込みの日本語日付→ISO年推定バグの根本修正**（`makerXlsx.js`／日付変換。過ぎた月日でも当年優先に）。

### 🟢 旧サマリ（2026-05-29 朝 自宅PC 更新）

- **✅ 見積書の再出力＝完了（2026-05-29 朝）**：正しい `_2347` セット6ファイルから全仕入先の見積書を出し直した → `output/*_見積書_20260529_0731/`（エフピコ25社／中央化学33社／大黒122品46社・39品30社／朝日122品25社・29品21社、**要確認は全社0件**、福助は全休眠でスキップ）。UIと同じ既定（add_increase factor1.25／端数0.01 round／自社上乗せ2%）で出力。`src/server.js` の `exportQuotes` を require して直接実行（サーバ/ブラウザ不要）。
- **⚠ 重要な落とし穴＝`input/` の `_2350/2351` は使うな**：mtime は `_2347`(23:47) より新しい（23:50〜51）が、**中身は劣る**（CD境界修正"前"＝中央化学CD一致18・見積可85・低信頼名前一致39件のノイズ、朝日は29品が欠落して1本だけ）。`_2347` が CLAUDE.md 記録の検証値（中央化学CD一致25・見積可104、朝日2本そろい）と完全一致＝**正しいのは `_2347`**。**sim.bat の「対象」ドロップダウンは mtime 新しい順で `_2350` を拾い、大黒/朝日の2本目も出せない**ので、sim で出し直すなら先に `_2350/2351`(計6本) を input/ から退避すること。
- **本日の作業＝照合エンジンの大幅強化＋sim画面の致命バグ修正**（すべて実データで検証済み）。詳細は下の各「最新」ブロック参照。
- **変更ファイル**：
  - `src/match.js` … 価格で判定（フロア超は+20%加点／80%未満で価格不一致なら除外VETO）・改定マーク「改」無視・**CD一致のコード境界修正**・**英↔カナのブランド救済**・**赤字(売値<仕入値)ガード**・色の英字↔カナ正準化
  - `src/hanbai.js` … CD照合用 `codeNorm`（区切りを残す正規化）
  - `src/shogo.js` … 出力ファイル名の上書きバグ修正（`uniqueOutPath`）
  - `src/server.js` … `matchPctCell` の正規表現エスケープ漏れ＝sim画面全壊バグ修正（要・sim再起動）
- **最新の正しい照合結果＝`input/*_20260528_2347*`**（7ファイル・中間版は削除済み）。見積可(≥80)は全仕入先で保持〜増加、CD一致・手動は全保持、赤字/誤爆/サイズ違いの誤マッチ＝0。
- **✅ 古いデータ退避＝完了（2026-05-29 朝）**：`input/` は `_2347` の7ファイルだけ残し他57件を `input/_old/` へ、`output/` は本日の6フォルダだけ残し旧18フォルダ（オリカ含む・〜2026-05-28）を `output/_old/` へ移動（削除でなく退避＝元に戻せる）。これで sim を開いても `_2347` が選ばれる。`_old/` は `.csv` で終わらない/ディレクトリなので `listLatestCsv`・`/api/maker-list` のスキャン対象外（無害）。
- **次の一手（候補）**：①出力した見積書のブラウザ実機確認。②不要なら `_old/` を本当に削除（今は退避のみ）。③sim の対象ドロップダウンは「各仕入先 最新1本」なので大黒/朝日の **122品(=234733/234734)** はUIに出ない（2本目だけ表示）。再出力は本セッションで全6本済み。UIから122品も選びたいなら listLatestCsv を複数本表示に直す案あり。
- 動かし方：`Claude起動.bat`→Claude / `sim.bat`→シミュレーション(自動でlocalhost:8766前後を開く)。**コード変更後は sim 再起動必須**。「Failed to fetch/データ出ない」は旧サーバ残留かサーバ未起動が原因。
- ※ Claudeのローカルメモリは両PCで同期されない。引継ぎは**この CLAUDE.md** と `_chatlogs/`（Drive同期）。

### 📍 照合エンジン：価格で判定（タレビン サイズ違い対策）（2026-05-28・最新）

**ユーザ指摘**：中央化学タレビンで「角小↔角特大」等のサイズ違いが67%で誤マッチ。「弊社現単価(=原単価)とメーカー改定前単価で紐付けできないか／優先順位は？」

**原因（実データで特定）**：
- メーカーは「ﾀﾚ壜 角小(D) **改**」のように改定マーク「**改**」を付ける。自社「ﾀﾚﾋﾞﾝ 角小」には無い→"改"が決定的トークン扱いで50%抑制→**休眠（正解の取りこぼし）**。角"大"だけ改が無く100%で当たっていた。
- 一方メーカー「角 特大」が自社「角**小**」に67%で当たる：`大/中/小/特`の単漢字は決定的だが**2文字の「特大」が素通り**し、共通の「角」で2/3=67%。
- 価格は当時「加点」のみで、誤マッチを**消す/正す力が無かった**。

**修正（`src/match.js` ＋ `src/shogo.js`）= 価格を判定に格上げ**：
1. **価格VETO**：`matchOne` に `priceVetoBelow`(=`shogo`が `matchThreshold`=80 を渡す)。自社原単価≒メーカー改定前単価が**不一致(±2%超, 両方>0)**で、名前一致が80%未満なら**休眠へ除外**（見積書に乗らない要確認ノイズだけ掃除）。→ 角小↔角特大(13.34 vs 約4)等を一掃。
2. **「改」を NOISE_TOKENS で無視**（`tokenize`）：改定マーク等の単独語は商品差ではない→除外。→ ﾀﾚ壜角小改↔角小 が**100%**で正マッチ。
3. **価格救済(フロア未満のuncap)は採用しない**：同シリーズ別サイズが**同価格**のことがある（実例：フルーツキャップ **WSR-110↔WSR-90** が価格一致で誤救済された）。価格は「除外」と「フロア超の加点」だけに使い、フロア未満は救わない（誤マッチ防止）。フロア未満の真の別名は手動紐付けで救う運用。

**検証（照合.bat=shogo 実走、新旧比較・実データ）**：
- 見積可(≥80%=CD+名前80+手動)は**全仕入先で保持 or 増加**：エフピコ62→62 / 中央化学85→**99(+14)** / 大黒94品78→78 / 朝日122品62→**62(完全同値)** / 朝日29品42→42。**CD一致・手動紐付けは全件保持・取りこぼしゼロ**。
- 80%未満ノイズ激減：中央化学27→**0** / 大黒94品50→**7** / 朝日29品18→**0**（価格不一致のサイズ違いを除外）。
- タレビン：角小/角中=休眠→**100%復活**、角特大誤爆67%は消滅、角大/角特大/角中太口/豚太口小/一合壜も正。WSR-110/90 は別商品として分離。
- ⚠ **要確認リストが縮む副作用**：価格不一致で自動除外するので「別物」が要確認から消える（中央化学 要確認13→0等）。基本は望ましいが、稀に「同一商品だが原単価データがズレている」ケースも一緒に消え得る。気になれば veto を名前<70 に緩める等で調整可。

**この比較中に見つけた別バグ（未修正・要対応）**：`shogo.js:stamp()` が秒単位なので、**1仕入先に複数のメーカー見積があり同じ秒に出力すると2本目が1本目を上書き**する（朝日122品が29品で消えた事例あり）。出力ファイル名にミリ秒/連番を足すか、出力前に存在チェックして退避させる修正が要る。

### 📍 続：CD一致（コード照合）の取りこぼしバグ修正（2026-05-28・最新）

**ユーザ指摘**：「メーカーコードと自社登録のメーカーコードでの照合も有効。これも大丈夫か？」

**原因（実データで特定）**：`hanbai.js` の `r.norm = normName(body)` が**空白を削除**するため、自社品名に埋め込まれたメーカー品番が直後の数量と連結（例「… 170078 50【…」→「17007850…」）。`codeHit` の単語境界判定が「後ろが数字」で落ち、**コードが入っているのに CD一致が発火しない**。記号（※等）が後ろの品だけ偶然成功していた（大黒はコードが数量の前にあり CD一致たった2件）。今は名前一致が拾うので実害は出ていなかったが、最強の手がかり（100%信頼）を捨て、名前違いの難ケースは取りこぼす状態だった。

**修正（`hanbai.js` ＋ `match.js`）**：CD照合専用の正規化 `normForCode`（NFKC＋小文字化のみ・**区切り空白は1個残す**）を追加し、レコードに `codeNorm` として保持。`matchOne` は `codeHit(cands, r.codeNorm||r.norm)` を使う。→「コード 数量」は空白境界で正しく一致、「大きい数字に埋もれたコード」は従来通り弾く。純数字は5桁以上のまま（4桁=2700等は誤爆回避で対象外。ユーザ選択）。

**検証（再照合・実データ）**：
- CD一致 激増：エフピコ12→**18** / 中央化学18→**25** / 大黒94品**2→85** / 大黒39品→**34** / 朝日122品13→**19**。**5桁以上の取りこぼし残=0・誤爆CD一致=0**。
- 見積可(≥80) 元基準→最終：エフピコ62→62 / 中央化学85→**102** / 大黒94品78→**105** / 朝日122品62→62 / 朝日29品42→42。**全仕入先で保持 or 増加、CD一致・手動全保持**。
- 大黒の休眠51→37：名前違いだがコードありの**真の取りこぼしが復活**（CD一致で救済）。

**現在の照合の優先順位（最新・確定）**：① 📌手動紐付け ＞ ② ✓CD一致（メーカー品番が自社品名に埋め込み・**境界保持で照合**）＞ ③ ✓名前一致（トークン重なり％・改定マーク「改」は無視・サイズ/部位/型番違いは50%抑制）。**価格**＝自社原単価≒メーカー改定前単価で、フロア超の名前一致に+20%加点／80%未満で価格不一致なら除外（VETO）。CD一致・手動には価格を使わない。

### 📍 続：赤字ガード＋英↔カナ ブランド救済＋出力上書きバグ修正（2026-05-28・最新）

**ユーザ指摘**：「赤字販売（売値＜仕入値）はほぼあり得ない＝誤マッチの証拠」。例：自社007205(手袋)にメーカー6442740(ギャザーキャップ黒)が誤マッチしていた。正解は自社005302(ギャザーキャップ ブラック)。

**調査結果**：
- 手袋↔キャップの赤字誤マッチは**先の価格VETO等で既に解消済み**（6442740は休眠化）。全仕入先で「売値<仕入値」の一致は**現状ゼロ**。
- だが正解の005302が逆に**取りこぼし**になっていた。原因＝① ブランドが「Ｆｒｅｓｈ＋(英字)」↔「フレッシュプラス(カナ)」で不一致、その英字語が決定的トークン扱いで50%抑制→休眠。② 色ガードが「黒(漢字)」と「ブラック(カナ)」を別色と誤判定し救済をブロック。

**修正（3点・すべて `src/match.js`、赤字ガードは `matchOne`）**：
1. **色の英字↔カナ正準化**（`COLOR_CANON`/`canonColor`）：黒=ブラック, 白=ホワイト 等を同一色として `colorMarkersIn` が返す。色違い誤判定を解消。
2. **英字ブランド救済**：`nameScoreInfo` が抑制原因の決定的語(`missedCrit`)を返す。抑制原因が**英字を含み数字を含まない語だけ**（＝英↔カナのブランド表記差の可能性）で、かつ**価格一致**なら抑制解除して救済（生スコア+20%）。**数字を含むサイズ/型番違い（WSR-110↔WSR-90）は対象外**＝誤救済しない。
3. **赤字ガード**：売値 < メーカー改定前単価（両方>0）かつ名前一致が80%未満なら休眠へ除外。原単価が無く価格VETOが効かない行でも効く独立保険（CD一致/手動は対象外）。

**検証（再照合・実データ）**：
- 005302↔6442740 を **87%+価格で正しく回復**。赤字販売の一致=**0**・誤爆CD一致=**0**・WSRサイズ混在=**0**。
- 見積可(≥80) 元基準→最終：エフピコ62→62 / 中央化学85→**104** / 大黒94品78→**105** / 朝日122品62→62 / 朝日29品42→42。CD一致・手動 全保持、取りこぼし復活。

**🐛 出力上書きバグも修正（`shogo.js`）**：1仕入先に複数のメーカー見積があると同じ秒に出力して2本目が1本目を上書きする問題（朝日122品が29品で消える事例が再発）。`uniqueOutPath` で既存/同一run名と衝突したら1秒進める（HHMMSS6桁＝`listLatestCsv` の `\d{4,6}` 維持）。再照合で**7ファイル全て生成を確認**（朝日122品・29品とも残る）。

**現在の照合の優先順位（最新・確定）**：① 📌手動紐付け ＞ ② ✓CD一致（メーカー品番が自社品名に埋め込み・境界保持で照合）＞ ③ ✓名前一致（語の重なり％・「改」無視・サイズ/部位/型番違いは50%抑制）。**価格**＝原単価≒改定前単価で「フロア超に+20%加点／80%未満で価格不一致なら除外」。**英字ブランド語だけが不一致＋価格一致なら救済**（英↔カナ）。**赤字（売値<仕入値）×弱い一致は除外**。CD一致・手動には価格/赤字を使わない。

**やり残し**：**見積書 再出力は未実施**（output/ の旧見積書は旧マッチ＝破棄して再出力推奨）。**最新の正しい照合結果は `input/*_20260528_2347*`**（中間版 `_2251`〜`_2342` は削除済み。`_1455` 等の旧版や古い蓄積CSVの整理はユーザ承認待ち）。

### 📍 ブラウザ実機確認セッション（2026-05-28・最新）

**「一致度」カラムと紐付けモーダルをブラウザで実機確認 → 画面全壊バグを発見・修正（最重要）。**

- 🐛 **致命バグ修正（`src/server.js:1123, 1126`）**：`matchPctCell` の正規表現が単一バックスラッシュで書かれていた（`/\+価格/`・`/(\d+)\s*%/`）。クライアントJSは server.js 内の**文字列**として配信されるため、`\+`→`+`、`\d`→`d` に潰れてしまう。結果 `/+価格/` が**無効正規表現→SyntaxErrorでインラインscript全体が停止→sim画面がブラウザで全壊**（`loadFiles`等が未定義・ファイル選択も表も出ない状態）。「ブラウザ実機確認は未実施」とメモされていた通り、今セッション中ずっと壊れていた。
  - 修正：`/\\+価格/`・`/(\\d+)\\s*%/` と**二重エスケープ**に統一（同ファイル1260/1276行の規約に合わせた）。
  - ⚠ **教訓：このクライアントJSの正規表現は必ず `\\d` `\\s` `\\+` と二重に書く**（単一だと黙って壊れる or SyntaxErrorで全壊）。今後 server.js のインラインJSに正規表現を足すとき要注意。
- ✅ **実機確認の結果（ヘッドレスChromeをCDPで操作して検証・スクショ取得）**：
  - 一致度カラムは「得意先(列0)と商品名(列2)の間＝列1」に正しく配置。
  - 中央化学129行の色分布＝緑85／橙(60-79%)27（例「67% 名前一致」）／休眠17／`—`フォールバック0。`+価格一致`の緑併記もOK。赤(<60%)はfloor60で休眠化されるため到達不能＝0件（仕様どおり）。
  - 紐付けモーダル：候補64件が類似度降順、検索「蓋」で64→20件に即絞り込み、決定的トークン警告（自社「身」↔メーカー「蓋」）が発火。すべて正常。
  - **保存(保存ボタン)は未検証**：`/api/product-link` が実 `settings.json` に書き込むため、誤ペアで実データを汚さないよう意図的に未実行（API往復は前セッションで単体確認済み）。
- 📝 メモ：ブラウザ確認には `chrome.exe --headless=new --remote-debugging-port=9222 --remote-allow-origins=*`（`--remote-allow-origins=*` が無いとCDP接続できない）＋ Node24 の global `WebSocket`/`fetch` でゼロ依存のCDPドライバを使った。
- ⚠ 旧セッションのサーバが 8765 に残留していることがある（PID別）。`sim.bat` 起動後に「Failed to fetch/データ出ない」時はポート確認を。

### 📍 現在地（2026-05-28 夜・会社PC→自宅PCへ移動）

**この日の夜セッションで実装（動作確認済み）：**

1. **シミュレーション画面に「一致度」カラムを追加（`src/server.js`）**
   - 背景：自社商品×仕入商品の照合の％（`✓ 名前一致(80%)`等）が商品名セルの2行目に小さく灰色で埋もれて分かりにくい、というユーザ要望。
   - 対応：表の **「得意先」と「商品名」の間** に独立した「一致度」カラムを新設。`matchStatus` 文字列から％を取り出して大きく色分け表示（80%以上=緑/60-79%=橙/未満=赤、CD一致・📌手動紐付けは100%緑、休眠は灰「休眠」）。`+価格` タグも併記。
   - ヘルパー `matchPctCell(r)` を追加。商品名セルからは重複する `status` テキストを除去（休眠行のラベルは残す）。列追加に伴うCSS/colspan依存は無し（確認済）。**ブラウザ実機確認＝2026-05-28に完了（下記「ブラウザ実機確認セッション」参照）。確認の過程で `matchPctCell` の正規表現エスケープ漏れ（=画面全壊バグ）を発見・修正済み。**

2. **チャットの自動バックアップを新設（`src/backup_chat.js` ＋ `.claude/settings.json`）**
   - 背景：API 400エラー（thinkingブロック）で画面を閉じた件＋PC間でClaudeの会話が同期されない件への対応。「会話を自動バックアップしたい」というユーザ要望。
   - 仕組み：**Stopフック**（Claudeが応答し終わるたび）で `src/backup_chat.js` を実行。Claude Codeが渡す transcript(.jsonl) を Drive内 `_chatlogs/` に **① 元ログ(.jsonl)コピー ＋ ② 人が読める整形版(.md)** の両方で保存。ファイル名は `<YYYYMMDD>_<session先頭8桁>`、1セッション1ファイルで毎回上書き。Drive経由で両PCに同期される。
   - フックは `.claude/settings.json`（プロジェクト限定・相対パス `node src/backup_chat.js`）。**初回起動時にClaude Codeがフック承認を求める場合あり→許可すれば有効**。
   - 既に今日のクラッシュしたセッション(7a76f539・331発言)も `_chatlogs/20260528_7a76f539.md` に整形保存済み。**今日の日中の作業内容を知りたい場合はこの.mdを読めば復元できる**。

**2026-05-28 日中セッション（7a76f539・クラッシュ）の作業＝以下に追記（コードで検証済み）：**

A. **🎯 価格一致ブースト（`src/match.js`・最重要）**
   - 背景：ユーザ指摘「自社の現単価とメーカーの現単価は通常一致する。照合に使ってほしい」。CLAUDE.md旧記述「原単価は0が多く信頼薄」は**古い**＝実データの約94%(1559/1661)で原単価>0と判明。
   - 実装：`pricesEqual(a,b,tol)`（自社`origCost`≒メーカー`currentCost`、許容±2%=`priceTolerance`、両方0超でなければnull）。`matchOne` に `priceBoost=20`/`priceTolerance=0.02`。**名前一致(フロア60以上)のみ**対象で価格一致時に名前スコア+20%(上限100)。CD一致・📌手動紐付け・フロア未満は対象外。価格不一致/片方未設定でも**減点なし**（後方互換）。
   - **色違いガード**：`colorMismatch()`+`colorMarkersIn()`+`COLOR_TOKENS`/`COLOR_KANJI_RE`/`COLOR_KANA_RE`。両側に色があり共有0なら色違い→ブースト無効。経緯：実データで「フルーツキャップ WSR-90 ピンク↔【白】」が価格一致で誤救済された。文字列全体を走査（`【白】`がトークン化で拾えない問題を回避）。単一漢字色は前後が漢字でない時、カナ色は前後が同種カナでない時のみ採用（複合語の誤検出回避）。
   - status文字列に `+価格` タグ付与（例 `✓ 名前一致(87%)+価格`）。色違いガードで弾いた時はタグを立てない。
   - 効果（チャット報告値）：朝日2件・大黒工業1件(ニシキ丸皿 赤石目ゴールド)が80%以上へ昇格、誤救済0、約145件に+価格タグ。
   - ⚠ **回帰未確定**：`_validate.js` 用ベースライン(オリカ/朝日の正解CSV)が `input/` から消失しており正式回帰は未実施。priceBoost=0/20 の差分比較で代替確認済み。**ベースラインCSVの復元が望ましい**。

B. **`src/server.js`（複数・保存済み）**
   - 見積書出力サマリ `#exportSummary`：`exportQuotes` 返値に supplier/customerCount/perCustomer[]/reviewSummary[] を追加し、緑枠カードで「見積N件/重複整理K件/要確認M件」＋得意先別チップ＋要確認プレビュー上位5件＋フォルダ/要確認.xlsxを開くボタンを表示。
   - `parseSelfName` を**未閉じ運賃カッコ**に対応（大黒の `100（大黒P1.5元未満800P別途…` のように `（` が閉じない形式。運賃キーワードを含めば末尾まで除去）。
   - 見積書の商品名を `productNameCore` 優先に（入数/運賃/メーカー品番/ロット/仕入先コードを除去）。注記`(M25)(D)(ﾎﾞ)`と**色名【白】【ｲｴﾛｰ】等は品名として残す**。
   - 表に常時表示の同期横スクロールバー（min-width 1500px・見出し固定）。「右が見えない」要望対応。
   - 対象ドロップダウンを**各仕入先の最新1本のみ**に（`listLatestCsv`、39本→5本）。古いCSVは削除せず残す。
   - `matchPctCell` に「+価格一致」緑文字表示を追加。

C. **`src/listPage.js`**：/list 下部に「📌 確定済み 商品紐付け一覧」（仕入先別に自社CD/メーカー商品名/解除ボタン。`/api/product-links`）。

D. **ユーザの方針判断（今後も守る）**：
   - 色名は品名 → 見積書でも残す＋照合で色違いは別商品扱い。
   - 見積書の商品名はコア商品名のみ。注記カッコは残す。
   - 中央化学の要確認13件（角中↔角中太口、角小↔角特大）は別物→**除外のまま正解。触らない**。
   - **コード変更後は sim.bat 再起動必須**（Nodeは起動時1回しか読まない）。「Failed to fetch/データ出ない」はほぼサーバ未起動 or 古いサーバ(8765-8768)残留が原因。

E. **やり残し**：最後の要望「得意先と商品名の間に照合%欄」はAPIエラーで中断（列自体は前夜から存在、位置/スタイル調整中だった）。価格ブースト反映後の**見積書再出力は未実施**。古いCSV34本の削除はユーザ承認待ち（保留）。

---

### 📍 現在地（2026-05-27 夜・会社PCで作業）

**この日に実機ウォークスルー→改善実装（すべて動作確認済み）：**

1. **CLAUDE.mdタスク1完了：3仕入先の見積書を最新CSV(_20260527_131825系)で出力**
   - 中央化学：27社分（要確認16件・dup除去37件）→ `output/中央化学_照合結果_20260527_131825_見積書_*`
   - エフピコ：25社分（要確認1件・dup除去3件）→ `output/エフピコ_照合結果_20260527_131824_見積書_*`
   - 大黒工業：35社分（要確認7件・dup除去54件）→ `output/大黒工業_照合結果_20260527_131831_見積書_*`
   - 福助：CSV内 1行のみで全休眠 → 見積書化対象なし（後述#5で友好的表示に）

2. **🐛 productLinks 誤登録を発見・削除**：
   - 観察：`settings.json:productLinks.エフピコ` に `007485 → "T-AP-F20-14 内嵌合蓋 ｴｺ"` が登録されていた。
   - **これは誤り**：007485 は `AP-F15-11 本体 ｴｺ`(本体)。 紐付け先の `T-AP-F20-14 内嵌合蓋` は **サイズ違い・蓋(本体ではない)**。
   - 原因：紐付けモーダルが「全メーカー商品をフラットなselectで並べるだけ」だったため、誤クリックを誘発。
   - 対応：007485 のリンクを削除（API: `POST /api/product-link makerName=''`）。007419(正しい紐付け)は残存。
   - 根本対処として `#1 紐付けモーダルUX改善` を実装（後述）。

3. **#2+#3+#4: /list ページの集約・状態判定・再照合バッジ（`src/server.js:listMakerQuotes`, `src/listPage.js`）**
   - **症状**：朝日食品容器が paste-import CSV 2本のせいで2行に表示。古い見積書フォルダがあるだけで「🟢 見積書作成済み」と誤判定（最新照合に対する見積書が無いのに）。
   - **修正**：
     - 仕入先キーで集約。同じ仕入先の複数ソースCSVは `sources[]` にまとめ、行の下に「＋ N件の取込CSV」と表示
     - 「最新照合に対する見積書があるか」で 🟢/🟡 を判定（古い見積書だけ残っているケースは正しく 🟡 に）
     - `needsRematch` フラグ：取込CSV mtime > 最新照合 mtime + 60秒 のとき「⚠ 再照合が必要」バッジを表示。60秒の余裕は xlsx 自動展開→直後の照合で mtime がほぼ同時刻になる現象を吸収。
   - **検証**：5仕入先（朝日含む）が正しく1行ずつ表示、ステータス＝中央化学/エフピコ/大黒工業が🟢・福助/朝日が🟡、`needsRematch:False` がすべての行で正しく抑制。

4. **#5: 空マッチCSVの友好的ハンドリング（`src/server.js:exportQuotes`）**
   - 症状：福助のような「未一致のみのCSV」に /api/export すると `throw new Error('提出先のある明細が...')` で 500。
   - 修正：`{ ok:false, reason:'no_matched', count:0, totalRows:N, message:'見積書化できる行がありません（CSV内 N 行すべて...）' }` を 200 で返す。Explorer は開かない。フロントは 黄色「⚠ ...」表示（赤エラーと区別）。
   - 成功パスにも `ok:true` を追加。フロントの分岐: `res.error`→赤 / `res.ok===false`→黄 / それ以外→緑(成功)。

5. **#1: 紐付けモーダルUX 大幅改善（`src/server.js:openLinkModal` 周辺）**
   - **追加機能**：
     - 候補を **自社商品名(coreName)との類似度(token Jaccard)順** に並べ替え（最有力候補が一番上）。`✓ [95%]` のような表示で一目で分かる。
     - **検索ボックス**（部分一致でリアルタイム絞り込み）
     - **決定的トークン警告**：選んだメーカー商品が、自社商品にない「決定的な語」（単一漢字＝蓋/身/大/中/小 等、または数字を含むトークン＝F15/11/0.4 等）を持つ場合、赤い⚠枠で警告。
   - **使い方**：行末「✏ 紐付け」→ モーダル → 検索 or 上位候補から選択 → 警告があれば再考 → 保存。
   - **動作確認**：linkSim / linkDecisiveDiff をユニットテスト済み。本体 vs 蓋(サイズ違い) → 警告発火・類似度33%。同サイズ同品種 → 類似度67%・警告なし。
   - **既知の限界**：「本体↔内嵌合(同サイズ)」のような 2文字以上の部位差は警告しない（決定的語は単一漢字＋数字含むトークンのみ）。実運用で頻発するなら decisive set を拡張可能。

6. **🎯 照合エンジンの根本改善：部位語をcriticalMissに追加（`src/match.js:isCriticalToken`, `src/server.js:LINK_PART_TOKENS`）**
   - **背景**：ユーザの「メーカー品名と弊社品名の紐付けがダメ」フィードバックを受けた構造化対応。Jaccardは「全トークン平等」なため、サイズ違い・部位違いを軽視してしまう（`AP-F15-11 本体` vs `T-AP-F15-11 内嵌合蓋` が 80%等）。
   - **方針**：「構造化フィールド抽出」フル実装の前に、最も効く小修正＝ **2文字以上の部位語(本体/内嵌合蓋/内嵌合/巻き蓋/防曇蓋/嵌合蓋/内外蓋/高蓋/中皿/部分内嵌合蓋) を criticalMiss に追加**。
     - 従来 criticalMiss は単一漢字（蓋/身/大/中/小）+ 数字含むトークン（F15/0.4）のみが対象。
     - 部位語が抜けると80%相当の見かけマッチが出てしまうので、これも critical 扱い（score → 50%上限 → 60floor未満で休眠）。
   - **回帰検証**：オリカ hit 9/9 ✓・朝日 hit 44/missed 9/extra 80 ✓（変更前と完全同値、PART_TOKENSは朝日(袋類)にヒットしないので影響なし）。
   - **実データ改善**：
     - エフピコ：80%以下名前一致 3→1（-2件、本体↔内嵌合・蓋↔本体 を削除）
     - 中央化学：80%以下名前一致 45→39（-6件、DXPD 丸38/丸44 防曇蓋↔SV-BK 身 系の 蓋↔身 を一掃）
     - 大黒工業：±0（部位語が登場しない品種）
     - 100%・CD一致・休眠 はすべての仕入先で完全に保持
   - **同じ語彙を紐付けモーダルの警告にも反映**（`server.js:LINK_PART_TOKENS`）→ 「本体↔蓋」誤クリック時に警告が出る。
   - **既知の限界**：エコ表記差（自社に「ｴｺ」が無いだけのケース）は67%のまま残り 要確認.xlsx 行き。これは「真の候補」なので人が手動で紐付ける運用が正解。

7. **🎯 メーカー名末尾の材質コード(PP/PET/OPET/APET/OPS) を tokenize 時に剥がす（`src/match.js:stripMaterialSuffix`）**
   - **背景**：ユーザフィードバック「[157811] T-ﾄﾞﾘｽｶｯﾌﾟ115内嵌合C字蓋PP が 0085島田で休眠だが実績ある」を受けた対応。
   - **原因**：メーカー名にスペースが無い場合、`T-ﾄﾞﾘｽｶｯﾌﾟ115内嵌合C字蓋PP` 全体が**1トークン**として扱われる。自社側 `T-ﾄﾞﾘｽｶｯﾌﾟ115内篏合C字蓋` は末尾 `PP` が無いため2文字差→1文字 ins/del を超えて取りこぼし。
   - **方針**：自社の販売実績には材質コードを書かないことが多い（営業の手書き）→ メーカー側の末尾材質サフィックスを名前空間から削る。具体的には `tokenize` で normName 後に `/[-]?(opet|apet|pet|ops|pp)$/i` を1回剥がす。
   - **回帰検証**：オリカ 9/9・朝日 hit 44/missed 9/extra 80（完全同値）。
   - **救済**：エフピコで **島田(0085)の3商品が新規に100%マッチ**（APFC-A10-30 ｴｺOPET / APFC-A12-30(V) ｴｺOPET / T-ﾄﾞﾘｽｶｯﾌﾟ115内嵌合C字蓋PP）。一致 55→58。
   - **既知の限界**：APET と OPET は接尾辞だけだと区別しにくいが、両方とも剥がすので副作用なし。HIPS/PSP/PLA など他の材質コードは出てきたら追加。

8. **🎯 メーカー名の部位語前後にスペースを差し込む `preSegmentMaker`（`src/match.js`）**
   - **背景**：ユーザフィードバック「[109254] T-箱弁23-17内嵌合IC蓋 OPS が休眠だがモンドヴィーノに実績ある」を受けた対応。
   - **原因**：メーカー `内嵌合IC蓋` / 自社 `内嵌合**蓋**IC蓋` のように **自社側に余分な1文字 (蓋)** があるケース。現行の 1文字 ins/del 救済は「メーカー側に余分な1文字」だけ対応のため救えなかった。
   - **方針**：tokenize の前に PART_TOKENS（本体/内嵌合蓋/内嵌合/巻き蓋/防曇蓋/嵌合蓋/内外蓋/高蓋/中皿）の前後にスペースを差し込む。`T-箱弁23-17内嵌合IC蓋` → `T-箱弁23-17 内嵌合 IC蓋` と分割され、各トークンが自社の部分文字列として独立に拾える。長い語(部分内嵌合蓋)から順に処理して二重分割を防ぐ。

9. **🎯 ラテン英字を含むトークン = シリーズ識別子 → critical 扱い（`src/match.js:isCriticalToken`）**
   - **背景**：preSegmentMaker 導入後、`FP丼 小(V)本体` (エフピコ150635) が `MS-ﾃﾞﾘｶ 小 黒 本体` (自社007697) に 67%でマッチする副作用が発生。両方 `小`+`本体` を持つので 2/3 ヒット。シリーズ識別子 `fp丼` の不一致が無視されていた。
   - **修正**：`isCriticalToken` に `/[a-z]/i` 条件を追加。シリーズ識別子(FP/MS/T-AP/DX/MFP/CF/AP/HS/...)が自社に無ければ別シリーズの可能性が高い → criticalMiss で 50% 上限 → 60floor 未満で休眠。
   - **回帰検証**：オリカ 9/9・朝日 hit 44/missed 9/extra 80（完全同値）。
   - **救済 (#8 で +4・#9 で -16 の差し引き)**：
     - 救済された行（4件・全て真の組み合わせ）：島田 T-AP-F22-19 内嵌合蓋 / 島田 T-ﾄﾞﾘｽｶｯﾌﾟ / トモテツ FP丼小 / モンドヴィーノ T-箱弁23-17
     - 削除された誤マッチ（16件）：
       - エフピコ: 島田 FP丼 ↔ MS-デリカ (67%・別シリーズ)
       - 中央化学: DXPD36-5 G-BK ↔ G-W (色違い)、DXPD 丸38-6 G-W ↔ SV-BK ×5 (色違い)、SDビストロ M20-16/M24-18 蓋↔身 ×4、DXPD 28 蓋 ↔ 別注重箱 (67%・別物)
       - 大黒工業: -3件 (同種の色違い)
     - 全削除行は 67%/75% で **見積書本体には乗らず要確認.xlsx に行ってた誤マッチ**。実害ゼロ。

12. **🎯 仕入先コードによる照合フィルタを実装（`src/match.js`, `src/shogo.js`, `src/importPage.js`, `src/listPage.js`, `src/server.js`）**
    - **背景**：ユーザ提案「照合に使うのは仕入先コードです」。メーカー見積を仕入先マスタの4桁コード(0013/0029/0053…)と紐付け、自社販売実績の末尾コードと一致する自社品だけを照合候補にする。
    - **設計方針(B)**：単一仕入先・厳格フィルタ。問屋経由の商品は問屋側のメーカー見積で別途拾われる前提（同一商品が複数のメーカー見積に拾われる重複を防ぐ）。
    - **実装**：
      - `match.js:matchOne` に `opts.purchaseCode` を追加。`r.purchaseCode !== filterCode` の販売実績は候補から除外。productLinks(手動紐付け) は filter を bypass。`r.purchaseCode` が空の販売実績は緩めに通す（誤判定で取りこぼし防止）。
      - `shogo.js`：各メーカー見積CSV処理時に `settings.makers[supplier].purchaseCode` を取得して matchAll に渡す。出力ログに `[仕入先=0013]` か `[仕入先未設定]` を表示。
      - `importPage.js`：① のセクションに「🏢 仕入先コード」プルダウン(112社マスタ全件)を追加。選択すると `🏢 0029 大黒工業株式会社` のようにバッジ表示。保存時に payload.purchaseCode を送信。既存メーカーを picker から選ぶと保存済みコードを自動セット。
      - `server.js:saveMakerQuote`：body.purchaseCode を `settings.makers[supplier].purchaseCode` に保存（空文字なら「未設定に戻す」意図）。
      - `listPage.js`：一覧に「仕入先コード」列を追加。設定済みは `🏢 0029 大黒工業株式会社`、未設定は `⚠ 未設定 /import で設定` 表示。
    - **検証**：
      - 既存設定(全件 未設定) → 一致数完全同値（後方互換）
      - 大黒工業=0029 設定 → 一致128件 完全保持（trailing 29 マッチは全て filter 通過）
      - エフピコ=0013 設定 → 一致62件 完全保持（trailing 13 マッチも全て filter 通過）
      - **フィルタは「安全網」として機能**：現状データでは弾かれる行ゼロだが、cross-supplier の誤マッチ混入を未然に防ぐ。
    - **既定設定（このセッションで設定）**: エフピコ/中央化学/福助/朝日食品容器 = 0013(朝日経由), 大黒工業 = 0029(直接)。間違っていたら /import で変更可能。

11. **🎯 `/self` ページに「実際に使われる販売実績ファイル」を表示 + `parseSelfName` 拡張（`src/server.js`, `src/selfPage.js`）**
    - **背景1**：ユーザ要望「実際に取り込んでいるファイルが見えるようにしてほしい」。これまで /self の上半分はファイルアップローダで「プレビュー」しかなく、本番照合(`照合.bat`)が裏で読んでいるファイルが画面に出ていなかった。
    - **追加**：`/api/hanbai-source` を新設し、`config.hanbai.path` から `resolveHanbaiSource` で解決した実ファイル（フォルダ→中の最新.xls/.xlsx/.csv）を返す。/self ページ上段に「📂 照合で実際に使われる販売実績ファイル」カードを追加：設定パス・実ファイル名・更新日時・サイズ・フォーマットを表示。XLSの場合は「Excel COM 経由変換」の注意も併記。
    - **背景2**：ユーザ要望「07394 白楊8寸... 100（10ｹ元/未満800円）「元払」 1124803 ※1000 88 をバラバラに分ける機能が欲しい」。既存の parseSelfName は 5フィールド (core/freight/supplierCode/lot/supplierId) しか返さず、入数(100)・注記(「元払」)・先頭の自社CD(07394)が混在していた。
    - **`parseSelfName` 拡張（後方互換）**：
      - 新フィールド `productCode`(3-7桁先頭コード)、`packSize`(運賃カッコ直前の1-5桁数字＝入数)、`note`(「」『』[]内の補足) を追加
      - 運賃カッコ判定を「直前空白 + 和字3+」→「**和字3+ のみ**」に緩和。`...3点 100（10ｹ元/未満800円）` のように `100` の直後にカッコが続く実データに対応（`（黒）`(1字), `（4.2）`(0字) などの短い注記は引き続きスキップ）
      - supplierId 抽出時の正規表現順序を `/※\s*\S+/g → /\d{4,7}/g` に修正（先に digit 除去すると `※5000 88` の `5000` が消えた後 `※\s*\S+` が `※ 88` まで巻き込んで 88 が失われるバグを解消）
    - **/self UI**：パース結果テーブルに「入数」「注記」列を追加・例文付き説明ボックス・XLS注意。サイズ縮小しても見える config 例示。
    - **検証**：白楊系3パターン・CF寿司容器L・APFC-A10-30(M25)ｴｺ で全フィールドが正しくバラバラに。/api/calc 既存出力(supplierIdEmbed='13')も完全保持。

10. **🐛 `parseSelfName` の発注先バッジ表示バグ修正（`src/server.js:parseSelfName`）**
    - **症状**：ユーザ報告「`[001433] APFC-A10-30 🏢 ｴｺ 13`」のように発注先バッジに「ｴｺ 13」が出る。本来は「🏢 0013 朝日食品容器 株式会社」のように仕入先マスタから名前解決された形になるべき。
    - **原因**：`parseSelfName` が「最初の(...)」を運賃条件として固定で扱っていた。`APFC-A10-30(M25)ｴｺ （朝日ﾋﾟｯｷﾝｸﾞ） ※100 13` のように **商品名に注記カッコ (M25) が含まれる** と、それを運賃と誤認 → 残りの `ｴｺ （朝日...） ※100 13` から supplierId を抽出 → カッコ・※を除去した残骸 `ｴｺ 13` が supplierId に。
    - **修正**：運賃カッコの識別ルールを厳格化：
      - **直前がスペース**（注記カッコは商品名に直接くっつく `(M25)` ⇔ 運賃カッコは ` （朝日...）`）
      - **中身に和字3文字以上**（`(D)` `(V)` `(ﾎﾞ)` 等の短い注記を除外）
      - supplierId 抽出も「4-7桁コード全件削除→末尾1-4桁数字を拾う」に変更（`202886/202888 ※600 13` のような複数コードでも正しく `13` を取り出す）
    - **検証**：APFC-A10-30(M25)ｴｺ / T-AP-F22-19 内篏合蓋A-PET / CF寿司容器L0.4 華紋 身 / DXHS-31(ﾎﾞ)L 5ｹｰｽ元払 / T-MFPピザ丸 など主要パターン全てで `supplierId='13'` を正しく取得。

**累計の効果**（CLAUDE.md タスク#6〜#10）：
- 朝日/オリカ baselineは完全同値（無傷）
- エフピコ：照合行 55→61 (**+6 真の救済**)、67%以下誤マッチ -1
- 中央化学：照合行 124→112 (**-12 誤マッチ整理**)、100%・CD一致は完全保持
- 大黒工業：照合行 131→128 (**-3 誤マッチ整理**)、100%・CD一致は完全保持
- 紐付けモーダル警告も同じPART_TOKENSを使用

**次にやること（候補）：**

1. **ブラウザで紐付けモーダルの実機確認**：sim → CSV選択 → 行末「✏ 紐付け」を試打して、検索/類似度/警告/保存の挙動を体感する。
2. **要確認.xlsx の処理（CLAUDE.mdタスク2の残り）**：今夜の出力フォルダの `要確認_低一致(80%未満).xlsx`（中央化学16件・大黒工業7件・エフピコ1件）を見て、必要な行は新しい紐付けモーダルから手動で紐付け→再照合→見積書再出力で取り込む。
3. **(任意) ウォークスルーで見つかった残り改善案**：
   - #4 (b) 見積書出力後、画面に「✓ 中央化学：27社 / 要確認16件 / dup37件」のサマリを残す（今は msg 行が短い）
   - #6 dupRemoved の中身を画面で確認できるUI（重複候補をリストで表示）
   - #8 /settings or /list に「📌 確定済み紐付け一覧」（誤登録に気づきやすくする・解除ボタン付き）
   - 大2文字のお決まり語（本体/内嵌合/巻き蓋…）も決定的トークンに昇格

---

### 📍 旧現在地（2026-05-27 昼・会社PCで作業）

**この日に実装した修正（すべて動作確認済み）：**

1. **`Claude起動.bat` を追加**：プロジェクトフォルダで Claude Code をワンクリック起動できるようにした（`cd /d "%~dp0" && claude`）。これで会社PC/自宅PC のどちらでも、フォルダの bat をダブルクリックするだけで自動で `CLAUDE.md` を読み込んだ Claude が起動する。

2. **照合の重大バグ修正（1文字漢字トークンが消えていた）**：
   - 症状：メーカー「CF寿司容器L 0.4 蓋」 と 自社「CF寿司容器L0.4 華紋 身」 が **100% で誤マッチ**。`蓋`(1文字漢字) が `tokenize` で消えてしまい、`[CF寿司容器L, 0.4]` の2トークンが両方含まれるため 2/2=100% になっていた。
   - 修正：`src/match.js:tokenize` の filter を `length >= 2 || /^[一-鿿]$/.test(t)` に変更。1文字でも漢字なら保持＝蓋/身/大/中/小/特 等の区別語が消えない。
   - 検証：`_validate.js` で オリカ9/9・朝日 hit45/missed8/extra118(80%以上80) のベースラインと完全一致。回帰なし。
   - 効果（再照合 _20260527_120524 系）：
     - 自社007924 (蓋) ← メーカー「蓋」 100% ✓ 正しい
     - 自社007923 (華紋身) ← メーカー「華紋身」 100% ✓ 正しい
     - 旧 100%誤マッチ（自社「華紋身」⇔ メーカー「蓋」）→ 67% に下がって要確認.xlsx へ正しく分離

3. **未一致行のメーカー商品表示を改善（`src/server.js`）**：
   - 症状：`【販売実績なし or 商品名不一致】` の placeholder が truthy なので、`isUnmatched` 行でもメーカー商品名が表示されなかった。
   - 修正：`isUnmatched(r)` の行は **メーカー商品名を最優先表示**（`［メーカー品］<makerName>`）。

6. **🆕 仕入先マスタ取込・自社データ確認・発注先表示（3画面追加）**：
   - `/suppliers`：販売大臣「仕入先表形式」(XLS/CSV/xlsx)を取込 → `settings.json:suppliers` に保存（{4桁コード: {name,address,phone,...}}）。112社の例で動作確認済。
   - `/self`：自社販売実績ファイルを読み込み、列の役割（階層セル/購買日/単価/金額/原単価/未使用）と「階層セル内のパース結果」を可視化。`settings.json:selfProfile` に保存。
   - 共通アップローダ `readUploadedTableFile`（server.js）：.XLS/.xlsx/.csv を統一的に取込（XLS は xlsToCsv 経由、xlsx は readXlsxBuffer、csv は loadCsv）。
   - `src/hanbai.js`：商品名末尾の 1-2 桁数字（発注先コード）を `trailingPurchaseCode` で抽出 → `rec.purchaseCode` (4桁ゼロ詰め)。
   - **シミュレーション画面に行ごとの発注先表示**：`rec.supplierIdEmbed` + `currentSuppliers` で `🏢 0013 朝日食品容器` のような黄色タグを 商品名セルに併記。
   - **重要な方針変更（2026-05-27 追加）**：当初「メーカー単位で発注先を固定」する設計（settings.makers[X].purchaseCode + match.js フィルタ）も実装したが、**「同じメーカーでも商品によって発注先が変わる」**ためこれは誤り。フィルタロジックは撤去し、表示のみに変更。 `/api/maker-purchase-codes` などの API は残存（無害）。

5. **🔑 メーカー商品コード(品番) によるCD一致パス強化**：
   - 観測：自社販売実績の商品名には「`CF寿司容器L0.4 華紋 身 （朝日ﾋﾟｯｷﾝｸﾞ） 92988 ※50 13`」のように **仕入先商品コード(=メーカー品番) が埋め込み記載** されている。一方、メーカー原本xlsx(中東 容器メーカー)には「商品CD」列が存在。これらが一致すれば 100% 信頼マッチ。
   - 修正1（`src/makerXlsx.js`）：`detectColumns` に「商品CD/品番/メーカー商品CD」検出を追加し、CSV出力に `メーカー商品CD` 列を含める。`shogo.js:loadMakerQuote` は既に該当エイリアスを読むので変更不要。
   - 修正2（`src/match.js:codeHit`）：純数字コードの最小長を `6→5` 桁に緩和（実コードは5桁が多い）。誤マッチ防止に **単語境界（前後が英数字でない）** を必須化。
   - 修正3（`src/server.js`）：自社商品名を `parseSelfName` で `core/freight/supplierCode/lot/supplierId` に構造化。`/api/calc` レスポンスに含め、シミュレーション画面の商品名セルを **2行表示** に変更：
     ```
     [007923] CF寿司容器L0.4 華紋 身  #92988
     └メーカー: [92988] CF寿司容器L 0.4 華紋 身  ✓ CD一致
     ```
   - 効果：
     - 中央化学：**CD一致 18件** が発火（CF寿司容器L 0.4 蓋/華紋身 等が 100%信頼に昇格）
     - 朝日 _validate.js：hit 43→44 で **001117 旭)松型2号 が CD一致(55505)で復活**
     - 画面で「自社CD・自社商品名コア・仕入先商品コード」と「メーカー商品コード・メーカー商品名」が並列表示され、**人が違いを判別しやすい**

3.5. **「全体的に誤マッチが多い」を解消：criticalMiss ルール追加（`src/match.js:nameScore`）**：
   - 症状：中央化学 67%が117件、エフピコ 67%が129件。`CF寿司容器L 0.4 蓋`→`CF寿司容器L 巻き蓋` 67%、`DXPD 28 蓋(PET)`→`DXPD 丸38 防曇蓋` 67% など、**サイズ・部位違いの取り違えが多発**。要確認.xlsx 行きとはいえ、画面で見たユーザに「全体的に紐付けが間違ってる」印象を与えていた。
   - 修正：`nameScore` で「決定的トークン(単一漢字 = 蓋/身/大/中/小/特/丸/角 や 数字含むトークン = 0.4/28/11号)」 がメーカー側にあって自社側にない場合、スコアを **50% に抑え込む**（= floor 60 未満で休眠化）。
   - 効果（再照合 _20260527_124705 系）：
     - 中央化学：67%が117→16/休眠17→23、誤マッチ大幅削減
     - エフピコ：67%が129→4、休眠61→80、画面が劇的に静かに
     - 朝日 _validate.js: hit 45→43、extras 118→81（うち67%系=16→1、75%系=22→0で**37件の誤マッチ消滅**）
   - 回帰での hit 減少 2件（朝日）：`旭)松型2号` ブランド接頭辞ケースと `ﾆｭｰｲｰｼﾞｰﾊﾞｯｸ⇔SKﾊﾞｯｸ` 別名ケース。**両方ともCLAUDE.md 残課題「別名辞書」案件で、商品紐付け辞書(B)で手動救済可能**。

4. **🆕 商品紐付け辞書（productLinks）の実装**：
   - 名前一致のあいまいさを手動で確定できる「自社CD ⇔ メーカー商品名」辞書。CLAUDE.md 残課題「別名辞書（商品辞書）」の本実装。
   - **保存場所**：`settings.json` の `productLinks: { 仕入先名: { 自社CD: メーカー商品名 } }`
   - **照合での挙動**（`src/match.js:matchOne`）：
     - 自社CD が「このメーカー品」に予約済 → **100% 強制マッチ（score 2000）**、status= `📌 手動紐付け`
     - 自社CD が「別のメーカー品」に予約済 → その自社CDへの fuzzy マッチは**除外**（取り合い防止）
   - **/api/calc の挙動**（`src/server.js:calcAll`）：照合時に productLinks を反映していない既存CSVに対しても、表示上は同じく適用される（取り合い行は除外、対象行は 📌 表示）。新しい紐付けが入った行を新規に追加したい場合は「↻ 照合を実行」で再生成。
   - **見積書出力での挙動**：`matchScore('📌')` = 2000（CD一致=1000より上）。dedup で 📌 が最優先される。
   - **API**：`GET /api/product-links` / `POST /api/product-link {supplier,productCode,makerName}`（`makerName=''` で削除）
   - **UI**：シミュレーション画面のテーブル末尾に「紐付け」列追加。各行に「✏ 紐付け」ボタン → モーダルで仕入先・自社商品を表示し、メーカー商品プルダウンから選択 → 保存（解除も同じUIで）。確定済の行は緑色「📌 確定」表示。
   - **動作確認済み**：API ラウンドトリップ・/api/calc 反映・settings.json 永続化すべて OK。

**次にやること（候補）：**

1. **見積書を再出力（タスク1）**：sim.bat → 中央化学/エフピコ の `_20260527_120524` 系 CSV を選択 → 「この内容で見積書を出力」。 ③ T-AP-F15-11 と DX仕切 R-12-N が見積書本体に乗る。CF寿司容器L 0.4 蓋 も自社007924 に正しくマッチ。
2. **要確認.xlsx を確認（タスク2）**：② AP-F22-19 本体・④ DX丸皿K-1 BK を島田/かもめ荘の見積書に手動追加。
   - もしくは、新しい商品紐付け辞書を使って「✏ 紐付け」ボタンから明示的に紐付け → 再照合 で自動的に取り込む。
3. **(任意) 別名辞書のさらなる活用**：CF寿司容器L 0.4 蓋 系の他にも 67% で要確認に行く案件があれば、画面から紐付けを保存して恒久化。

---

### 📍 前日の現在地（2026-05-26 夜・自宅PCで作業）

**自宅PCでのセットアップ済（参考）：** `Cursor`（VSCode派生）をインストール→日本語化拡張→このフォルダを開く→ターミナルで `npm.cmd install -g @anthropic-ai/claude-code`→`Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`（PowerShellの .ps1 ブロック回避）→ `claude` で起動成功。会社PCでも同じ手順で入れ直せばOK（Drive同期で本ファイルは引き継がれる）。

**この日の夜（自宅PC）に実装＝照合エンジン改良 v2（GO後・検証済）：**
タスクB「要確認の最終チェック」の中で、エフピコ③ `T-AP-F15-11 内嵌合蓋ｴｺ`(蓋)が誤って `AP-F15-11 本体 ｴｺ`(本体, 67%) に当たり、本来の蓋が休眠していた問題を修正。`src/match.js` に **品番アンカー(`isCodeLike`= ハイフン+数字+長さ4+)** を導入：
- メーカー名に品番アンカーが rec に**完全一致**で含まれる場合、その品名内の他トークン（長さ4+）について**1文字 挿入/削除**のあいまい一致を許容（「内嵌合蓋」↔「内嵌合」の蓋有無を救う）。数字内容が変わらない事を必須。
- トークン自身が品番パターン(長さ5+)なら、アンカー無しでも 1文字 挿入/削除 を許容（「R-12-N」↔「-12-N」のR有無）。同時に品番らしさ(`isCodeLike`)を保つ必要。
- 同長置換のあいまい一致は従来どおり異体字/濁点ホワイトリスト限定（「角中↔角小」等は弾く）。

**検証結果（_validate 回帰・ベースラインと完全同値＝無傷）：**
- オリカ: hit 9/9・missed 0・extra 0
- 朝日: hit 45/53・missed 8・extra 118（うち80%以上=80, 80%未満=38）

**実データ改善（照合再実行→ `input/` に新しい _211845系 CSV 生成済）：**
- ③ `T-AP-F15-11 内嵌合蓋ｴｺ` → 007486 (T-AP-F15-11 内篏合 ｴｺ) に **100%一致** で蓋に正マッチ。本体側67%は重複除去で消える。
- 🎁 `DX仕切 R-12-N(ﾎﾞ)` （CLAUDE.mdの残課題）→ 001377 (DX仕切-12-N(ﾎﾞ)) に **100%一致** で取りこぼし救出。**年間29万円規模**。
- エフピコ 一致 86→88、中央化学 一致もアップ。
- 中央化学④ `DX丸皿 K-1 BK(1800)` ↔ `DX丸皿K-1 BK` は **(1800)ロット表記差** で 67% のまま（要確認.xlsx に残る）。
- ② `AP-F22-19 本体 ｴｺ` ↔ `AP-F22-19 本体`（エコ無し）も **エコ表記差** で 67% のまま（要確認.xlsx に残る）。

**ユーザーの最終判定（タスクB）：**
- ① うなぎ vs ピザ → ❌ 別物・除外（67%なので自動で要確認.xlsxに分離・OK）
- ② AP-F22-19 本体 → ✅ 採用（67%＝要確認.xlsx に残る・**手動で本体見積書へ追加が必要**）
- ③ T-AP-F15-11 蓋 → ✅ 採用（**修正後100%・自動で見積書に乗る**）
- ④ DX丸皿K-1 BK → ✅ 採用（67%＝要確認.xlsx に残る・**手動で本体見積書へ追加が必要**）

**翌日（会社PC）にやること：**
1. **見積書を再出力**（sim.bat → 中東 中央化学/エフピコ の最新照合結果CSV `_20260526_211845` 系を読み込み → 「この内容で見積書を出力」）。これで ③ と DX仕切 R-12-N が **自動で見積書本体に乗る**。
2. 上記とは別に **要確認_低一致(80%未満).xlsx を確認** → ②（AP-F22-19 本体）と ④（DX丸皿K-1 BK）の2件を手で見積書(島田・かもめ荘)に追加。
3. ①（うなぎvsピザ）と他の60-79%案件は除外のまま。
4. **任意の改良（②④の自動救済）**：
   - ② エコ表記差：「ｴｺ/エコ」のような短いサフィックスを最後尾に付くオプション語として軽視するロジック
   - ④ `(1800)` ロット数：4桁以上の数字だけのカッコを注記扱いで除去（2-3桁は OK袋(50) 等のサイズなので温存）
   どちらも match.js / tokenize に1〜2行の追加で実装可能。GO待ち。

**この日午後（会社PC）に実装（すべて動作確認済み）：**
1. **ファイル選択ボタン**：/import に「📂 ファイルを選択」追加。CSV(UTF-8/Shift_JIS自動判定)・xlsx対応。複数シートxlsxはシートピッカーで選択。`POST /api/read-xlsx` (server.js) + `readXlsxBuffer` (xlsxread.js新規エクスポート)。
2. **書式紐付けの可視化**：仕入先ピッカーで登録済みを選ぶと「✓ 書式登録済み：商品名=2列目／現単価=4列目…」と緑色サマリ表示（`showProfSummary`）。
3. **ステップ②のロック**：仕入先未選択時はファイル選択・貼付欄・読み取りボタンを全部 disabled＋赤いガイド（`updateStep2Lock`）。新規入力で名前を1文字でも入れたら解放。
4. **保存ボタンの再配置＋黄色注意ボックス**：ステップ③の見出しの右に「この内容で保存」を置き、その下に「⚠ここまでは未保存／保存すると①maker_quotes/に②settings.jsonの書式に書き込まれます」を常時表示。
5. **A/B/C 三段の前処理**（大黒のような挨拶文付きxlsx対応）：
   - **A 自動見出し検出**：`jsDetectColumns` で「商品名・単価」を含む行を上から探し、それより上を skip。
   - **B 日本語日付→ISO**：`jpDateToISO` で「7月1日～」→ `2026-07-01`。ファイル日付か今日基準で年を推定（「次に来るM/D」ルール）。
   - **C 手動スキップ**：「先頭の[N]行を捨てる」スピナー。A が外したら手で調整。
   - 捨てる行は **グレー＋打ち消し線で表の上に表示**＋黄色の境界帯「▲ここまで捨てる/▼ここから取り込む」。Cを動かすとリアルタイムで切り替わる。
6. **/list 一覧・進捗ページ新設**：`src/listPage.js`。`/api/maker-list` が maker_quotes/ をスキャンして、input/<仕入先>_照合結果_*.csv の有無＝「🟡 照合済み」、output/<仕入先>_照合結果_見積書_*\の有無＝「🟢 見積書作成済み」を判定。「📂 取込CSVを開く」「📁 見積書フォルダを開く」（`POST /api/open`で explorer 起動）。ヘッダーから誰でも飛べる。
7. **xlsx取り込み時の数値整形**：浮動小数誤差（`9.5299999999999994`→`9.53`）と Excel日付シリアルの自動変換を `normalizeMakerGrid` でサーバー側で実施。

**新規/更新ファイル：**
- 新規：`src/listPage.js`
- 更新：`src/xlsxread.js`（`readXlsxBuffer` 追加エクスポート）、`src/server.js`（/list, /api/maker-list, /api/open, /api/read-xlsx, normalizeMakerGrid, listMakerQuotes, safeOpenPath, header に listLink）、`src/importPage.js`（大幅改修）

**実物の検証データ：**
- 中東 `日野折箱店様（容器メーカー）.xlsx` ＝ 3シートで読める（既存）
- 大黒 `C:\Users\takaf\Downloads\日野折箱店様　価格改定一覧　20260526.xlsx` ＝ 冒頭12行が挨拶文、A自動検出で headerRow=12、B で「7月1日～」が「2026-07-01」に。手元で末端まで通った。**自宅PCで再現する場合はこのファイルがDownloadsにある会社PC前提。Driveにコピーするか、自宅は別のサンプルで確認すること**。

**設計の補足（中東）：**
- `extractSupplierFromMakerFile` は `メーカー見積_<仕入先>_YYYYMMDD_HHMM(SS)?\.csv` に対応（stamp() は HHMM の4桁）。
- `safeOpenPath` は ROOT 配下のみ許可するガード入り。

---

### 📍 元の現在地（2026-05-26 朝〜午前）
中東（問屋）の価格改定 `日野折箱店様（容器メーカー）.xlsx`（エフピコ/中央化学/福助の3シート）を
**取り込み → 照合 → 得意先別 見積書の出力まで一通り実施済み**。
- 最新の照合結果：`input/エフピコ_照合結果_20260526_072153.csv` / `中央化学_…_072154.csv`（福助は休眠のみ）
- 最新の見積書：`output/…_見積書_20260526_0723/`（エフピコ25社・中央化学33社＋要確認）
- この日に実装した新機能：**.xlsx直読みインポータ**（`maker_quotes/`に.xlsxを置く→照合.batで自動取込。または取込.batにD&D）。
- この日に実施した**照合エンジン改良**（サイズ語の誤マッチ停止・表記ゆれ吸収・出力の同点タイブレーク。回帰検証OK＝下記参照）。

**次にやること（候補）：**
1. **要確認の最終チェック**：エフピコ3件・中央化学1件。正しければ採用、誤りなら除外のまま。
2. **`DX仕切 R-12-N`（メーカー）と `DX仕切-12-N`（実績）が同一品か確認**。同一なら「Rの有無＝1文字挿入/削除」を安全に救う仕組みを追加（今は休眠＝取りこぼし）。
3. **見積書を提出用に確定**：必要なら `sim.bat` でルール/実施日/自社上乗せ%を調整して再出力。
4. **カモ井のPDF**（手紙形式・ルート直下の2ファイル）は未取込。必要時に /import で貼り付け取込。

---
（以下は経緯メモ）

**照合エンジン本体は完成・検証済み（2026-05-25）。** `src/hanbai.js`＋`src/match.js`。
既存の オリカ/朝日 照合結果（正解データ）で検証：商品レベル再現 オリカ8/8・朝日45/53、
現売単価/年間金額は全件一致、別商品への誤マッチ(80%以上)はゼロ。生成した照合結果CSVは
既存 `load.js` で読め、転嫁計算まで通る（メーカー見積→照合→既存パイプラインが一本化）。
照合の要点：①CD一致（メーカー品番が販売実績の商品名に埋め込まれている）を優先、
②名前一致（コア商品名のトークン一致率。同長1文字違いの異体字は救い、`5号`≠`15号`/`10号`≠ロット`10`
は数字境界＋同長あいまい一致で弾く）、floor60未満は休眠。朝日の取りこぼし8件は
ブランド接頭辞(ﾌｼﾞ/旭))・商品辞書(VM規格袋→ロールパーチ)・単位ゆれ(2㎏vs2K)で、安全側に休眠/要確認へ。
→ 将来の別名辞書(商品辞書)で改善可。

**照合の実行動線も完成（2026-05-25）：`照合.bat`＝`node src/shogo.js`。**
販売実績は config.hanbai.path から最新を自動選択し、旧.XLSなら `xls2csv.js` がExcel COMで自動変換 →
`maker_quotes/` の各メーカー見積を照合 → `input/<仕入先>_照合結果_<日時>.csv` を出力。
実走確認済み（実物.XLS変換→オリカ9品→照合10行＝9一致+1休眠、元データと一致）。
**Excel依存は xls2csv.js（=照合.bat実行時）だけに隔離**。クラウド化時はここを通さずCSVを渡せばエンジンはそのまま。

**①仕入先プロファイル＋②照合ボタンも完成（2026-05-25）。**
- ① `/import` に仕入先ピッカー。仕入先を選ぶと前回の列マッピング(delim/見出し含む)を自動適用。
  見積保存時にその対応を `settings.json` の `makers["仕入先名"]={map,delim,hasHeader}` に記憶（GET/api/makers, saveMakerProfile）。
- ② sim画面に「↻ 照合を実行」ボタン（POST /api/shogo → `node src/shogo.js` 相当 → 新しい照合結果を作って一覧に反映）。
  ※ shogo.run() はライブラリ用にエラー時 throw（サーバが落ちないよう process.exit しない）。

**照合エンジン改良（2026-05-26 実施・回帰検証済）**
中東タレビンのレビューで判明した誤マッチ＆取りこぼしを修正。
- サイズ語の誤マッチ停止：「角中↔角小」「角大↔角特大」等の1文字差を異体字扱いしていたのを、
  **異体字/濁点だけのホワイトリスト限定**に変更（`match.js`）。`ﾄﾚｰC-7↔V-79` のような100%誤マッチも解消。
- 表記ゆれ吸収：`壜/瓶→ビン`・`篏→嵌` を正規化（`textnorm.js`）、英字のみ注記カッコ`(D)(KS)`を照合時に無視、
  区切り `-/×/・` を同一視。→ ﾀﾚﾋﾞﾝ角中 を買う複数社が「ﾀﾚ壜 角中(D)改」で正しく拾えるように。
- 同点タイブレーク：見積出力で同%なら「メーカー名の語数が多い＝具体的な品」を優先（`server.js`）。
  → グリル壱乃蔵が「角中太口」で正しく一致（並び順に依存しない）。
- 検証：_validate 回帰（オリカ9/9・朝日hit45/危険80＝無傷）。中央化学の要確認は10→1件に減少。
- 軽微な残課題：挿入/削除1文字のあいまい一致は廃止したため「DX仕切 R-12-N↔DX仕切-12-N」のR有無差は今は休眠。

**残り（任意の改善）**
3. 別名辞書（商品辞書）でブランド接頭辞(ﾌｼﾞ/旭))・単位ゆれ(2㎏vs2K)・別名(VM規格袋→ロールパーチ)の取りこぼしを救済（朝日の8件など）。
   → 実運用で本当に必要な別名が見えてから作るのが効率的（今は安全に休眠/要確認へ逃げる）。
※ `_validate.js`（直下）＝回帰確認スクリプト。`node _validate.js input/オリカ_照合結果.csv`（販売実績は自動解決）。本番不要だが回帰に便利。

### 照合の設計メモ（既存データの形）
- **販売実績**：`G:\マイドライブ\価格改定\価格改定AI\販売大臣CSV\…年間販売実績.csv.XLS`（旧Excel/Excel COMで読む）。販売大臣の階層出力（得意先見出し行＋商品行）。商品行の1列目に「自社商品コード＋商品名＋埋込メーカー品番＋ロット」が詰まっている。バラ最新単価=現売単価、原単価=現仕入単価（0が多く信頼薄）、金額=年間金額。**仕入の現/新単価はメーカー見積側を使う**（販売実績の原単価ではない）。得意先名は見出し行から整形（半角スペース＋配送語で切る）。
- **仕入先マスター**：同フォルダ `仕入先マスター\仕入先表形式.XLS`（仕入先コード/名/住所/電話）。
- **メーカー見積の形式は3種**（サンプルはツール直下に配置）：エフピコ=きれいなExcel表 / 大黒工業=PDF表（空欄で列ズレ）/ カモ井=PDF手紙（表でない・手入力）。→ だから取り込みは「貼り付け＋人の確認」方式にした。
- 自社品↔メーカー品の結び付け：御社は仕入先商品コードを自社マスタに登録（CD一致）＋名前一致。

## 2台のPCでの共有（Google Drive）

このフォルダは Google Drive 上にあり、会社PC・自宅PCに自動同期されます。
1. **PCを切り替える前に Drive の同期完了を待つ**（右下アイコンが「最新」）
2. **2台で同時に編集しない**（コンフリクトコピー防止）
3. もう片方のPCに **Node.js** が入っていること（無ければ https://nodejs.org/ja のLTS）

## データの場所

- 業務データ：`G:\マイドライブ\価格改定`（得意先別の改定Excel）。`価格改定AI/` に元データ（販売大臣CSV・仕入先マスター 等）
- メーカー見積サンプル：このフォルダ直下（日野折箱店様（容器メーカー）.xlsx / …カモ井…pdf / …価格改定のご案内.pdf）
- 取り込んだメーカー見積：`maker_quotes/`
- 動かすとき：照合結果CSV を `input/` に置く（必要なら `損益.csv` を直下に）

## メモ
- GitHubは未使用（将来導入可）。node_modules は未使用の残骸（消しても動く）。
- 旧Excel(.xls)はテキストで読めない→Excel COM（PowerShell）。PDFはWord COMでテキスト抽出可（ただし配布/クラウドでは使えないので、取り込みは貼り付け方式）。
