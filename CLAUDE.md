# 価格転嫁見積ツール — AI向けガイド（CLAUDE.md）

Claude Code / Cursor が起動時に読み込むファイルです。  
**会社PC・自宅PCのどちらでも、この内容で方針と構成を把握できます。**

> 過去の詳細な作業ログ・引き継ぎメモは **`CLAUDE_作業履歴.md`**（旧版の全文）にあります。日々の細かい検証結果はそちらを参照してください。

外販（他社への販売）に関する検討は 外販メモ_他社データ対応.md を参照。

---

## 📌 会社PCで再開したら最初に（保留タスク・AIは会話の冒頭で知らせる）

> **AIへの指示**：新しいチャットの最初に、ユーザーが会社PC（販売大臣DBに接続できる環境）で作業しているなら、下の未消化タスク（`[ ]`）を要約して知らせること。会社PC/自宅PCの判別は、必要なら `/api/hanbai-source` の `source` や DB接続（`probeDbConnection`）で裏取りする。済んだ項目はユーザー確認のうえ消す。自宅PC（DBなし）では「DB必須」印の項目は実行しない（日野9000自社製造照合が劣化する事故あり）。

**会社PC（DBあり）で実施する保留タスク：**

- [x] **【DB必須】定番チェック（2026-06-15 実施・合格）**：自社製造DB再抽出（719品）→ 再照合。`/api/dup-check`=**0**、日野自社製造一致=**1051**（休眠8はすべて `預り分`/`製造ﾏｽﾀ`/`使用しな`/`ｻﾝﾌﾟﾙ` の内部品＝当然休眠で正常）。※ 記録上の「1085」は5月末スナップショット。実態は得意先×自社品ペアのローリング窓で月次変動するため、**固定値で判定しない**。判定基準は「dup-check=0 ＋ 日野の休眠が内部/サンプル品のみ」。
- [x] **002172 ✓CD一致 へ昇格（2026-06-15 完了）**：販売大臣の商品名3(SHOHIN.NM3)に `370121` は**登録済み**だった（DB確認済）。それでも📌表示だったのは冗長な手動紐付け(`productLinks`)が score2000 でCD一致(1000)を**マスク**していたため。`saveProductLink('大黒工業','002172','')` で📌を削除→再照合で **✓CD一致**（`フレッシュメイト…A-6`・塚脇紙器のみ／703050には付かず）。dup0・recall不変。
- [x] **却下情報(`cdReview.rejected`)を照合エンジンに反映（2026-06-15 実装）**：監査の保留#4の一部を先行実装。`match.js` の `matchOne` に「却下済み 自社CD×メーカー品番」除外を追加、`shogo.js` から `rejected` を `matchAll` に受け渡し。これで誤一致 **007798↔3500813（W-120 を WS-120 に名前一致100%）が消滅**（matchaudit の false positive 1→**0**）。dup0・日野1051・recall93.9%すべて不変＝副作用ゼロ。**※要 sim.bat 再起動（エンジン変更）。**
- [~] **【DB必須・要検証】監査の保留 #3：見送り（2026-06-15 実データで評価）**：`IRISUU` は多くが0・`LASTSHITANK` はスケール不整合（19000/1900000）・`SHOHIN.SHIIRE`(マスタ仕入先)は既存の伝票由来 `purchaseCode` フィルタと重複。**recall未捕捉15件は全て「窓内に売上が無い休眠」で構造化列では復活しない**。dup0/recall93.9%の安定系に対しリスク>リターンのため**着手しない**。将来やるなら効果が見える品が出てから。
- [~] **【DB必須・要検証】監査の保留 #4：見送り（2026-06-16 実データで評価）**：スコアリング改良（共通語の重み下げ・双方向スコア・ブランド別名/接頭辞辞書）。現状は **誤一致0・名前一致の平均スコア99.7%・未捕捉15件は全て休眠（候補に上がらない）**＝改良を効かせる先が無い。安定系（dup0／日野自社製造1052／recall94.6%）へのリスク>リターンで**着手しない**（#3と同型）。将来やるなら効果が見える品が出てから。`matchaudit` で前後比較。
- [~] **【DB必須・要検証】性能リファクタ D2〜D5（2026-06-16 一部実施・残り見送り）**：①**DB二重ロードは既に解消済み**だった＝`shogo.js` の全期間モード（本番設定 `candidateMonths=0`）は `selfPoolLoad=hanbaiLoad` で再利用。二重ロードは窓モード時のみ。②**`matchOne` ホットループの `padSelfCode` 再計算をレコードキャッシュ化**（`match.js` の `padCodeCached`）。`padSelfCode` は純関数なので結果不変。再照合＋回帰スナップショット(`matchsnap`)で **lost0/changed0/gained0** を確認済み。残りの Map 化等は「結果が変わらないことを確認してから」効果が見えれば着手。

**2026-06-17 設計整理：日野はDB専用運用へ（4点・会社PCで実施・検証済み）**

- [x] **販売実績の取得元をDB専用化（`settings.json` `hanbai.source`：`auto`→`db`）**：`auto`は「DB不可なら黙ってファイル(1年分)へ降格」する穴があり、「全期間」が嘘になる事故（例：6921が拾えない）が起きていた。`db`（厳格）にして降格を廃止＝DB不可なら照合は明示エラーで停止。**ファイル経路(`source:'file'`)のコードは将来のSaaS/他社版用に温存**（config.js 既定は`file`のまま。日野だけ settings で`db`上書き）。
- [x] **照合は全期間（再確認・実装済み）**：`candidateMonths=0` → `shogo.js` run() が一般メーカーにも `fullHistory:true` を渡し、DBを **全期間（約8263件・2000年まで遡及）** で読む。`monthsBackStart` の12か月クランプは fullHistory 経路では効かない＝一般照合も全期間で正しく動作。
- [x] **CD一致のハイフン正規化バグ修正（`match.js` `codeHit`）**：メーカー品番側は記号除去（`L-2083`→`l2083`）するのに、実績の埋め込みコードは `l-2083` と残るため非対称で当たらなかった。`codeHit` の英字コード判定を**記号除去版でも照合**するよう1行追加（純数字の単語境界判定は誤一致防止のため対象外＝従来どおり）。前後比較で **dup0／要確認0／誤一致0／日野1052／recall290(98.6%)不変、一致3548→3578(+30)・CD一致+142（名前→CD格上げ含む）**。6921=`L-2083 ビリジスメロン`が✓CD一致（シンコー・現売108）に復活。**※要 sim 再起動。**
- [x] **閲覧モード（自宅PC対応・`source:'db'`の副作用解消）**：`server.js` に `isDbReachable`/`isViewOnly`（probe・成功60秒/失敗5分キャッシュ）。`getHanbaiRecordsCached` を耐障害化＝DB不可でも例外で画面を壊さず空で続行。`/api/calendar` の `viewOnly` でフロントが判定し、**🔒閲覧モード**バナー表示＋↻照合ボタン無効化＋`/api/shogo`は親切エラー。会社PC=`viewOnly:false`で全機能・無回帰。自宅PCはDrive同期された input/ 照合結果CSVで**表示専用**。
- [x] **見積の「抽出期間」を正式化（`customersPage.js`）**：旧「表示期間」を**「抽出期間」**に改称（照合は全期間／ここで“見積に出す期間”を絞る・選択肢A＝得意先の絞り込みのみ。価格は全期間ベース）。選択を **localStorage に永続化**（`applyFilter`で保存・`initControls`で復元）＝毎回「過去1年」リセットを解消。6921(最終売上2024/8)は抽出期間「過去2年」でシンコー見積に出る。
- [x] **「照合から外す」機能＝🚫除外／💤休眠（保留）の2種**：販売実績はあるが もう仕入れない品が、似た名前の別メーカー品に誤紐付けされるのを根本から防ぐ。`productLinks[仕入先][自社CD]` に特別マーク（`productLink.js`：**`__EXCLUDE__`**＝🚫別物として除外／**`__DORMANT__`**＝💤休眠（保留）＝正しい品だが今は仕入れていない）を保存。`isExcludeLink` は両マークを「スキップ対象」と判定、`linkMarkKind` が表示種別を返す。`match.js` の `matchOne` 候補ループで**CD一致・名前一致・価格救済すべてに優先してスキップ**（動きは2種とも同じ＝意味とバッジだけ違う＝外した理由が後で分かる）。仕入先単位・可逆（📌編集で別品選択 or 解除で戻る）。UIは📌編集モーダル（mode='maker'）の「🚫 別物として除外」「💤 休眠（保留）にする」2ボタン＋紐付けセルの「🚫 除外中」/「💤 休眠（保留）」バッジ。`auditProductLinks`/`isProductLinkActive` は両マークを対象外（orphan扱いしない・一致扱いしない）。指定が無ければ照合結果は完全に不変＝**無回帰**（dup0・要確認0・誤一致0・日野1052・recall298/302で確認）。エンジン実証：L-2083で6921を除外/休眠どちらでも→休眠。**※要 sim 再起動。**

**2026-06-26 実装：💤休眠の「復帰通知」（自宅PC実装・要 会社PC検証）**

- [x] **【DB必須・2026-06-28 会社PC検証合格】💤休眠なのに最近 売上ありを通知（`dormantRevival.js` 新規）**：休眠（保留）にした自社品に その後 新しい売上が立ったら、紐付け監査バナー（`#linkAlert`）に **「💤→🔼 休眠なのに最近 売上あり N件」** を表示し、各行に最終売上日を出して **「🔄 復帰（解除）」ワンクリック→ 会社PCで「↻ 照合」** で見積に戻せる。自動では戻さない（返品/サンプル単発の誤検知を避け、人が判断＝[[prefer-flag-bad-data-not-auto-fix]]）。🚫除外（別物）は対象外＝💤休眠のみ。判定は **「休眠にした日」より後に売上があるか**＝`settings.js` の `saveProductLink` がマーク化時に `productLinkMarkedAt[仕入先][自社CD]='YYYY-MM-DD'` を記録（解除/通常名で消す・`saveSettings` ホワイトリストに追加）。休眠日の記録が無い旧マークは「直近120日に売上」でフォールバック。判定材料は販売実績の `lastDate`（最終売上日）＝DB必須（自宅PC＝閲覧モードは hanbai 空で0件＝素通り）。副次修正：`linkBetterAudit.js`（better/suspect 監査）が 🚫/💤 マークを **対象外に**（従来は休眠品を「より確実な候補」と誤提案しうるバグ＝`auditProductLinks` と作法を統一）。エンジン（`match.js`）は無改変＝`matchaudit` 不変（要確認0・日野1052・recall95.4%確認済）。単体テスト：`node src/dormantRevival.js`。**※要 sim 再起動（server.js 変更）。**
  - **会社PC検証（2026-06-28）**：全変更ファイル `node -c` OK／`dormantRevival selfTest OK`／`matchaudit` 無回帰（日野自社製造**1052**・要確認**0**・recall**95.4%**・誤一致**0**）／実画面 `/api/link-check` は復帰**0件**（現時点で休眠11件＝markedAt有3・旧マーク8 のいずれも休眠後の売上なし＝正常な0件）／**注入試験**（休眠品に当日売上を与える）で復帰**1件**を検知＝検知ロジック動作確認／実DBレコードは `productCode`・`lastDate` を保持（常時0件化しない）／「🔄 復帰（解除）」は既存の `POST /api/product-link`（`makerName:''`）でマーク解除＝可逆。

**2026-07-11 実装：🔁「コード再利用の貼り替え忘れ」検知（自宅PCで実装・実input/で検証合格・DB不要）**

- [x] **📌が旧品を指したまま＝旧原価が静かに出続ける事故を検知（`linkSwapAudit.js` 新規）**：運用で「自社CDはそのまま・中身のアイテムだけ入れ替える」ことがある（例：コパックス V-79 を同じ 002050 のまま エフピコ FLB-A13-20 へ差替、原価4.95→4.8・仕入先も変更）。このとき販売大臣のマスタ品名は新品に更新されるが、ツールの 📌手動紐付け が旧品(V-79)を指したまま残りやすい。📌は最優先(score2000)で照合を上書きするため、放置すると**旧品・旧仕入先の原価が“静かに”出続ける**（新品にならない）＝人が気づきにくい価格事故。紐付け監査バナー（`#linkAlert`）に **「🔁 コード再利用の貼り替え忘れ N件」** を表示＋メイン表の📌セルに **🔁バッジ**＋📌編集モーダルに警告。各行「✏ 直す（新品へ貼替）」「解除（別仕入先なら）」。自動では直さない（[[prefer-flag-bad-data-not-auto-fix]]）。
  - **誤検知対策が肝**：素の「メーカー名とマスタ名の品番が食い違う」では実データ14件中**12件が誤検知**（同一品の別表記：風呂敷 035x900＝№90／NK30BA＝NK30／IF桃ﾊﾟｯｸ 267X427＝PS267×427 等）。本物の入れ替えは品番が完全に無関係（`V79`↔`FLBA1320` は共通2文字なし）。よって **「両者の品番コードが共通2文字を1つも持たない」ときだけ出す**（少しでも重なれば同一品＝正常）。これで**誤検知12件が全消え、本物2件だけ**（コパックス 002050＝今回の件／002043 V-26→FLB-012-33 も自動発見）。判定材料は照合結果CSVのみ＝**閲覧モード(DB不可)でも効く**。単体テスト：`node src/linkSwapAudit.js`。
  - **副次のバグ修正（`server.js` `/api/link-check`）**：返却 `issues` を `slice(0,80)`（仕入先名順）していたため、**orphan（古い紐付け＝画面に出さない・現在344件）が枠を食い潰し、貼り替え忘れ/勘違い/復帰/より確実の重要通知が切り捨てられていた**潜在バグを発見。描画する種別を**優先度順に並べ替えてから返す**よう修正（枠も120へ）＋`swapCount`/`suspectCount` を返却に追加。これで復帰1・勘違い1・より確実4 の既存バナーも復活。
  - **検証（自宅PC・実input/＋実settings.json）**：`node -c` 全OK／`linkSwapAudit selfTest OK`／`/api/link-check` で `swapCount:2`（002043・002050 のみ・誤検知0）／実ブラウザ(localhost:8765)でバナー描画＆**コンソールエラー0**を確認。エンジン（`match.js`）は**無改変**＝`matchaudit` 不変のはず（会社PCでの最終確認推奨）。**※要 sim 再起動（server.js 変更）。**
  - **参考（元の指摘）**：内部品番 `auditProductLinks` は `getRecs`（正規化済みレコード）に対し日本語ヘッダキーで読むため**全件orphan扱いになっている**（＝上記344の主因。画面に出さないので実害は無いが、いつか整理候補）。`linkSwapAudit` は正規化フィールド(`matchStatus`/`productCode`/`makerName`/`masterName`)・生CSVキーの両対応で実装済み。
  - [x] **Fable5 独立レビュー反映（2026-07-11）**：別モデル(Fable5)にコードレビューを依頼＝致命問題なし・実データ310グループで誤検知0の総評。指摘の🟠1件を修正＝**「✏直す/解除の直後（未照合）に🔁が消えず、隣の解除で新📌を誤削除」**を防止。検知はCSVスナップショットで判定するが、📌の解決有無だけは“現在の”productLinksを見て判定＝`auditLinkSwaps` で「解除済み(`!linked`)／別品へ貼替済み(`!linkNamesEqual(linked, makerName)`)」ならスキップ（`linkNamesEqual` を import 追加）。実データ検証：2050解除→即2043のみ／2043貼替→即2050のみ（未照合でも即バナーから消える）。単体テストに貼替済/解除済ケース追加＝`linkSwapAudit selfTest OK`・実`/api/link-check`=swapCount2 不変。🟢の限界（同一シリーズ内の入替 V-26→V-29 等は共通2文字で見逃す／和名のみ品は品番なしで対象外＝310中174件）は誤検知0優先の意図的トレードオフとして許容。

**2026-07-11 実装：🔁 貼り替え忘れの「自動修復」＝取込＋↻照合だけで直る（`autoRepoint.js` 新規・会社PC実証合格）**

- [x] **背景（要注意の設計知見）**：名前一致は**販売実績の伝票品名(`r.productName`)**で照合する（マスタ名ではない＝`match.js`）。コード再利用で品を入れ替えても伝票品名は旧名(例 002050=「ﾄﾚｰV-79 無地…」)のまま残るので、**📌を外すだけでは新品に名前一致せず休眠に落ちる**。正しい直し方は**「再ポイント」＝旧供給元の📌解除＋新供給元に新品で📌**（📌は最優先なので伝票品名が旧名でも強制で寄る）。手動だと2手かかる。
- [x] **自動化（`autoRepoint.planRepoints` + `server.applyAutoRepoints`）**：`runShogoGuarded` の**照合直前**に、`linkSwapAudit` が検知した swap のうち**入替先の新品が“今のメーカー見積に現マスタ名で完全一致して実在”するものだけ**、📌を自動で新品へ張り替える（旧供給元📌解除＋新供給元に📌）。→ 利用者は**入替見積を取り込んで↻照合するだけ**で直る（解除ボタン不要）。**安全**：入替先が無い/複数供給元・複数名で曖昧なものは触らない（休眠化させない）。検知は誤検知0の`linkSwapAudit`に乗る。エンジン(`match.js`/`shogo.js`)は**無改変**（server側で照合前に settings の📌を直すだけ）。**透明**：張替内容を`/api/shogo`応答`autoRepointed`で返し、照合完了メッセージに「🔁 …自動修復 N件」を表示＋settings操作で**可逆**。既定ON、`settings.json` の `autoRepointSwaps:false` でOFF（`getSettings`/`saveSettings` ホワイトリストに追加）。単体テスト：`node src/autoRepoint.js`。
- **会社PC実証（2026-07-11・DB）**：①**002050 は再ポイントを手動API実行**（`/api/product-link` でコパックス解除＋朝日「ﾄﾚｰFLB-A13-20 W」）→↻照合→朝日で📌・現仕入**4.8**・dup0・🔁は002050消え002043のみ。②**002043 は自動修復を実証**＝入替先「ﾄﾚｰFLB-012-33 ｴｺ」(朝日/4.8)取込済みの状態で↻照合するだけで、コパックス📌が自動解除＋朝日へ自動📌→朝日CSVで📌 FLB-012-33・**4.8**、コパックス002043=0件、**swapCount0・dup0**。③`matchaudit` 健全＝総行4798・**要確認0**・**日野1055**・名前平均99.4%・**false positive0**（手動1442→1480＝自動修復2件分の増）。全ファイル`node -c`OK・`autoRepoint selfTest OK`・配信ページ200健全。**※要 sim 再起動（server/settings変更）。**

**SaaS化の前に（会社PCでなくても可）：**

- [x] **settings.json の世代バックアップ（2026-06-16 実装）**：保存（全UI操作が通る `settings.js` の `saveSettings` 単一経路）の**上書き直前に現物を `settings_backup/settings_YYYYMMDD_HHMMSS_mmm.json` へ退避**。直近50世代を保持し古い物のみ剪定（`BACKUP_KEEP`）。バックアップ失敗は保存を止めない（例外を飲み込む）。`settings_backup/` は gitignore。原子的書込みでは救えない「誤った内容で正しく上書き」事故を、Drive版履歴に頼らずツール内の1ファイルで復元可能に。復元は **`/self` の「💾 設定のバックアップと復元」からワンクリック**（一覧→選んで復元。復元前に現状も自動退避＝戻したあと元へも可）。API＝`GET /api/settings-backups`・`POST /api/settings-restore`（`settings.js` の `listSettingsBackups`/`restoreSettingsBackup`、ファイル名検証でトラバーサル防止）。
- [x] **パスワード（`accessPassword`）のハッシュ化、リクエスト本文サイズ上限（DoS対策）（2026-06-28 確認・実装済）**：①ハッシュ化＝`settings.js` の `hashAccessPassword`（`sha256$<hex>`・固定ペッパー `apwhash|`）。`saveSettings` が保存時にハッシュ化、`getSettings` は出力で平文を空にし `hasAccessPassword` 真偽のみ返す。`/api/login` が `verifyAccessPassword` でハッシュ比較（旧平文とも後方互換）。②本文上限＝`server.js` の `MAX_BODY_BYTES=20MB`、`readBody` が `req.on('data')` 累積で超過時 reject＝全POSTが経由（巨大POSTのOOM/DoS対策）。
- [ ] 日付正規化／`normLinkName` の統合整理。

**2026-06-28 アラ探し（コードレビュー）：自宅で直した分＋会社PCへの引き継ぎ**

> 4領域の並行レビュー＋現物裏取りで洗い出し。致命バグは無し（よく作り込まれている）。以下は確認済みの本物のみ。

- [x] **自宅で修正済み（DB不要・検証済・※要 sim 再起動）**：
  - **① `rules.js` `sell_cost_rate` の0円ガード**：新原価0のとき `現売価×0=0円` を出していた（`target_margin_rate`/`keep_margin_rate` は対応済みなのに非対称）。`currentCost>0 && newCost>0` に修正＝0なら絶対額上乗せにフォールバック（90等）。単体検証3/3。**照合エンジン非改変＝matchaudit不変のはず。**
  - **② `customersPage.js` 行ルールの factor 持ち越し**：行を「掛率×1.25」→「粗利率%」へ変えると 1.25 が 1.25% と誤解釈された。種別変更時に `delete rowFactor[k]`（全体既定から出し直し）。配信クライアントJS構文0エラー確認。
  - **③ AIキーの案内矛盾＋マスク**：CLAUDE.md は「`settings.json` の `ai.apiKey`」可と案内していたが `ai.js` は環境変数のみ読む（書いても無効）。文書を実装に合わせ修正＋`server.js` `settingsForClient` で `ai.apiKey` も防御的にマスク（元設定を壊さないよう複製してから）。単体検証4/4。
- [x] **【DB必須・2026-06-29 会社PCで実施・合格】⑥ `match.js` 集約キーの区切り文字追加**：`matchOne`/`matchSelf` の集約キー（得意先コード+商品コードの区切り無し連結）は、**可変長コードのファイル/SaaS運用**で「得意先123×商品4567」と「1234×567」が同一キー衝突し明細が黙って消える潜在バグ（DB運用は固定6桁ゼロ詰めで衝突せず実害なし）。**両箇所に制御文字区切り `\u0001` を挿入**（コードに絶対出現しない＝衝突不能）。`node src/matchaudit.js` 前後比較で**完全無回帰**：総行数4714・一致3697(CD+自社1918/名前381/手動1398)・休眠1017・**要確認0**・名前一致平均99.4%・**日野自社製造1052**・recall334/355(94.1%)・false positive0、全仕入先別内訳まで1件残らず同一（総行数不変＝キー衝突による明細消失/重複ゼロの直接証拠）。`node -c` OK。**※要 sim 再起動（エンジン変更）→ ↻照合 → `/api/dup-check`=0 を本番でも最終確認。**
- [~] **④⑤ 外部データ取込の堅牢化（保留＝効果が見える品が出てから）**：④`csv.js:35` フィールド途中の裸の `"`（例 `5"`）で行崩れ＝外部の非整形CSV取込時のみ（自前生成CSVは安全）。⑤`xlsxread.js:90` `r=`属性無しセルを無視＝Excel以外で生成の.xlsx取込時のみ（純正Excelは安全）。いずれも実害が出るファイルを実際に踏んでから対応（#3/#4と同方針）。

> 改良に着手する前に **`node src/matchaudit.js` の数字（一致/休眠/要確認の件数）を控える**と、変更の影響を前後比較で安全に判断できる。

---

## 📌 現売価＝「売上単価履歴」を反映（2026-06-30 実装・会社PC検証合格／※要 sim 再起動）

> **背景**：運用は「見積ツールで決めた単価をCSVで大臣に取込み、**売上単価履歴(`URIRIREKI`)**を更新」。実際の販売時は大臣がこの履歴から単価を拾う＝**履歴が“合意済みの現在価格”の真実**。ところがツールの現売価は**売上伝票(`URIMEI`)の最新実売だけ**を読んでいたため、実出荷が止まった休眠得意先で古い実売価が残った（例：おむすびや立樹0009×パイロンセロハンテープ004127＝履歴110円/2026-05-01 が入っているのに、現売価が2018年の実売70円のまま）。「現売価×仕入改定%」の2次値上げが70起点→84円と誤算出される問題。

- [x] **現売価を2系統に分離（`db_hanbai.js`／`match.js`）**：
  - `currentSell`＝**実売(伝票)の最新**。`match.js` の `matchOne`（赤字ガード `belowCost` 等）が使う＝**照合判定は完全に不変**。
  - `effectiveSell`＝**出力/表示/値上げ計算用**。`URIRIREKI`（`BARACASEKBN=1`＝バラ・最新DATE・TANKは実売と同じ×10000）と実売の**“日付が新しい方”**を採用。`match.js` の `matchAll` 出力1行（`currentSell:`）だけが `effectiveSell` を使う。ファイル経路(`hanbai.js`)は `effectiveSell` 無し＝従来どおり実売（後方互換）。
  - SQLに `hist` CTE（`URIRIREKI` 最新バラ）を `LEFT JOIN`。`MAS1`=得意先ICODE・`MAS2`=商品ICODE。`histSell`/`histDate` もレコードに保持（透明性）。
- **検証（会社PC・DB／再照合→matchaudit前後比較）**：**完全無回帰**＝総行数4713・一致3697(CD+自社1915/名前382/手動1400)・休眠1016・**要確認0**・名前平均99.4%・**日野自社製造1053**／**`/api/dup-check`=0**。おむすびや立樹の新CSV現売価が **70→110** に修正。「新しい方が勝つ」も実証（豊松農産0265は実売2026-05-13が履歴2026-05-01より新しく実売110維持・履歴126で上書きしない）。影響＝**1606件**（履歴が実売より新しい品。大半は休眠＝年間金額0で損益影響は軽微）。`sell_cost_rate` 検算：現売価110×(99/82.5)=**132円**（従来70起点なら誤84円）。
- **狙い**：休眠でも履歴の合意単価が現売価に出る／直近に実売がある得意先はその実売を優先＝既存挙動を壊さない。**単価の手動編集ゼロ**で、既存の単価履歴エクスポートCSV（`hanbai_export.js`）に正しい単価が乗る。**※要 sim 再起動（エンジン変更）。**

---

## 最優先事項

1. **品質を最優先する**  
   スピードより「正しく動く」ことを優先する。動作未確認のコードを「できた」と言わない。変更後は `node -c`・該当スクリプトの実行・必要なら `sim.bat` 再起動まで含めて確認する。

2. **動作を重くしない**  
   不要なライブラリや過剰な処理を避け、軽い実装にする。重くなる選択（全件再読込の増加、キャッシュ破棄の乱用、同期I/Oの連打など）をするときは、**先に理由を説明する**。  
   本プロジェクトは **npm 依存ゼロ** が設計原則。新規 `npm install` は原則禁止。

3. **できないことは「できない」と正直に言う**  
   推測で実装を進めない。不明点・難しい点は勝手に判断せず、**先に確認する**（例：自宅PCでDB再照合不可、実データの所在、仕入先名の正式表記など）。

---

## 進め方

- **大きな変更は段階的に**進め、区切りごとに「何をしたか・次に何をするか」を報告し、確認を取ってから次へ進む。
- **既存の動いているコードを勝手に書き換え・削除しない**。変更時は理由を説明してから行う。
- 照合エンジン（`match.js`）や販売実績ローダの変更は副作用が大きい。**会社PC＋DB再照合での検証**が必要な場合は、その旨を明示する（自宅PCでは日野9000の自社製造照合が劣化する事故あり）。

---

## セキュリティ

- **APIキーやパスワードをコードに直接書かない**。  
  - AI: 環境変数 `ANTHROPIC_API_KEY` のみ（`src/ai.js`）。**`settings.json` の `ai.apiKey` には保存しない**（`ai.js` は環境変数しか読まない＝書いても無効。なお `settingsForClient` で出力時にマスク済み）  
  - 画面ロック: `settings.json` の `accessPassword`（ハッシュのみ保存）  
  - DB接続: `config.js` / `settings.json` の `hanbai.db`（gitignore 対象の `settings.json` で上書き）
- **販売大臣DBは読み取り専用（SELECTのみ）**。INSERT/UPDATE/DELETE/DDL は絶対禁止。
- `settings.json`・`input/`・`output/`・`maker_quotes/` は **gitignore**（業務データをコミットしない）。

---

## その他

- **説明・コメント・コミットメッセージはすべて日本語**で行う。
- **ツール実行時の説明文（description）も必ず日本語で書く**。許可確認の画面に
  表示される「このコマンドが何をするか」の一文のこと。利用者はこれを読んで
  Yes/No を判断するため、日本語であることが特に重要。
- **`.bat` ファイルの本文は ASCII のみ**（日本語を書くと cmd が化ける。案内は `node` 側の `console.log` に任せる）。
- コード変更後は **`sim.bat` の再起動**が必要（Node は起動時に一度だけ読み込む。二重起動防止で古いサーバが残ると新コードが配信されない）。
- 2台PC（Google Drive同期）では **同期完了を待ってから編集**。同時編集しない。

---

## 使用技術

| 区分 | 内容 |
|------|------|
| **言語** | **JavaScript（Node.js）** のみ。CommonJS（`require` / `module.exports`）。TypeScript・Python・PHP・React・Vue は**未使用**。 |
| **ランタイム** | Node.js LTS（`package.json` あり。**`npm install` 不要**＝`dependencies` なし） |
| **Webサーバ** | Node 標準の **`http` モジュールのみ**（Express / Fastify 等なし） |
| **フロントエンド** | **バニラ JavaScript**（HTML/CSS を `server.js` や `*Page.js` が文字列テンプレートで配信）。ビルドツール・SPAフレームワークなし |
| **Excel** | 自前実装：`xlsxread.js`（zlib で .xlsx ZIP 解凍）、`xlsx.js` / `xlsxutil.js`（書き出し）、`quoteXlsx.js`（見積書レイアウト） |
| **CSV** | 自前 `csv.js`（UTF-8 / Shift-JIS 自動判定） |
| **DB（会社PCのみ）** | Microsoft SQL Server（販売大臣）へ **PowerShell + .NET SqlClient** 経由で読取（`db_hanbai.js`） |
| **旧Excel .XLS** | Windows + Excel COM（`xls2csv.js`）。照合.bat 実行時のみ。クラウドでは未使用 |
| **AI（任意・既定OFF）** | Claude API を `https` 直叩き（`ai.js`）。キー未設定時は外部送信ゼロ |
| **バッチ** | Windows `.bat`（`sim.bat` / `照合.bat` / `取込.bat` / `run.bat` / `得意先別.bat`） |
| **バージョン管理** | Git（業務データは `.gitignore` で除外） |

---

## このツールは何か

メーカーからの値上げに対し、**得意先ごとの「転嫁後 販売単価」を計算し、見積書（Excel）を出力**する社内ツール。

**役割分担（確定）**

| 画面 | 役割 |
|------|------|
| **メイン（sim `/`）** | 照合・紐付けの確認、コスト確認、全体損益 |
| **得意先別（`/customers`）** | 転嫁ルール・単価調整・見積書発行 |
| **取込（`/import`）** | メーカー見積の貼り付け / ファイル取込 |
| **コード化（`/cdlink`）** | 商品名3（メーカー品番）登録候補の確定 |

---

## ディレクトリ構成

```
価格転嫁見積ツール/
├── config.js              # 初期値（会社情報・既定ルール・DB接続テンプレ）
├── settings.json          # 利用者設定（gitignore・Drive同期）
├── package.json           # npm メタのみ（依存パッケージなし）
├── sim.bat / 照合.bat / 取込.bat / run.bat / 得意先別.bat
├── 使い方手順書.md / .html
├── src/                   # ソース一式（下表）
├── input/                 # 照合結果CSV（gitignore）
├── output/                # 見積書・発行履歴等（gitignore）
├── maker_quotes/          # メーカー見積CSV（gitignore）
├── CLAUDE.md              # 本ファイル（AI向けガイド）
└── CLAUDE_作業履歴.md     # 過去の詳細作業ログ
```

---

## 主要モジュール（`src/`）

| ファイル | 役割 |
|----------|------|
| `server.js` | 中核。HTTPサーバ・API・メイン画面・設定・見積出力 |
| `customersPage.js` | 得意先別ページ（見積作成・発行） |
| `importPage.js` | メーカー見積取込画面 |
| `shogo.js` | 照合実行（`照合.bat` の中身）。メーカー見積統合 `mergeMakerFiles` |
| `match.js` | 照合エンジン（CD一致・名前一致・手動紐付け） |
| `hanbai.js` / `db_hanbai.js` | 販売実績ローダ（ファイル / DB直結） |
| `rules.js` | 転嫁ルール計算 |
| `settings.js` | `config.js` + `settings.json` の合成・保存 |
| `quoteXlsx.js` / `xlsx.js` | 見積書 Excel 出力 |
| `makerXlsx.js` / `xlsxread.js` | メーカー見積 .xlsx 読込 |
| `productLink.js` | 手動紐付け（📌）の共通ロジック |
| `merge_quotes.js` | 得意先別見積の統合（`得意先別.bat`） |
| `ai.js` | AI取り込みアシスト（既定OFF） |
| `pruneInput.js` | 古い照合CSVの `input/_old` 退避 |
| `navUi.js` | 全ページ共通ヘッダーナビの唯一の情報源（`navLinks(現在ページ)`）。**ページを足す/順番を変えるときはここだけ直す**＝全ページに反映（メイン`/`は緑CTA等が固有のため手書き：リンク追加時は server.js も要追記） |

---

## 典型的な運用フロー

1. メーカー見積を **`/import`** または `取込.bat` / `maker_quotes/` へ配置  
2. **`照合.bat`**（または sim の「↻ 照合」）→ `input/<仕入先>_照合結果_*.csv`  
3. **`sim.bat`** → ブラウザ `http://localhost:8765` で照合確認  
4. **`/customers`** で得意先ごとに単価・実施日を決めて見積発行  

**問屋経由のメーカー**（例：中央化学・エフピコ → 朝日食品容器）は `settings.json` の `makerChannel` で寄せる。  
**20%などの改定単価を反映するときは、取込仕入先を「朝日食品容器」で保存する**（中央化学名義だけだと古い朝日取込に負けることがある）。

---

## 開発・検証の注意

- **配信クライアントJS**は `server.js` 内のテンプレートリテラル経由。正規表現は **`\\d` `\\s` 等の二重エスケープ**が必要（単一 `\` だと SyntaxError で画面全壊）。
- 構文確認: `node -c src/server.js`  
- 単体テスト例: `node src/productLink.js`、`node src/matchaudit.js`  
- **自宅PC（DBなし）では `照合.bat` の本番再照合を避ける**（日野9000自社製造照合の劣化事故あり）。データ修正・merge検証は可。2026-06-17以降は `hanbai.source='db'`（厳格）なので、自宅PCの sim は自動で **🔒閲覧モード**（input/ の照合結果CSVで表示専用・照合とDB抽出は不可）になる＝画面は壊れない。照合は会社PCで実行し、結果CSVがDrive同期で自宅へ届く運用。
- 会社PCでの定番チェック: 再照合後に `/api/dup-check`=0、かつ 日野の自社製造の休眠が内部/サンプル品（`預り分`/`製造ﾏｽﾀ`/`使用しな`/`ｻﾝﾌﾟﾙ`）のみであること。自社製造一致の件数は得意先×自社品のローリング窓で月次変動する（2026-06-15時点=1051／旧記録1085は5月末値）。固定値では判定しない。

---

## 参照ドキュメント

- 利用者向け手順: `使い方手順書.md` / `使い方手順書.html`（sim の「📖 使い方」からも開ける）
- 概要: `README.md`
- 詳細な過去セッションの記録: `CLAUDE_作業履歴.md`

---

## 📚 共通ノウハウ集（作業前に該当章を必ず参照）

過去の全プロジェクトの定石・教訓・事故記録が
`G:\マイドライブ\Claudecode\ノウハウ集.md` に集約されている（このプロジェクトの教訓も多数収録）。
次にあてはまる作業を始める前に、**必ず該当章を読んでから**進めること。

- 販売大臣・SQL Server など基幹データに触れる → 第4章 4-1（テーブル知識・読み取り専用の定石）
- CSV・Excel・JANコード・文字コード（文字化け）を扱う → 4-2
- エックスサーバー等レンタルサーバへの設置・PHP → 4-3（Webのセキュリティは 4-9）
- Google ドライブ上のフォルダで開発する（node_modules 等） → 4-4
- .bat・タスクスケジューラ・Word/Excel 自動化など Windows まわり → 4-5（Excel整形は 4-8）
- Claude API など AI 機能を組み込む → 4-6
- カラーミーショップ（EC）の SEO・テンプレート → 4-7
- 新しいツールの設計・構成を決める → 第2章・第3章（共通原則）
- 原本を人に渡す → 第7章（譲渡の手順と注意）

**ノウハウの書き戻し**：作業中に新しいハマりどころ・定石を発見して解決したら、
セッションの終わりに「ノウハウ集への追記案」を利用者に提案すること
（ノウハウ集を勝手に書き換えず、案を示して承認を得てから追記する）。

---

## 🔍 リサーチ・思考の規律（Claude Fable 5 システムプロンプトより抽出）

「最優先事項」3（できないことは正直に言う）を補強する具体的な規律。調査・検索・判断のときは以下に従う。

### いつ検索するか
1. **変わらない知識は検索しない** — 歴史的事実・科学原理・定義・確立した技術知識は自分の知識で即答する。
2. **「現在の状態」は必ず検索** — 役職・地位・政策・法律・価格・バージョンなど「今どうなっているか」は、変化が遅いものでも検索して確認する。
3. **知らない固有名詞は必ず検索** — 認識できない製品名・ツール名・バージョン名は、答える前に必ず調べる。
   **シリーズや作者を知っていることは、新作を知っていることではない**。
4. **比較では項目ごとに適用** — 既知のものが多くても、知らない1つは推測で並べず個別に調べる。

### 検索のやり方
5. **クエリは1〜6語の短さで** — まず広く（1〜2語）検索し、必要なら絞り込む。似たクエリを繰り返さない。
6. **年を入れるなら実際の現在の年** — 古い年を入れると古い結果が返る。
7. **スニペットで満足しない** — 検索結果の断片では足りないときは、ページ全文を取得して読む。
   URLを示されたら必ずそのURLを直接開く。
8. **調査の規模を複雑さに合わせる** — 単一の事実は1回、中程度は3〜5回、深い調査は5〜10回。
   複雑な調査はまず「何をどの順で調べるか」の計画を立ててから着手する。

### 情報源の選び方
9. **一次情報源を優先** — 公式ドキュメント・公式ブログ・査読論文・政府サイトを、まとめサイト・二次情報より優先する。フォーラム等の低品質ソースは原則参考程度に。
10. **矛盾は放置しない** — ソース同士が食い違ったら明記し、追加で調べて解消する。

### 判断（認識論）の規律
11. **検索結果は基本的に信じる** — 自分の知識と食い違う「驚くべき結果」でも受け入れる。
    ただし、陰謀論の対象になりやすい話題や **SEOの影響が強い話題（商品推薦など）** は、上位表示でも疑う。
12. **「見つけられなかった」と「存在しない」を混同しない** — 検索結果の有無について断定に飛びつかない。
13. **謙虚さと実質を両立** — 不確実さは認めつつ、必ず実質的な回答をする。「調べましょうか？」だけで終わらせない。
14. **推測で他者の動機・心理を語らない** — 検証できないことは断定しない。
15. **間違えたら言い訳せず認めて直す** — ただし過剰な謝罪はせず、問題解決に集中する。

## 🚧 作業終了時ルール（開発途中フラグ）

このプロジェクトでの作業を「終わります／おつかれ」等で締めるとき、Claude は
**必ず利用者に次を確認し、その答えでこのフォルダ直下の `_続き作業あり.txt` を作成／削除する**。

**聞き方**：口頭で流さず、必ず `AskUserQuestion`（選択式ボタン）で、**推奨を第一選択に置いて**聞く（利用者が押すだけで答えられる形にする）。

確認する内容：

1. **このツールは「開発途中」か、「区切りがついた」か**（実態に合う方を推奨＝第一選択に置く）
2. **次に開く人（＝翌日の自分）へ残すメモ**。次の3点で簡潔に：
   **①やったこと**（どこまで完了したか）／**②次の一歩**（次に何をするか）／**③確認待ち・注意**（利用者や取引先への確認事項）
3. **ノウハウ集に残す学びはあるか**を同じ締めで一緒に確認する（別々に聞かない。追記は「共通ノウハウ集」節に従い、勝手に書き換えず案を示して承認を得る）

対応：

- **続きがある** → このプロジェクトフォルダ直下に `_続き作業あり.txt` を作成し、本文に上記メモを書く。
  この本文が **ツールハブのカードに黄色帯で表示される**（＝翌日ハブを開けば一目で分かる）。
- **区切りがついた** → `_続き作業あり.txt` があれば削除する（無ければ何もしない）。
  消すのは **この固有名マーカーだけ**。他のファイルには触れない（データ保護ルール厳守）。

補足：

- マーカーの置き場所は「いま作業している、このプロジェクトフォルダの直下」。
  ファイル名は必ず `_続き作業あり.txt`（ツールハブがこの名前を読む）。
- ツールハブ（`G:\マイドライブ\Claudecode\ツールハブ`）はこのマーカーを**読むだけ**。
  翌日ハブを立ち上げると「🚧 開発途中」バッジとメモが表示される。
- **手動での付け外しは不要**。押し忘れ（実態とのズレ）を防ぐため、作業終了時に Claude が自動で行う。

